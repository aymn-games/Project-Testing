# CLAUDE.md — دليل العمل على المشروع (للمطوّرين ونماذج الذكاء الاصطناعي)

> هذا الملف هو **المرجع الإلزامي الأول** لأي مطوّر أو نموذج ذكاء اصطناعي
> (بما فيها Claude) قبل كتابة أي كود جديد أو تعديل أي ملف في هذا المشروع.
> إن تعارض أي طلب مستقبلي مع ما هو مذكور هنا، فالأولوية لما هو مذكور هنا،
> ويجب توضيح التعارض للمستخدم بدل تجاهله.

## 1) فلسفة المشروع (بإيجاز)

المشروع هو **منصة ألعاب جماعية للبث المباشر**، ليس مجرد موقع ثابت.
البنية الحالية (`AGP.*`) هي أساس تحويل الموقع تدريجياً من صفحة عرض
إلى منصة حقيقية، دون كسر أي شيء يعمل حالياً. راجع تفاصيل الفكرة
والأهداف في `PROJECT_VISION.md`، والقواعد التشغيلية في `PLATFORM_RULES.md`،
وفلسفة الواجهة في `UI_GUIDELINES.md`.

القاعدة الذهبية: **"إضافة فوق البنية القائمة، لا استبدال لها."**

## 2) ما الذي يُمنع تغييره

هذه أشياء لا تُعدَّل أبداً إلا بطلب صريح وواضح من صاحب المشروع، ومع فهم
كامل لأثر التغيير:

- **لا تعديل على منطق الألعاب الحالية** (روليت القبائل، مين الامبوستر،
  مافيا) أو طريقة تشغيلها/روابطها.
- **لا تعديل على HTML/CSS الحالي في `index.html`** لمجرد "تحسين" أو
  إعادة تنسيق، إلا إذا طُلب ذلك تحديداً.
- **لا تغيير في ترتيب تحميل ملفات JS** (`agp-core.js` →
  `agp-events.js` → `agp-timer-manager.js` → `agp-storage-manager.js` →
  `agp-services.js` → `agp-stream-connector.js` → `agp-registry.js` →
  `agp-session.js` → `agp-rooms-manager.js` → `agp-player-manager.js` →
  `agp-player-source.js` → `agp-keyword-manager.js` →
  `agp-queue-manager.js` → `agp-score-manager.js` → `agp-lobby.js` →
  `agp-game-api.js` → `agp-game-engine.js` → `agp-game-manager.js` →
  `agp-game-bridge.js` → `games/<game-id>/agp-<game-id>.js` →
  `agp-round-manager.js` → `agp-bootstrap.js`)، فكل ملف يعتمد على الذي
  قبله أو على ما هو مذكور صراحة في تعليقات رأسه.
- **لا كتابة مباشرة على `window`** أو متغيرات الصفحة العامة؛ كل كود
  جديد يوضع حصراً داخل `window.AymanGamesPlatform` (`AGP`).
- **لا تفعيل ميزات غير مكتملة**: أي `feature flag` في `AGP.config.features`
  يبقى `false` حتى تكتمل الميزة فعلياً وتُختبر.
- **لا حذف أو إعادة كتابة ملفات كاملة** (`agp-core.js` وغيره) إلا عند
  ضرورة حقيقية موثقة؛ التعديل الجزئي (إضافة دوال/كائنات جديدة) هو
  الافتراضي دائماً.
- **لا إضافة اتصال شبكي حقيقي** (WebSocket، APIs خارجية لتيك توك/يوتيوب/
  تويتش) قبل أن تصل خارطة الطريق (`ROADMAP.md`) لتلك المرحلة صراحة.

## 3) طريقة كتابة الكود

- **اتبع نمط الملفات الحالية بدقة**:
  - IIFE يستقبل `AGP` كوسيط: `(function (AGP) { 'use strict'; ... }(window.AymanGamesPlatform));`
  - حارس بسيط في بداية كل ملف يعتمد على `agp-core.js`:
    `if (!AGP.log) { AGP.log = function () {}; }`
  - تعليقات توثيقية بالعربية بنفس الأسلوب المستخدم حالياً (شرح الغرض،
    لا وصف حرفي للكود).
- **JavaScript عادي (Vanilla JS)** بدون أي إطار عمل (Framework) أو أداة
  بناء (Build tool)، إلا إذا تقرر تغيير ذلك صراحة مستقبلاً.
- **كل شيء جديد يدخل عبر `AGP.*` فقط**: خدمة جديدة → `AGP.services.X`،
  حدث جديد → يُبث عبر `AGP.events.emit`، إعداد جديد → `AGP.config`.
- **التزم باصطلاح تسمية الأحداث الموثَّق في `agp-events.js`**
  (`namespace:action`، مثل `session:created`، `player:joined`،
  `lobby:opened`، `game:registered`)، واحجز Namespace خاصاً بأي وحدة
  جديدة (`round:*`, `tiktok:*`, `cloudflare:*`, `network:*`) بدل
  استدعاء دوال وحدة أخرى مباشرة.
- **استخدم `AGP.log` بدل `console.log` مباشرة** لأي رسالة تصحيح، حتى
  يبقى التحكم بها مركزياً عبر `AGP.config.debug`.
- **لا اعتماديات خارجية (external libraries/CDN)** لأي منطق داخل `AGP.*`
  إلا بموافقة صريحة، حفاظاً على خفة الموقع وبساطته.

## 4) طريقة إضافة لعبة جديدة

اتبع النمط المتبع حالياً بدقة (كما في الألعاب الثلاث الموجودة):

1. أضف بطاقة `.game-card` جديدة داخل `index.html` بنفس البنية الحالية
   (نفس الأصناف: `.game-cover`, `.game-title`, `.btn-play` ...).
2. أضف خاصية `data-agp-game-id` بمعرّف فريد وواضح للعبة (مثلاً
   `data-agp-game-id="new-game-name"`)، وإن أمكن `data-agp-game-status`
   (`active` أو غيرها حسب الحاجة).
3. **لا حاجة لتعديل `agp-registry.js`**: دالة الاكتشاف التلقائي
   (`_discoverGamesFromDOM`) ستلتقط البطاقة الجديدة تلقائياً عند تحميل
   الصفحة، طالما اتبعت نفس تسمية الأصناف.
4. إن احتاجت اللعبة منطقاً خاصاً بها (مثل صفحة لعب منفصلة)، يُبنى هذا
   المنطق في ملفاتها الخاصة، ولا يُقحم داخل ملفات `agp-*.js` العامة إلا
   إذا كان منطقاً مشتركاً فعلياً بين عدة ألعاب (عندها يصبح `Service`
   عاماً في `agp-services.js`).
5. إن احتاجت اللعبة **ربطاً برمجياً فعلياً** بدورة حياة المنصة (فتح/
   إغلاق التسجيل، بدء/انتهاء جولة، انضمام/مغادرة لاعب)، تُسجَّل كـ
   "كائن لعبة" (Game Object) عبر `AGP.gameAPI.register({...})` وفق
   العقد الموحّد الموثَّق في `agp-game-api.js` (دوال اختيارية مثل
   `onLoad`, `onLobbyOpen`, `onRoundStart`, `onPlayerJoin` ...). هذا
   منفصل تماماً عن بطاقة `.game-card` في الخطوة 1-2 (تلك للعرض، وهذا
   للمنطق)، ولا يوجد بعد Game Engine حقيقي يستدعي هذه الدوال كمنطق لعب
   كامل — الموجود حالياً هو فقط تمرير بسيط للأحداث العامة.
6. لتشغيل لعبة **مسجَّلة بالفعل** فعلياً (تحميلها، تشغيلها، إيقافها،
   تدميرها)، استخدم `AGP.gameEngine` (`agp-game-engine.js`):
   `loadGame(id)` ثم `start()` / `stop()` / `destroy()`. المحرك الحالي
   نسخة أولية (Minimal) تدير لعبة واحدة محمَّلة فقط؛ `onLoad()` تُستدعى
   عند `loadGame()` وليس عند `register()`، حتى لا تُستدعى مرتين.
   **تحديث**: `stop()`/`destroy()` تُستدعيان الآن **تلقائياً** أيضاً
   عند `game:roundEnded`/`game:reset` (مزامنة عامة مضافة داخل
   `agp-game-engine.js` نفسه، تعمل لأي لعبة تُبلِّغ بنفس اسمي الحدثين)،
   بالإضافة للاستدعاء اليدوي المباشر الموضَّح أعلاه.
7. **مرجع فعلي**: `games/roulette/agp-roulette.js` هو أول "Plugin"
   حقيقي يربط لعبة موجودة (روليت القبائل) بالمنصة، ويجب أن يُحتذى به
   لأي لعبة جديدة. كل لعبة تملك ملف ربط (Adapter) خاص بها تحت
   `games/<game-id>/agp-<game-id>.js`، يسجّل نفسه عبر
   `AGP.gameManager.registerGame(...)` بعد الاستماع لحدث `platform:ready`
   (لضمان اكتمال اكتشاف بطاقات الألعاب أولاً). **ملاحظة مهمة (محدَّثة)**:
   إن كانت اللعبة مستضافة على نطاق خارجي منفصل (كحال الروليت، حيث الكود
   الفعلي غير موجود ضمن هذا المشروع)، فالربط الفعلي يمر إلزامياً عبر
   **`js/agp-game-bridge.js`** — جسر `postMessage` عام مُستخرَج من منطق
   الروليت نفسه، يُستدعى عبر `AGP.gameBridge.connect({ id, playLinkEl,
   incomingSource, reportedEvents, ... })`. **لا يُعاد كتابة منطق فتح
   النافذة أو postMessage يدوياً داخل أي ملف Adapter جديد**؛ الـ Adapter
   يقتصر على التسجيل في Game API + استدعاء `AGP.gameBridge.connect`
   بخيارات اللعبة الخاصة (معرّفها، اسم `source` الذي تستخدمه، وأنواع
   الأحداث التي تُبلِّغ بها). `games/roulette/agp-roulette.js` هو
   المرجع الحي لهذا النمط بعد استخراج الجسر.
8. **نقطة الدخول الموصى بها لأي كود جديد**: `AGP.gameManager`
   (`agp-game-manager.js`) هو واجهة موحّدة (Facade) فوق `AGP.gameAPI` +
   `AGP.gameEngine` — `registerGame/unregisterGame/loadGame/unloadGame/
   getCurrentGame/getRegisteredGames`. أي كود **جديد** يحتاج التعامل مع
   الألعاب يستخدم هذه الواجهة بدل استدعاء `AGP.gameAPI`/`AGP.gameEngine`
   مباشرة. **ملاحظة صادقة**: `games/roulette/agp-roulette.js` (الملف
   الوحيد الذي يشغّل لعبة حقيقية حالياً) لا يزال يستدعي `AGP.gameAPI`/
   `AGP.gameEngine` مباشرة كما كان قبل إنشاء الواجهة الموحّدة — لم
   يُعدَّل عمداً لتفادي أي تغيير غير ضروري على كود يعمل فعلياً. لا تفترض
   أنه يستخدم `AGP.gameManager`.
5. راجع `PLATFORM_RULES.md` لقواعد الجلسات والانضمام قبل تصميم أي تدفق
   لعب جديد.

## 5) كيف يتم تطوير المشروع مستقبلاً

- اتبع الترتيب الموضح في `ROADMAP.md` كمرجع عام للأولويات؛ لا تقفز إلى
  مرحلة متقدمة (مثل التكامل مع تيك توك) قبل إنجاز الأساس (مثل الغرف
  والجلسات الحقيقية) ما لم يُطلب ذلك صراحة.
- أي ميزة جديدة تبدأ دائماً بـ:
  1. تعريف الهيكل/الواجهة أولاً (Skeleton) دون منطق فعلي، كما هو متبع
     في `agp-services.js` حالياً.
  2. تفعيل جزئي ومختبر بعيداً عن الإنتاج إن أمكن.
  3. تفعيل كامل عبر `feature flag` بعد التأكد من عدم كسر أي شيء قائم.
- عند الشك في نطاق أو تأثير أي تعديل، السؤال المرجعي دائماً: **"هل هذا
  التعديل يغيّر سلوك الألعاب أو الواجهة الحالية للمستخدم النهائي؟"** إن
  كانت الإجابة نعم دون طلب صريح بذلك، فالتعديل غير مسموح به بصيغته تلك.

## 6) الالتزام بعدم كسر التوافق مع الألعاب الحالية

- أي تعديل على `agp-core.js`, `agp-services.js`, `agp-registry.js`, أو
  `agp-bootstrap.js` يجب اختباره ذهنياً (أو فعلياً) للتأكد من أن:
  - الروليت، مين الامبوستر، ومافيا ما زالت تُفتح وتعمل بنفس الطريقة.
  - البانر المتبدّل، عداد الزوار، والنوافذ المنبثقة (السياسة/الوصف) لم
    تتأثر إطلاقاً.
  - لا أخطاء جديدة تظهر في الـ Console عند تحميل الصفحة.
- أي دالة أو كائن موجود حالياً (مثل `AGP.registry.getAllGames`) يجب أن
  يبقى بنفس التوقيع (Signature) والسلوك عند التعديل، ما لم يكن التغيير
  مقصوداً وموثقاً في `CHANGELOG.md`.

## 7) البناء فوق النظام الحالي، لا إعادة كتابته

- الافتراضي دائماً هو **التوسعة (Extend)** لا **إعادة الكتابة
  (Rewrite)**. إذا بدا أن ملفاً معيناً "يحتاج إعادة هيكلة كاملة"،
  التوقف وطرح ذلك على صاحب المشروع صراحة بدل تنفيذه من تلقاء نفسك.
- أي كود جديد يُفضَّل أن يُضاف كملف جديد ضمن نفس نمط `agp-*.js` (أو
  ضمن مجلد فرعي منظم لاحقاً)، مع تحديث ترتيب التحميل في `index.html`
  إن استدعى الأمر ذلك، بدل حشو الملفات الحالية بمنطق غير متعلق بغرضها
  الأصلي.
- كل ملف جديد يوثَّق في `CHANGELOG.md` تحت قسم `[Unreleased]` أو رقم
  إصدار جديد، ويُحدَّث `ROADMAP.md` إذا غيّر ذلك من المراحل المخطط لها.

---

> بإيجاز شديد: **اقرأ قبل أن تكتب، وسّع قبل أن تعيد الكتابة، واختبر
> التوافق قبل أن تعتبر أي تعديل مكتملاً.**
