# قواعد المنصة — AymanGamesPlatform

> هذا الملف يوثّق القواعد المفاهيمية (Conceptual Rules) التي يجب أن
> تحكم أي تطوير مستقبلي للجلسات، الغرف، واللاعبين داخل المنصة، حتى قبل
> أن يوجد تنفيذ فعلي لها في الكود. هدفها هو ضمان اتساق القرارات بين كل
> الألعاب (روليت، مين الامبوستر، مافيا، وأي لعبة قادمة).

## 1) قواعد انضمام اللاعبين

- الانضمام إلى أي غرفة يتم دائماً عبر **رابط اللعبة أو كود غرفة**، لا عبر
  حسابات مستخدمين دائمة (لا يوجد "تسجيل دخول" في هذه المرحلة).
- كل لاعب يُمثَّل داخل المنصة ككائن بسيط (اسم، معرّف مؤقت، حالة) عبر
  `PlayersService` مستقبلاً، دون تخزين بيانات حساسة أو دائمة.
- يُسمح بانضمام لاعب جديد فقط طالما الغرفة **لم تبدأ اللعبة فعلياً** أو
  طالما اللعبة تسمح بالانضمام المتأخر (يُحدَّد ذلك لكل لعبة على حدة).
- عند امتلاء الغرفة أو انتهاء الجلسة، يجب إعلام اللاعب بوضوح بدل تركه
  في حالة انتظار غامضة.

## 2) قواعد الجلسات (Sessions / Rooms)

- كل جلسة/غرفة تخص **لعبة واحدة فقط** في وقت واحد؛ لا يوجد خلط بين
  ألعاب مختلفة داخل نفس الغرفة.
- الغرفة لها دورة حياة واضحة: **إنشاء → انتظار لاعبين → قيد اللعب →
  انتهاء**. أي حالة جديدة تُضاف مستقبلاً يجب أن تحترم هذا التسلسل.
- الهوست هو المسؤول الوحيد عن بدء الجلسة وإنهائها؛ اللاعبون العاديون لا
  يملكون صلاحية إنهاء الجلسة لبقية المشاركين.
- عند انقطاع الاتصال (مستقبلاً عبر `NetworkService`)، يجب أن تحافظ
  الجلسة على حالتها لفترة معقولة قبل اعتبار اللاعب "خارج الغرفة"، بدل
  إسقاطه فوراً.

## 3) قواعد تغيير الألعاب

- الانتقال بين الألعاب يتم فقط من الصفحة الرئيسية (Hub)، ولا يوجد
  "تبديل لعبة" من داخل جلسة قائمة؛ يجب إنهاء/مغادرة الجلسة الحالية أولاً.
- كل لعبة مستقلة تماماً في منطقها الداخلي؛ لا تتشارك الألعاب حالة لعب
  مباشرة فيما بينها (Isolation)، وأي تشارك بيانات يمر عبر `AGP.events`
  أو `AGP.services` فقط، لا عبر الوصول المباشر لكود لعبة أخرى.
- أي لعبة جديدة تُضاف يجب أن تُسجَّل في `AGP.registry` بنفس الطريقة
  المتبعة حالياً (عبر بطاقة `.game-card` واكتشاف تلقائي)، دون الحاجة
  لتعديل منطق الاكتشاف نفسه.

## 4) قواعد كلمة/كود الانضمام (Room Code)

- كود الغرفة يجب أن يكون **قصيراً وسهل النطق والكتابة** (مناسب للقراءة
  بصوت عالٍ أثناء بث مباشر).
- الكود الواحد يخص جلسة واحدة فقط، ولا يُعاد استخدامه لجلسة أخرى إلا
  بعد انتهاء الجلسة السابقة فعلياً.
- لا يُشترط أن يكون الكود سرياً بمعنى أمني صارم (فهو مخصص لتسهيل
  الانضمام أثناء البث)، لكن يجب ألا يكون قابلاً للتخمين بسهولة مفرطة
  لتفادي انضمام غرباء غير مقصودين لجلسات خاصة.

## 5) قواعد إدارة اللاعبين

- الهوست يملك صلاحيات إدارية أساسية داخل جلسته: قبول/رفض انضمام، إخراج
  لاعب مخالف، وإنهاء الجلسة.
- لا يوجد نظام حظر دائم (Ban) على مستوى المنصة في هذه المرحلة؛ أي إجراء
  إداري يقتصر على نطاق الجلسة الحالية فقط.
- أي منطق إدارة لاعبين مستقبلي (مثل الأدوار داخل مافيا/مين الامبوستر)
  يبقى **داخل اللعبة نفسها**، ولا يُرفع إلى مستوى المنصة العام إلا إذا
  كان منطقاً مشتركاً فعلاً بين أكثر من لعبة (عندها يُصاغ كخدمة عامة في
  `AGP.services`).

---

> ملاحظة: هذه القواعد **مفاهيمية توجيهية** لتوحيد القرارات المستقبلية،
> وليست وصفاً لتنفيذ فعلي موجود حالياً في الكود (الكود الحالي في مرحلة
> بنية تحتية أولية فقط: تسجيل ألعاب واكتشافها، بدون غرف أو جلسات حقيقية
> بعد).
