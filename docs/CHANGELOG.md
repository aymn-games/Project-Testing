# سجل التغييرات (Changelog) — AymanGamesPlatform

يوثّق هذا الملف التغييرات الرئيسية في المشروع عبر الإصدارات، بدءاً من
أول إصدار موثَّق للبنية التحتية للمنصة (`AGP`). يتبع هذا السجل تنسيقاً
مبسّطاً مستوحى من [Keep a Changelog](https://keepachangelog.com/).

تنويه: أرقام الإصدارات هنا تخص **بنية المنصة الداخلية** (`AGP.config.version`)
وليس بالضرورة إصدارات الموقع ككل.

---
## [0.45.5] — إصلاح محاذاة 8 إطارات لاعب حية (فراغات/تراكب) + ألوان نص مقروءة + أخضر مخصَّص لإطار الأهلي

### السياق
صاحب المنصة بلّغ (بلقطات شاشة فعلية من لوبي الروليت الحي) أن عدة
إطارات لاعبين تظهر بشكل غير مضبوط: فراغات بين الصورة الشخصية وفتحة
الإطار، الصورة مو راكبة بالضبط على الفتحة ببعض الإطارات، ولون اسم
اللاعب فوق بعض اللوحات غير مقروء (نص أبيض فوق لوحة فاتحة). قيد صريح
من صاحب المنصة قبل البدء: **عدم المساس إطلاقاً بملفات لعبة روليت
الإقصاء** (`games/elimination-roulette/*`) — حادثة سابقة تكسّرت فيها
ملفات تلك اللعبة أثناء تعديل غير مرتبط، والنطاق هذي المرة انحصر حصراً
بـ`js/agp-player-card.js`.

⚠️ **اكتشاف مهم أثناء التنفيذ (يستحق التوثيق صراحة)**: صاحب المنصة رفع
لي صور الإطارات المُشتبَه فيها واحدة تلو الأخرى بأسماء وصفية مؤقتة
(`frame-level-1.png`…`frame-level-7.png`, `frame-distinguished.png`,
`frame-supporter.png`) لتسهيل المحادثة — **لكن هذي الأسماء لم تكن
أسماء الملفات المسجَّلة فعلياً بجدول `FRAME_TEMPLATES`**. قبل أي تعديل،
قرأت الملف الحي فعلياً من GitHub (رابط أرسله صاحب المنصة مباشرة) وقارنت
قياسات `contentTop`/`contentHeight` الجديدة (لكل صورة قستها بالبكسل من
جديد) بقياسات كل مدخل قديم بالجدول — تطابقت 8 أزواج بدقة شبه حرفية
(نفس `contentTop` تماماً، `contentHeight` بفارق بكسل واحد فقط ضمن هامش
عتبة القياس)، وهو دليل شبه قاطع أنها **نفس ملفات الصور تماماً**، فقط
بأسماء مختلفة أثناء الرفع لي بالمحادثة. الأزواج المؤكدة:

| اسم وصفي أثناء الرفع | = المفتاح الفعلي بالجدول |
|---|---|
| frame-level-1.png | `frame-blacksteel.png` |
| frame-level-2.png | `frame-ice.png` |
| frame-level-4.png | `frame-purple.png` |
| frame-level-5.png | `frame-floral.png` |
| frame-level-6.png | `frame-crystalline.png` |
| frame-level-7.png | `frame-celestial.png` |
| frame-distinguished.png | `frame-frozen.png` |
| frame-supporter.png | `frame-phoenix.png` |

بناءً على هذا التطابق **صُحِّحت قياسات المفاتيح العشرة الموجودة أصلاً
بالجدول (بنفس أسمائها) — لم يُضَف ولم يُحذَف أي مفتاح جديد**.

### تغيّر — `js/agp-player-card.js` فقط
- **إعادة قياس كاملة بالبكسل** (نفس منهجية [0.44.8]: تحليل قناة ألفا +
  Connected-Components لفتحة الصورة، فحص أعمدة/صفوف لحدود لوحة الاسم،
  تحقّق بصري بصندوقين أحمر/أخضر فوق كل ملف قبل الاعتماد) لـ8 مداخل:
  `frame-floral.png`, `frame-ice.png`, `frame-blacksteel.png`,
  `frame-phoenix.png`, `frame-purple.png`, `frame-celestial.png`,
  `frame-crystalline.png`, `frame-frozen.png`. أرقام `avatarLeftPct/
  avatarTopPct/avatarWidthPct/avatarHeightPct` و`nameLeftPct/nameTopPct/
  nameWidthPct/nameHeightPct` (و`contentTop/contentHeight`) استُبدلت
  بالكامل بالقيم الجديدة المقاسة.
- **خاصية جديدة اختيارية بالجدول: `textColor`** (لون نص الاسم، hex).
  لو غير موجودة بمدخل معيّن يبقى اللون الأبيض الافتراضي بالـCSS كما هو
  تماماً (صفر تغيير سلوك على أي إطار ما يحتاجها). أُضيفت لـ:
  - `frame-floral.png`, `frame-ice.png`, `frame-celestial.png`,
    `frame-frozen.png`: `#1c1c24` (غامق محايد) — لوحات أسمائها فاتحة
    (كريمي/أزرق ثلجي/سحاب وردي فاتح/جليدي أبيض) وكان الأبيض الافتراضي
    غير مقروء عليها.
  - `frame-al-ahli.png`: `#046D38` — **بطلب صريح من صاحب المنصة** (لون
    نص الاسم = نفس أخضر شعار نادي الأهلي بالضبط، مسحوب فعلياً بالبكسل
    من داخل الشعار بالصورة نفسها، وليس تخميناً). القياسات الهندسية
    لهذا الإطار (المواقع/الأحجام) **لم تتغيّر** — كانت صحيحة أصلاً من
    [0.45.1]، التعديل الوحيد هو إضافة `textColor`.
  - تعديل صغير مقابل بدالة `renderFramedHtml`: `nameStyle` يضيف
    `color:` inline فقط لو `tpl.textColor` موجودة.
- `frame-founder.png`: **لم يُمَس إطلاقاً** — صاحب المنصة أكّد أنه سليم
  ومرجعي (استُخدم فعلياً كمرجع مقارنة أثناء القياس).

### لم يتغيّر
- **صفر تعديل** على `games/elimination-roulette/*`، أو `js/agp-game-
  shell.js`، أو `js/agp-entrance.js`، أو أي ملف آخر غير
  `js/agp-player-card.js` — تحقّق نصي قبل التسليم أن لا ملف ثاني تغيّر.
- لا تغيير على آلية القص/التحجيم العامة (`computeLayout`)، ولا على أي
  إطار غير الثمانية المذكورة أعلاه.
- لا تغيير على `frame_catalog` أو منطق منح/تفعيل الإطارات بالباك إند —
  هذا الإصدار عرض/محاذاة فقط بالواجهة.

### ⚠️ لم يُسلَّم بهذا الإصدار — يحتاج قرار/ملف منك
- إطار وصلني بصيغة **JPEG بدون شفافية حقيقية** (لقطة/تصدير، مو أصل
  PNG شفاف) باسم وصفي "frame-streamer.png" — لا يطابق أي مفتاح موجود
  بالجدول (تحقّقت، مو من الثمانية أعلاه ولا من founder/al-ahli). حسب
  شرط الملف نفسه أعلى `js/agp-player-card.js` ("PNG بخلفية شفافة
  حقيقياً — لا JPEG بخلفية بيضاء")، ما أضفته للجدول. لو هذا إطار حقيقي
  جديد تبيه يشتغل، ارفع لي نسخة PNG شفافة أصلية منه (أو أكّد لي اسمه
  المسجَّل الفعلي لو هو أحد الإطارات الموجودة أصلاً بمسمى مختلف).
- بعد رفع هذا الملف على GitHub، تأكيد بصري نهائي من اللوبي الحي على 8
  الإطارات المعدَّلة (خصوصاً الخمسة اللي صار لها لون نص غامق جديد).

---
## [0.45.4] — رابط الدعم الفعلي (كريترز) + تغيير عملة عرض مبالغ الدعم لدولار

### السياق
صاحب المنصة جهّز صفحته على منصة كريترز (دكان تب سابقاً)، وطلب: (1)
تفعيل رابط الدعم الفعلي بدل placeholder `href="#"`، (2) عرض مبالغ الدعم
بالدولار بدل الريال بكل مكان تظهر فيه.

### تغيّر — `support.html`
- زر "ادعم الآن عبر كريترز": `href="#"` → `https://creators.sa/aymngame`.
- دالة `formatAmount()` (تُستخدم بقائمة "آخر الداعمين" بنفس الصفحة):
  اللاحقة `' ريال'` → `' دولار'`.
- حُدِّث التعليق التوثيقي فوق الزر (كان يشرح إنه placeholder مؤقت،
  صار يوثّق أنه أُضيف فعلياً بهذا الإصدار).

### تغيّر — `index.html`
- دالة `formatSupporterAmount()` (شريط الداعمين المتحرك بالصفحة
  الرئيسية، نفس منطق العملة مكرَّر هناك بشكل مستقل): اللاحقة `' ريال'`
  → `' دولار'`. لا تعديل آخر على الملف.

### ⚠️ لم يُسلَّم بهذا الإصدار — يحتاج تعديل يدوي بسيط منك
`admin-settings.html` (حقل "المبلغ" وقت ما الأدمن يسجّل دعماً يدوياً)
فيه نفس النص `"المبلغ (ريال)"` — لقيته فعلياً بالنسخة الحية على GitHub
(**السطر 274 حالياً**، تحقّقت منه مباشرة). ما سلّمت نسخة كاملة معدَّلة
من هذا الملف لأن نسختي المحلية منه قديمة وتختلف بنية عن النسخة الحية
بشكل كبير (فروقات ترقيم أسطر واسعة تدل على تعديلات لاحقة لست متأكداً
من تفاصيلها) — تسليم نسخة كاملة كان يحمل خطر استبدال/فقدان تعديلات حية
لا علم لي فيها. **الأسلم**: افتح `admin-settings.html` من GitHub مباشرة،
دوّر (Ctrl+F) على `المبلغ (ريال)`، وغيّرها يدوياً إلى `المبلغ (دولار)`
— تعديل كلمة واحدة بس.

### لم يتغيّر
- لا تعديل على منطق حساب/تخزين المبالغ نفسه (`backend/supporters/`) —
  الأرقام المخزَّنة كما هي، هذا تغيير في **تسمية العملة المعروضة فقط**،
  وليس تحويلاً حسابياً فعلياً بين العملتين.

---
## [0.45.3] — تحديث وسوم SEO/المشاركة الاجتماعية للدومين المخصَّص الجديد

### السياق
الدومين المخصَّص `aymngames.online` رُبط بخدمة `agp-platform` على Render
(DNS + شهادة SSL)، بديلاً عن الدومين المؤقت `agp-platform.onrender.com`
(الأخير يبقى شغّالاً بالتوازي — راجع "Render Subdomain" مفعّل). الموقع
كان يشتغل صحيح بالفعل على الدومين الجديد بدون أي تعديل كود (لا CORS ولا
أي جسر لعبة يتأثر — الاثنان فُحصا وتأكّد عدم تأثرهما)؛ التعديل هنا
تجميلي بحت لمحركات البحث ومعاينات المشاركة (واتساب/تيك توك/تويتر).

### تغيّر — `index.html` فقط (5 أسطر داخل `<head>`)
- `<link rel="canonical">` → `https://aymngames.online/`
- `<meta property="og:image">` و`<meta name="twitter:image">` → `https://aymngames.online/logo.png`
- `<meta property="og:url">` → `https://aymngames.online/`
- `"url"` بوسم `application/ld+json` (Schema.org WebSite) → `https://aymngames.online/`

### لم يتغيّر
- صفر تعديل على أي منطق أو ملف آخر — تبديل نص القيم فقط داخل `<head>`.
- الموقع يفتح بنفس الشكل تماماً على الدومينين (القديم والجديد)، هذا
  التعديل يخص فقط كيف يظهر الرابط لمحركات البحث ولما يُشارَك خارجياً.

---
## [0.45.2] — 10 بطاقات "قريباً" إضافية بالصفحة الرئيسية (index.html)

### السياق
طلب صريح من صاحب المنصة: إضافة 10 بطاقات ألعاب جديدة فوق الموجودة حالياً
(بدون حذف أي بطاقة قديمة). الصفحة كانت أصلاً تحتوي بطاقة واحدة فعّالة
(روليت الإقصاء) + 9 بطاقات "قريباً" (دفعة أولى، `coming-soon-2` حتى
`coming-soon-10`) — أُضيفت هذي الدفعة الثانية فوقها مباشرة.

### أُضيف — `index.html` فقط
- 10 بطاقات "قريباً" جديدة (`coming-soon-11` حتى `coming-soon-20`)، بنفس
  البنية والتصميم (`game-card--v2 game-card--soon`) والمنطق تماماً كالدفعة
  الأولى — بدون `data-agp-game-id` فريد لأي لعبة حقيقية بعد، `data-agp-game-status="coming-soon"`
  حتى ما تُحسب ضمن الألعاب الفعّالة. المجموع الآن: 20 بطاقة (1 فعّالة + 19 "قريباً").
- كل بطاقة تتوقّع صورة غلاف باسم `coming-soon-N-cover.png` (يُرفَع لجذر
  المستودع لاحقاً عند تجهيز كل لعبة، بنفس أسلوب باقي الأغلفة — لو الملف
  غير موجود بعد، الغلاف يختفي بصمت عبر `onerror` الموجود مسبقاً، بدون
  أيقونة "صورة مكسورة").

### لم يتغيّر
- صفر تعديل على أي بطاقة قديمة (لا الفعّالة ولا الدفعة الأولى من "قريباً").
- صفر تعديل على أي ملف `js/agp-*.js`، أو `games/`، أو منطق قفل الألعاب
  (`btn-play` click handler)، أو أي سكربت بنهاية الصفحة — إضافة HTML بحتة
  ضمن نفس القسم (`#games-section > .container`) بدون أي أثر على التصميم/
  السلوك الحالي.

### للتحقّق لاحقاً
- رفع أغلفة `coming-soon-11-cover.png` حتى `coming-soon-20-cover.png` (أو
  تركها بدون غلاف مؤقتاً، تظهر رسالة "سيتم إضافة صورة الغلاف قريباً"
  تلقائياً بحكم `.no-cover::after` الموجودة مسبقاً بالـCSS).

---
## [0.45.1] — إطار "الأهلي" (نادي الأهلي السعودي) — إضافة عبر مسار الإطارات الحصرية

### السياق
صاحب المنصة أرسل ملف إطار جديد بتصميم نادي الأهلي السعودي، وطلب إضافته
كإطار "خاص". فُحص الملف فعلياً (PNG بخلفية شفافة حقيقية، 1536×1024)،
وقيست فتحة الصورة + بلاطة الاسم بالبكسل (تحليل قناة الألفا + كشف
المنطقة البيضاء الفاضية) بنفس منهجية كل الإطارات السابقة، مع تحقّق
بصري بصندوقين قبل الاعتماد. أُضيف مدخل `frame-al-ahli.png` بجدول
`FRAME_TEMPLATES` بـ`js/agp-player-card.js`.

⚠️ **قرار معماري**: هذا الإطار **لم يُضَف** كخامس مدخل بقائمة
`frame_catalog.kind = 'special'` الثابتة (founder/streamer/supporter/
distinguished — الأربعة الوحيدة، كل واحد منها له شرط منح تلقائي محدد
بالكود). سبب عدم إضافته هناك: أولاً، تعديل قيد `CHECK` بجدول SQLite
موجود يتطلب إعادة بناء الجدول بالكامل (غير مبرَّر لإطار واحد إضافي)؛
وثانياً، وهو الأهم، لا يوجد شرط منح تلقائي واضح لإطار نادي الأهلي (على
عكس الأربعة الحاليين). بدلاً من ذلك استُخدم نظام **"الإطارات الحصرية"
(custom_frames)** الموجود أصلاً وجاهز بالكامل بدون أي تعديل كود إضافي
— نفس الغرض بالضبط (إطار خاص يُمنح يدوياً من الأدمن لأي مستخدم يختاره)،
عبر `admin-settings.html` (تبويب "إطارات حصرية" ثم "منح / سحب مقتنى").

### لم يتغيّر
- `backend/db/database.js`، `backend/collectibles/collectibles-service.js`
  — صفر تعديل، النظام الحالي يكفي بالكامل.

### للتحقّق لاحقاً
- رفع `frame-al-ahli.png` فعلياً لجذر المستودع بنفس الاسم المقاس، ثم
  تسجيله عبر لوحة الأدمن ومنحه لمستخدم تجريبي للتأكد البصري النهائي.

---
## [0.45.0] — تفعيل تتبّع البث الفعلي + إصلاح صورة/اسم تيك توك + نظام SP جديد + تفعيل/إيقاف الدخولية ذاتياً + إعادة تنظيم لوحة الأدمن

### السياق
دفعة تحديثات مطلوبة مباشرة من صاحب المنصة على 5 محاور منفصلة، كل واحد
منها فُحص أولاً بقراءة الكود الفعلي (لا تخمين) قبل أي تعديل — راجع
تفصيل كل محور أدناه. **لا تعديل إطلاقاً** على أي ملف `js/agp-*.js`، ولا
على `platforms/connector-router.js`، ولا على بروتوكول WebSocket
(`protocol/message-*.js`)، ولا على أي شيء داخل `games/` — كل التعديلات
منحصرة بـ`backend/` (خارج `platforms/tiktok/tiktok-connector.js` نفسه —
لم يُلمَس) + الصفحات المستقلة (`profile.html`, `admin.html`,
`admin-settings.html`, `auth/auth-client.js`).

### 1) إحصائيات البث كانت صفراً دائماً — سبب حقيقي غير مبني على تخمين
فحص كامل لـ`backend/platforms/tiktok/tiktok-connector.js` و
`dashboard-core/js/dashboard-core.js` أكّد: دوال التتبّع
(`startBroadcast`/`endBroadcast`/`incrementBroadcastStat`/`addGiftValue`)
كانت موجودة وصحيحة بـ`auth-service.js` منذ البداية، لكن **لا شيء
يستدعيها فعلياً** — فجوة ربط حقيقية، لا خلل إعداد.

الحل: ربط كامل داخل `backend/websocket/ws-server.js` فقط (لا تعديل على
البروتوكول ولا على الموصِّل نفسه ولا على `connector-router.js`). عند
"connect" لمنصة `tiktok` بيوزرنيم مطابق لحساب موثَّق فعلياً بالمنصة
(`authService.findVerifiedUserByTikTok` — دالة موجودة أصلاً، لم تُعدَّل)
يُسجَّل بث جديد؛ كل تعليق/هدية/متابعة واردة أثناءه تزيد عدّاد الإحصائية
المطابقة؛ عند "disconnect" أو إغلاق الاتصال (حتى غير المتوقَّع) يُختَم
البث. لو اليوزرنيم غير مرتبط بأي حساب موثَّق (حالة شائعة — مراقبة بث أي
شخص بدون حساب هنا)، لا يحدث شيء بصمت.

### 2) صورة/اسم تيك توك ما كانا يظهران فوق البروفايل رغم التوثيق الناجح
فحص `extractProfileAvatarFromHtml`/`extractProfileDisplayNameFromHtml`
بـ`auth-service.js` كشف السبب الأرجح: الرجيكس القديم يفترض ترتيباً
ثابتاً لخصائص وسم `<meta>` (property ثم content مباشرة) — يفشل بصمت لو
كانت هناك خاصية أخرى قبل property بنفس الوسم (مثل `data-rh="true"`،
علامة React Helmet SSR الشائعة جداً). الإصلاح: دالة استخراج جديدة
(`extractMetaTagContent`) تبحث عن كامل وسم `<meta>` المطابق أولاً، ثم
تستخرج `content` منه بمعزل عن ترتيب الخصائص — مع سجل تشخيصي محدود (أول
5 محاولات فقط، بنفس أسلوب `_debugFollowStatus` الموجود أصلاً بالمشروع)
يوضّح مباشرة بسجلات الخادم هل HTML المجلوب يحتوي وسوم `og:` من الأساس.

⚠️ **ملاحظة صادقة**: هذا تحسين مبني على سبب فشل معروف وشائع، لكن لم
يُختبَر بعد ضد تيك توك حقيقي من هذه البيئة (لا وصول شبكي لتيك توك هنا).
لو تيك توك لا يُرسِل وسوم `og:` أصلاً لطلبات غير المتصفح، هذا الإصلاح
وحده لن يكفي — السجل التشخيصي الجديد سيوضّح ذلك مباشرة بعد الرفع.

### 3) نظام "مستوى الستريمر (SP)" — جديد بالكامل
تصميم مفوَّض بالكامل من صاحب المنصة. `SP = (دقائق البث الفعلية × 1) +
(إجمالي قيمة الهدايا بالماس ÷ 10)` — نظام مركّب عمداً (مكافأة الالتزام
بالبث + التفاعل الفعلي معاً، لا أحدهما فقط). 7 مستويات بجدول جديد
`streamer_levels` (`backend/db/database.js`، `CREATE TABLE IF NOT
EXISTS` — إضافة بحتة، صفر خطر على أي جدول موجود) بدل إضافة قيمة جديدة
لقيد `CHECK` بـ`frame_catalog.kind` (تعديل قيد CHECK بـSQLite يتطلب
إعادة بناء الجدول بالكامل — غير مبرَّر هنا). عتبات كل مستوى قابلة
للتعديل الكامل من `admin-settings.html` (تبويب "مستويات SP") بدون أي
تعديل كود لاحق. يظهر الآن كأرقام حقيقية ببطاقة "مستوى الستريمر (SP)"
بالبروفايل (كانت "قريباً" ثابتة).

⚠️ **ملاحظة صادقة**: بما إن تتبّع البث لم يكن مفعَّلاً قبل هذا الإصدار
(البند 1 أعلاه)، لا توجد أي بيانات بث تاريخية — كل ستريمر يبدأ من SP =
صفر لحظة أول بث بعد هذا التحديث، بصرف النظر عن نشاطه الفعلي قبل ذلك.
هذا متوقَّع وليس خللاً.

### 4) تفعيل/إيقاف الدخولية ذاتياً من البروفايل
عمود جديد `user_entrances.enabled` (`INTEGER NOT NULL DEFAULT 1` عبر
`ensureColumn` — الصفوف الحالية كلها "مفعّلة" تلقائياً، صفر تغيير سلوك
ظاهر لأي مستخدم عنده دخولية اليوم). الإيقاف **لا يحذف** الدخولية —
فقط يخفيها عن اللوبي (`getEquippedEntranceForVerifiedTikTok` تستبعد
الآن `enabled = 0`)، مع بقاء القالب/النص محفوظين لإعادة التفعيل بضغطة
واحدة. مسار جديد `POST /api/entrance/toggle` (بنفس نمط
`/api/collectibles/equip` بالضبط — صاحب الجلسة فقط). تعيين دخولية جديدة
من الأدمن (`/api/admin/entrance`) يعيد `enabled = 1` تلقائياً.

### 5) إعادة تنظيم لوحة الأدمن — صفحتان بدل صفحة واحدة طويلة
`admin.html` أصبحت مخصَّصة للمستخدمين فقط، بتبويبين (اللاعبون/الستريمرز
— تصنيف بمنطق العميل من نفس البيانات الموجودة، لا تعديل خادم) مع بحث
منفصل لكل تبويب. كل أقسام "الإعدادات" (الإعلان، الداعمون، ثيم
المناسبات، كتالوج الإطارات، الإطارات الحصرية، منح/سحب مقتنى، الدخولية
المستقلة) انتقلت لصفحة جديدة `admin-settings.html` منظّمة بتبويبات، +
تبويب جديد لمستويات SP (البند 3). لا تعديل على أي منطق/مسار HTTP موجود
— فقط إعادة توزيع نفس العناصر والاستدعاءات بين صفحتين.

### لم يتغيّر
- أي ملف `js/agp-*.js`، `games/`، `platforms/connector-router.js`،
  `platforms/tiktok/tiktok-connector.js`، بروتوكول WebSocket
  (`protocol/message-*.js`) — صفر تعديل.
- `backend/points/points-service.js` (نقاط/مستوى **اللاعب** — نظام
  منفصل تماماً عن SP الجديد).
- كل مسارات/بيانات المستخدمين الحالية — التعديلات إضافية بحتة (أعمدة/
  جداول جديدة بقيم افتراضية آمنة)، صفر فقدان بيانات متوقَّع.

### للتحقّق لاحقاً على بيئة حقيقية
- تأكيد فعلي من سجلات الخادم أن تشخيص og:image/og:title (البند 2) يوضّح
  السبب الحقيقي على HTML تيك توك الفعلي.
- تأكيد أن SP تتحرك فعلياً مع بث حقيقي بعد الرفع (البند 3).

---
## [0.44.9] — 3 إطارات جديدة بالجدول + تحقّق بكسلي مباشر من founder الفعلي على القرص (يقفل ملاحظة [0.44.8] رقم 3)

### السياق
صاحب المشروع رفع دفعة رابعة من ملفات الإطارات (4 صور، بدون تسمية،
برسالة "هذي باقي الايطارات"). فحصها بالبكسل بنفس منهجية [0.44.8]
(تحليل قناة الألفا + Connected-Components لدائرة الصورة، flood-fill أو
قياس يدوي بصندوق للبلاطة عند فشل الـflood-fill، تحقّق بصري بصندوقين
قبل الاعتماد) كشف:

- **3 من الـ4 ملفات سليمة وصالحة للاستخدام مباشرة** — PNG بقناة ألفا
  حقيقية، فتحة صورة شفافة فعلياً، بلاطة اسم فاضية، ونفس الاتجاه
  الموحَّد (صورة يسار/اسم يمين) المتّبع بكل الإطارات السابقة.
- **الملف الرابع غير صالح إطلاقاً** — راجع "ملاحظة صادقة" رقم 1 أدناه؛
  لم يُضَف للجدول.
- من ضمن ملفات هذي الدفعة كان أيضاً — باكتشاف عرَضي أثناء الفرز
  بالتوقيت — **الملف الحقيقي لـ`frame-founder.png` المصحَّح** الذي رفعه
  صاحب المشروع قبل دفعة الإطارات الخمسة الأولى (بمحادثة سابقة: "تمام
  عندي ملف frame-founder.png معكوس وجاهز"). هذا أتاح فرصة تحقّق مباشر
  من قياسات founder المبرمجة، بدل الاعتماد على تصريح شفهي فقط (كانت
  ملاحظة صادقة مفتوحة بـ[0.44.8] رقم 3).

### أُضيف — `js/agp-player-card.js` (جدول `FRAME_TEMPLATES`)
3 مداخل جديدة بأسماء **مؤقتة وصفية** (نفس قيد [0.44.8] — لسه ما
تأكَّدت من `frame_catalog.image_filename` الفعلي):
- `frame-celestial.png` — تصميم غيوم/سماء وردي-بنفسجي فاتح (1254×1254).
- `frame-crystalline.png` — تصميم بلّوري أسود-فضي ببلاطة نجوم/كون
  بنفسجية (1536×1024).
- `frame-frozen.png` — تصميم جليد/بلّورات زرقاء ببلاطة ثلج (1254×1254).

كل مدخل مقاس بكسلياً بنفس المنهجية (نافذة القص، دائرة الصورة، بلاطة
الاسم كلها % محسوبة من فحص فعلي، لا تخمين) ومتحقَّق منه بصندوقين قبل
الاعتماد.

### تحقّق إضافي (بدون تغيير كود) — `frame-founder.png`
قِسْت الملف الفعلي المرفوع (نفسه، لا نسخة معاد بناؤها محلياً) بنفس
الطريقة: دائرة الصورة طابقت القياسات المبرمجة بالجدول **تطابقاً تاماً**
(avatarLeftPct/avatarWidthPct/avatarTopPct/avatarHeightPct = 9.73/24.24/
33.12/51.71 بالضبط)، وبلاطة الاسم طابقت ضمن هامش قياس معقول (~1-2%،
متوقَّع بين طريقتين مختلفتين لقياس نفس البلاطة). هذا يقفل الشك المفتوح
بملاحظة [0.44.8] رقم 3 — القيم المبرمجة حالياً لـfounder صحيحة فعلاً
مقابل الملف الحقيقي، لا تخمين.

### التحقق
- كل من الملفات الـ3 الجديدة فُحص بصرياً بصندوقين (أحمر/أخضر) فوق ملفه
  الحقيقي قبل اعتماده بالجدول.
- عرض حقيقي عبر Chromium (Playwright) لكل التسعة إطارات معاً (founder +
  8 إطارات جديدة) بنفس CSS الحقيقي لعنصر `<li>` (بدون `flex`)، باسم
  إنجليزي قصير واسم عربي طويل جداً — كل التسع بطاقات محتواة بالكامل،
  بدون تمديد ولا انفلات ولا تراكب.
- `node --check` ناجح.

### لم يتغيّر
لا تعديل على أي ملف غير `js/agp-player-card.js`. قياسات الإطارات الستة
السابقة ([0.44.8]) لم تتغيّر إطلاقاً.

### ملاحظة صادقة
1. **الملف الرابع من هذي الدفعة مرفوض ولم يُضَف للجدول** — عند فحصه
   بـPython/PIL تبيّن أنه **JPEG حقيقي بدون قناة ألفا إطلاقاً** (وضع
   RGB، لا RGBA)، بخلفية بيضاء معتمة كاملة — بالضبط المخالفة الموصوفة
   بمتطلب رقم 1 بتعليق رأس الملف نفسه ("PNG بخلفية شفافة حقيقياً، لا
   JPEG بخلفية بيضاء"). التصميم نفسه (حلقة كونية/ألوان مائية بلاطة
   بنفسجية فاتحة مسطّحة) يبدو جاهزاً بصرياً، لكن الملف بصيغته الحالية
   غير قابل للاستخدام في نظام القص بالـCSS المعتمد (يحتاج فتحة صورة
   شفافة فعلية لتظهر صورة اللاعب من خلالها). **لازم يُعاد تصديره من
   ChatGPT/أداة التصميم كـPNG بقناة ألفا حقيقية** (خلفية شفافة + فتحة
   دائرة الصورة شفافة فعلياً) قبل ما أقدر أقيسه وأضيفه.
2. أسماء الإطارات الثلاثة الجديدة بالجدول مؤقتة ووصفية فقط (نفس قيد
   [0.44.8] رقم 1) — ولا زلت بحاجة للأسماء الحقيقية المسجَّلة بلوحة
   الأدمن لكل الإطارات المضافة حتى الآن (الثمانية غير founder)، حتى
   يعمل النظام صح فعلياً بالإنتاج بدل الاعتماد على `DEFAULT_TEMPLATE_KEY`
   الاحتياطي بصمت.
3. أي إطار يُضاف لاحقاً لازم يُقاس بنفس الطريقة (فحص بكسلي فعلي) — لا
   تغيّر بهذي الجولة عن القاعدة المتّبعة منذ [0.44.8].

---
## [0.44.8] — تحوّل جذري: جدول قياسات مستقل لكل إطار (بدل ثوابت عامة واحدة) + تصحيح founder

### السياق
صاحب المشروع صحّح ملف `frame-founder.png` نفسه على GitHub (رجّعه
لاتجاهه الصحيح: صورة يسار/اسم يمين) بدل ما نعكس كل الإطارات الجديدة —
وبالتوازي أرسل أول 5 ملفات إطارات فعلية جديدة (مولَّدة عبر ChatGPT،
جلسات منفصلة). فحصها بالبكسل (نفس طريقة founder: تحليل قناة الألفا +
Connected-Components لدائرة الصورة، وflood-fill لوني لبلاطة الاسم، عبر
Python/PIL/SciPy/scikit-image) كشف مشكلة أعمق من مجرد الاتجاه:

- الاتجاه فعلاً موحّد بين الملفات الجديدة الخمسة (كلها صورة-يسار) —
  متوافق مع founder المصحَّح، ولا حاجة لأي عكس.
- **لكن أبعاد القماشة نفسها غير موحّدة**: ملفان بحجم 1254×1254 (مربّعة،
  زي founder)، وثلاثة ملفات بحجم **1536×1024** (مستطيلة، نسبة مختلفة
  كلياً). قالب [0.44.5]–[0.44.7] كان مبنياً بالكامل على افتراض قماشة
  مربّعة 1254×1254 ثابتة لكل الإطارات — افتراض غير صحيح عملياً.

### أُصلح/أُعيد بناؤه — `js/agp-player-card.js`
- استُبدلت الثوابت العامة الواحدة (`CANVAS_SIZE`/`CONTENT_TOP_PX`/
  `AVATAR_*_PCT`/`NAME_*_PCT`) بجدول **`FRAME_TEMPLATES`** — مدخل مستقل
  لكل ملف إطار (المفتاح = اسم الملف بالضبط)، فيه: أبعاد القماشة
  الحقيقية، نافذة القص الرأسي، وموقع/حجم دائرة الصورة وبلاطة الاسم (%
  من نافذة القص). إطار غير موجود بالجدول يستخدم قياسات `frame-founder.png`
  كاحتياط دفاعي (بدل ما ينكسر كلياً) — لا يمنع الحاجة لقياس أي ملف جديد.
- `computeLayout(tpl)` تحسب px الفعلية (عرض البطاقة، حجم/إزاحة طبقة
  القماشة) **لكل قالب على حدة** بدل قيمة واحدة مشتركة — كل إطار يعرض
  بعرض مختلف قليلاً حسب نسبة قماشته الحقيقية (الارتفاع ثابت `CARD_HEIGHT_PX
  = 72px` للكل، حفاظاً على تناسق ارتفاع صفوف اللوبي).
- `renderFramedHtml` صار يحقن كل القياسات (px/%) **inline** لكل بطاقة
  (بدل كلاسات CSS ثابتة بالـ`<style>` المُحقَن مرة وحدة) — ضروري لأن كل
  إطار له قياسات مختلفة فعلياً الآن؛ الكلاسات المشتركة أبقت فقط الخصائص
  الثابتة (الموضع/الطبقات/شكل النص).
- `frame-founder.png` بالجدول رجع لقياسات [0.44.5] الأصلية (صورة يسار/
  اسم يمين) — **بشرط أن يكون الملف المرفوع فعلاً على GitHub الآن هو
  النسخة المصحَّحة** كما أكَّد صاحب المشروع.
- 5 إطارات جديدة أُضيفت للجدول بأسماء **مؤقتة وصفية** (`frame-floral.png`،
  `frame-ice.png`، `frame-blacksteel.png`، `frame-phoenix.png`،
  `frame-purple.png`) — لسه ما تأكَّدت من الأسماء الفعلية المسجَّلة
  بلوحة الأدمن (`frame_catalog.image_filename`)؛ لازم تُستبدَل بالاسم
  الحقيقي لكل ملف قبل الرفع (راجع "ملاحظة صادقة" أدناه).

### التحقق
- كل إطار جديد فُحص بصرياً بصندوقين (أحمر لدائرة الصورة، أخضر لبلاطة
  الاسم) فوق ملفه الحقيقي قبل اعتماده بالجدول — لا نسخ قياسات ملف على
  آخر.
- عرض حقيقي عبر Chromium (Playwright) لكود `AGP.playerCard.renderHtml`/
  `fitAllNames` الفعليين، بنفس CSS الحقيقي لعنصر `<li>` (بدون `flex`)،
  للستة إطارات كلها معاً (founder المصحَّح + الخمسة الجديدة)، باسم
  إنجليزي قصير واسم عربي طويل جداً لكل واحد — كل الست بطاقات محتواة
  بالكامل، الصورة داخل حلقتها، الاسم داخل بلاطته، بدون تمديد ولا انفلات
  ولا تراكب، حتى للملفات المستطيلة (1536×1024) رغم اختلاف نسبتها الكلي
  عن founder.
- `node --check` ناجح.

### لم يتغيّر
لا تعديل على أي ملف غير `js/agp-player-card.js`. توقيع الدوال العامة
(`AGP.playerCard.renderHtml`/`fitAllNames`) لم يتغيّر — بقية الملفات
اللي تستدعيها لا تحتاج أي تعديل.

### ملاحظة صادقة
1. أسماء الإطارات الخمسة الجديدة بالجدول (`frame-floral.png` وغيرها)
   **مؤقتة ووصفية فقط** (وضعتها أنا حسب شكل كل تصميم) — لازم تُستبدَل
   بالاسم الحقيقي المسجَّل بلوحة الأدمن لكل ملف قبل الاعتماد النهائي،
   وإلا `getTemplate()` ما بيلقى القالب الصحيح ويرجع لقياسات founder
   الاحتياطية (خطأ صامت: الإطار يظهر لكن بقياسات إطار غير هذا).
2. أي إطار سادس عشر أو أكثر يُضاف لاحقاً **لازم يُقاس بنفس الطريقة**
   (فحص بكسلي فعلي، لا نسخ قياسات) قبل إضافته للجدول — أثبتت هذي
   الجولة أن التشابه البصري بين الملفات لا يعني تطابق القياسات.
3. لم يُتحقَّق بعد من ملف `frame-founder.png` الفعلي المصحَّح على GitHub
   مباشرة (نفس قيد الشبكة المذكور بـ[0.44.7]) — الاعتماد على تأكيد
   صاحب المشروع الشفهي أنه صحّحه، لا فحص بايتات مباشر من طرفي.

---
## [0.44.7] — تصحيح اتجاه القالب: الملف الفعلي على GitHub معكوس أفقياً عن النسخة المُرسَلة بالمحادثة

### السياق
بعد [0.44.6]، صاحب المشروع أرسل لقطة شاشة من اللوبي الحقيقي تُظهر
الشارة **محتواة بشكل سليم تماماً** (لا انفلات، الإصلاح السابق نجح) —
لكن معكوسة أفقياً: التاج/حلقة الصورة يمين، بلاطة الاسم يسار (عكس ما
صُمِّم/قِيس بـ[0.44.5]: صورة يسار/اسم يمين). فحص القيم الحيّة عبر
DevTools (`getComputedStyle`) أثبت أن الكود يشتغل **بالضبط** كما هو
مبرمج (`avatar.left = 14.32px` بالضبط = 9.73% من عرض الصندوق، `transform:
none` على كل العناصر الثلاثة) — يعني لا خلل بالكود ولا بالـCSS إطلاقاً.

بطلب فتح رابط الصورة الفعلي مباشرة (`frame-founder.png?v=` لكسر أي
كاش)، تأكَّد بالصورة أن **الملف الفعلي المنشور على GitHub نفسه معكوس
أفقياً** عن الملف اللي أرسله صاحب المشروع لي بالمحادثة وقِستُ عليه
بـ[0.44.5] (على الأغلب رفع بروفايل خطأ لنفس التصميم، أو صدَّره بمرآة
عكسية من برنامج التصميم). تأكيد إضافي: طابقت الصورة الحقيقية (بعد
عكسها أفقياً برمجياً محلياً عندي) تماماً مع لقطة الشاشة الحيّة — نفس
الشكل تماماً، بكسل بالضبط.

### أُصلح — `js/agp-player-card.js`
- `AVATAR_LEFT_PCT`: `9.73` → `66.03`.
- `NAME_LEFT_PCT`: `42.11` → `10.77`.
- (باقي الثوابت — `*_TOP_PCT`/`*_WIDTH_PCT`/`*_HEIGHT_PCT`/`CANVAS_SIZE`/
  `CONTENT_TOP_PX`/`CONTENT_HEIGHT_PX` — بدون تغيير: العكس أفقي فقط،
  الأبعاد الرأسية والأحجام كما هي تماماً).
- القيمتان الجديدتان محسوبتان رياضياً من القياسات الأصلية المؤكَّدة
  بـ[0.44.5] (`CANVAS_SIZE - old_right` / `CANVAS_SIZE - old_left`) —
  عكس دقيق، لا إعادة قياس تخميني.

### التحقق
- عكستُ الملف الأصلي اللي عندي أفقياً برمجياً (Python/PIL)، وقارنته
  بصرياً مع لقطة شاشة اللوبي الحيّة (بعد كسر الكاش) — تطابق بصري كامل.
- رسمت صندوقي التحقّق (دائرة الصورة/بلاطة الاسم) بالإحداثيات المعكوسة
  الجديدة فوق النسخة المعكوسة من الصورة — تطابق دقيق بكسل بالضبط (نفس
  أسلوب التحقّق المتَّبع بـ[0.44.5] الأصلي).
- عرض حقيقي عبر Chromium (Playwright) لكود `AGP.playerCard.renderHtml`
  الفعلي، بنفس CSS الحقيقي لعنصر `<li>` (بدون `flex`)، ضد النسخة
  المعكوسة من الصورة — الصورة داخل الحلقة الذهبية يمين، الاسم داخل
  البلاطة الزرقاء يسار، محتواة بالكامل، بدون تمديد ولا انفلات.
- `node --check` ناجح.

### لم يتغيّر
لا تعديل على أي ملف غير `js/agp-player-card.js`. لا تعديل على ملف
`frame-founder.png` نفسه (يبقى كما هو على GitHub — الكود تكيَّف معه).

### ملاحظة صادقة
بيئة العمل عندي (Sandbox سحابي) ما تقدر تصل مباشرة لملفات موقعك الحيّة
(`agp-platform.onrender.com`) — الوصول محجوب على مستوى الشبكة (فشل
`curl`/`Playwright` بخطأ اتصال). القياس المُصحَّح هنا مبني على: (أ)
عكس رياضي دقيق لقياسات [0.44.5] المؤكَّدة بكسلياً على الملف اللي
أرسلته لي، و(ب) مطابقة بصرية للنتيجة المعكوسة مع لقطة شاشتك الحيّة —
مو فحص بايتات الملف الفعلي مباشرة من طرفي. لو فيه أي فرق دقيق إضافي
(غير مجرد عكس أفقي بسيط) بين الملف الحقيقي والنسخة اللي عندي، ممكن
يحتاج ضبطاً بسيطاً إضافياً بعد المعاينة الحية التالية.

---
## [0.44.6] — إصلاح انفلات شارة الإطار خارج صندوقها باللوبي الحقيقي (bug في [0.44.5] نفسه)

### السياق
صاحب المشروع أرسل لقطة شاشة من اللوبي الحقيقي (شاشة "بانتظار اللاعبين")
تُظهر شظية صغيرة فقط من فن الإطار (طرف جناح ذهبي/أزرق) عائمة منفردة عند
حافة صندوق اللوبي — لا شارة كاملة، ولا صورة، ولا اسم. هذا رغم أن تحقّقي
البصري في [0.44.5] (عرض حقيقي بمتصفح Chromium) أظهر شارة سليمة تماماً.

السبب: عنصر الشارة المؤطَّرة (`.agp-pcard-tpl`) عبارة عن `<span>`
(عنصر `inline` افتراضياً)، وCSS القالب يضبط عليه `width`/`height`/
`overflow:hidden` صراحةً — لكن هذي الخصائص **لا تُطبَّق إطلاقاً** على
عنصر inline عادي حسب مواصفة CSS (تحتاج `display:block`/`inline-block`/
`flex` وغيرها). داخل عنصر `<li>` الحقيقي بقائمة اللوبي
(`js/agp-game-shell.js` — `.agp-shell-player-list li`، مجرد `padding`/
`text-align`، **بدون** `display:flex`)، الشارة كانت تنهار لصندوق شبه
معدوم الحجم، فيما أبناؤها (الصورة، طبقة الإطار 147×147px، الاسم) —
كلهم `position:absolute` — يفلتون خارج أي قصّ فعلي ويظهرون متناثرين
خارج مكانهم الصحيح.

**سبب عدم اكتشافها بتحقّقي السابق**: صفحة الاختبار المحلية اللي بنيتها
لتحقّق [0.44.5] استخدمت عنصر `<li>` بـ`display:flex` (افتراض مني، مو
منسوخ من الملف الحقيقي) — وعناصر `flex` تُحوَّل تلقائياً لـ`block` لأي
ابن مباشر داخلها بغض النظر عن نوعه الأصلي (`blockify`)، فأخفى هذا
بالصدفة نفس الخلل اللي كان سيظهر لو اختبرت ضد كود `agp-game-shell.js`
الفعلي مباشرة. درس مستفاد: التحقق البصري لازم يكون ضد الـHTML/CSS
المحيط الحقيقي فعلياً، لا نسخة افتراضية شبيهة.

### أُصلح — `js/agp-player-card.js`
- إضافة `display:inline-block;` صراحة لقاعدة `.agp-pcard-tpl` (نفس
  النمط المستخدَم أصلاً بالبطاقة الأساسية `.agp-pcard{display:inline-flex;...}`
  المجاورة لها بنفس الملف — كان ناقصاً فقط من القالب الجديد). سطر واحد،
  لا تغيير آخر بالملف.

### التحقق
- أعدت إنتاج الخلل فعلياً: بنيت صفحة اختبار بنفس CSS الحقيقي الدقيق
  لعنصر `.agp-shell-player-list li` المنسوخ حرفياً من `js/agp-game-shell.js`
  (بدون `display:flex`) — الكود القديم (بدون `display:inline-block`)
  أنتج فعلياً نفس العرض المكسور بالضبط (شظية متناثرة خارج الصندوق)، ما
  يؤكد هذا هو السبب الحقيقي فعلاً، لا تخمين.
- نفس صفحة الاختبار بالكود المُصلَح (`display:inline-block` مضافة) —
  الشارة كاملة، محتواة تماماً داخل صندوقها، الصورة والاسم بمكانهما
  الصحيح — ضد نفس CSS الحقيقي لعنصر `li` هذي المرة (لا افتراض).
- `node --check` ناجح.

### لم يتغيّر
لا تعديل آخر على أي ملف — القياسات/الثوابت/بقية منطق [0.44.5] كما هي
تماماً، هذا إصلاح لخطأ CSS واحد فقط.

---
## [0.44.5] — إعادة تصميم قالب الإطار بالكامل (شارة عريضة موحّدة: بلاطة اسم + دائرة صورة داخل شكل الإطار نفسه)، مبنية ومقاسة من الملف الفعلي النهائي

### السياق
بعد [0.44.4] (القالب الدائري: حلقة رفيعة حول الصورة + اسم كنص عادي
بجانبها)، صاحب المشروع أرسل تصميماً مرجعياً مختلفاً جذرياً وأوضح صراحة
أن هذا هو "المفترض والمطلوب والطبيعي جداً": بدل حلقة رفيعة + اسم منفصل،
الاسم والصورة الحقيقيان يُدمَجان **داخل شكل الإطار نفسه** — شارة عريضة
واحدة فيها منطقتان مخصَّصتان (دائرة صورة + بلاطة اسم) مرسومتان ضمن
تصميم الإطار الفني (تاج/أجنحة/تفاصيل)، ونفس القالب الموحَّد يُطبَّق على
الإطارات الأربعة عشر كلها (4 خاصة + 7 مستوى + أي حصرية لاحقاً) — تأكَّد
هذا صراحة عبر سؤال مباشر لصاحب المشروع.

أول قياس للقالب الجديد تم على ملف **معاينة JPEG** أرسله صاحب المشروع
(1090×843px، خلفية بيضاء صلبة، دائرة الصورة يمين/بلاطة الاسم يسار).
قبل تسليم ذلك القياس، صاحب المشروع أوقفني صراحةً وأخبر أن الملف الفعلي
المرفوع فعلياً على GitHub باسم `frame-founder.png` مختلف — وأرسله. فحص
مباشر لذلك الملف (Python/PIL على قناة الألفا) أثبت أنه **مختلف تماماً**
عن معاينة الـJPEG الأولى في كل شيء تقريباً: PNG 1254×1254px (مربّع، لا
1090×843)، دائرة الصورة على اليسار (لا اليمين)، بلاطة الاسم على اليمين
(لا اليسار)، وهامش شفاف كبير أعلى/أسفل التصميم الفعلي داخل القماشة
المربّعة. لو كان القالب سُلِّم بقياسات الـJPEG القديمة لكانت الصورة
والاسم ظاهرين بمكان خاطئ تماماً فوق الإطار الحقيقي.

### أُصلح — `js/agp-player-card.js` (قالب الإطار بالكامل، إعادة بناء ثالثة)
- ثوابت القياس (`NAME_*_PCT`/`AVATAR_*_PCT`) أُعيد قياسها بالكامل من
  الملف الفعلي `frame-founder.png` — تحليل قناة الألفا +
  Connected-Components (Python/PIL/SciPy) لإيجاد حدود فتحة الصورة
  الشفافة الحقيقية وحدود بلاطة الاسم الفارغة بدقة البكسل، ثم تحقّق بصري
  نهائي عبر عرض حقيقي بمتصفح Chromium (Playwright) لكود `agp-player-card.js`
  الفعلي (لا معاينة يدوية تقريبية) ضد الملف الحقيقي — تطابق تام، لا
  تشويه ولا انزياح.
- القماشة الأصلية مربّعة 1254×1254px، لكن المحتوى الفعلي يشغل شريطاً
  رأسياً فقط بداخلها (هامش شفاف كبير أعلى/أسفل). بدل عرض القماشة كاملة
  (يعطي بطاقة مربّعة ضخمة فيها فراغ شفاف واضح)، البطاقة المعروضة الآن
  بحاوية بحجم ثابت + `overflow:hidden`، وطبقة القماشة الكاملة بإزاحة
  سالبة للأعلى تقصّ الهامش الشفاف بصرياً — **دون أي تعديل على الملف
  الفعلي المرفوع على GitHub** (نفس الملف يُستخدَم كما هو تماماً).
- دائرة الصورة صارت يسار الشارة، بلاطة الاسم يمين (مطابقة لموضعهما
  الحقيقي بالملف، عكس الترتيب المقاس أولاً من معاينة الـJPEG).
- باقي الآلية كما هي: الصورة الشخصية تُرسَم خلف طبقة القماشة (فتظهر
  حواف الإطار الذهبية فوقها تلقائياً)، والاسم فوقها كنص عادي يصغّر
  تلقائياً لو طويل (`fitAllNames`، بدون تغيير)، مع قصّ بـellipsis
  كحل أخير للأسماء الطويلة جداً حتى بأصغر خط.

### التحقق
- `node --check` ناجح على الملف المُعاد بناؤه.
- فحص مباشر لملف `frame-founder.png` الفعلي (`Image.open(...).mode`)
  أثبت `RGBA` فعلية (أكثر من 1.19 مليون بكسل بقيمة ألفا صفر) — الشرط
  الأول (PNG شفاف حقيقي) متوفر فعلياً، ليس افتراضاً.
- تحليل قناة الألفا (Connected-Components) أثبت أن منطقة دائرة الصورة
  **فتحة شفافة حقيقية فعلاً** (لا شعار نخلة معتم كمعاينة الـJPEG
  الأولى) — الشرط الثاني متوفر.
- فحص لوني لمنطقة بلاطة الاسم أثبتها **فارغة تماماً من أي نص مرسوم
  مسبقاً** (لا "AYMN" كمعاينة الـJPEG الأولى) — الشرط الثالث متوفر.
- تحقّق بصري نهائي: تصيير حقيقي عبر Chromium (Playwright) لكود
  `AGP.playerCard.renderHtml`/`fitAllNames` الفعليين (لا نسخة اختبار
  منفصلة) ضد الملف الحقيقي، بثلاث حالات (اسم إنجليزي قصير، اسم عربي
  طويل جداً، صورة شخصية حقيقية عبر رابط خارجي) — الصورة داخل الحلقة
  الذهبية تماماً، الاسم داخل البلاطة الزرقاء تماماً، لا تمديد ولا فراغ
  شفاف ظاهر ولا تراكب.

### لم يتغيّر
لا تعديل على أي ملف غير `js/agp-player-card.js` نفسه (نفس واجهة
`AGP.playerCard.renderHtml(player, opts)`/`fitAllNames(rootEl)`
المستخدَمة من `js/agp-game-shell.js` ومنطق كل لعبة — توقيع الدوال لم
يتغيّر، فلا حاجة لتعديل أي ملف آخر يستدعيها). لا تعديل على ملف الصورة
`frame-founder.png` نفسه.

### ملاحظة صادقة
هذا القالب يفترض أن الإطارات الأربعة عشر الأخرى (المستقبلية) تُصدَّر
بنفس قماشة 1254×1254px ونفس موضع/هامش الشريط المحتوي بالضبط (بما إنها
"قالب موحّد" حسب التأكيد الصريح) — لم يُتحقَّق من ذلك فعلياً لأن باقي
الملفات غير مرفوعة بعد وقت هذا التسليم. لو ملف إطار مستقبلي مختلف
بحجم القماشة أو هامشه أو موضع الدائرة/البلاطة، هذي الثوابت (`CANVAS_SIZE`/
`CONTENT_TOP_PX`/`CONTENT_HEIGHT_PX`/`NAME_*_PCT`/`AVATAR_*_PCT` بأعلى
`js/agp-player-card.js`) تحتاج إعادة قياس لذلك الملف تحديداً بنفس
الطريقة (لا تخمين).

---
## [0.44.4] — إصلاح شكل الإطار (قالب دائري بدل العريض) + أول تفعيل فعلي لنظام الدخوليات باللوبي

### السياق
بعد إصلاح [0.44.3]، الإطار صار يظهر فعلياً — لكن شكله جاء متكسّراً
(ممطوطاً أفقياً بشكل غريب). تشخيص حي بالمتصفح مع صاحب المشروع
(`getComputedStyle` + تحميل فعلي لملف الصورة عبر `Image()`) أثبت السبب
بالأرقام: صندوق البطاقة المعروض 179×56px (مطابق تماماً للقالب القديم
3.2:1 — الكود سليم)، لكن ملف الصورة الفعلي المرفوع (`frame-founder.png`)
**1254×1254px — مربّع 1:1 بالكامل**. قالب [0.37.0]/[0.43.0] القديم كان
يفترض قماشة عريضة (دائرة صورة + صندوق اسم جنبها) لا تطابق شكل الإطارات
الحقيقية المرفوعة إطلاقاً، فكان يمطّها بقوة (`background-size:100% 100%`).

بالتشاور المباشر، القرار: تغيير **الكود** ليطابق شكل الإطارات الحقيقية
(ميداليات/حلقات دائرية) بدل إعادة تصميم/تصدير كل ملفات الإطارات
الأربعة عشر من جديد.

بالتوازي، صاحب المشروع أكّد أنه جهّز دخوليات (نظام `user_entrances`
المبني من [0.37.0] — 4 نماذج أنيميشن gold/neon/fire/ice، بتصميمهم
الكامل مُسلَّم مسبقاً كملف معاينة `entrance-animations-preview.html`)
وعيَّن دخولية لحسابه فعلياً من لوحة الأدمن، لكنها ما ظهرت أبداً باللوبي
— لأن النظام كان مبنياً بالباك إند فقط بدون أي كود بأي لعبة يستهلكه
(موثَّق صراحة بـ[0.37.0]: "الطبقة 2 خارج هذا الإصدار تماماً... لعبة
الروليت مستضافة بمستودع منفصل"). هذا أول تفعيل فعلي لها.

### أُصلح — `js/agp-player-card.js` (قالب الإطار بالكامل، إعادة بناء)
- القماشة صارت **مربّعة (1:1)** دائماً بدل 3.2:1.
- الصورة الشخصية بمنتصف الإطار تماماً (62% من قطر المربّع)، والإطار
  يُرسَم فوقها كاملاً بـ`background-size:contain` (**لا تمديد إطلاقاً**
  مهما كانت أبعاد الصورة الفعلية — أهم فرق عن القالب القديم).
- الاسم لم يعد جزءاً من داخل الصورة (صندوق ثابت بمكان محدَّد) — صار نصاً
  عادياً بجانب الدائرة المؤطَّرة (نفس أسلوب البطاقة الأساسية بدون إطار)،
  أبسط وأكثر أماناً لأي تصميم إطار مستقبلي.
- `RING_BOX_PX`/`AVATAR_SIZE_PCT` ثابتتان واحدتان بأعلى الملف لضبط
  الحجم لاحقاً لو احتاج تعديلاً بسيطاً بعد المعاينة الحية.

### أُضيف — تفعيل نظام الدخوليات (4 ملفات)
- **`backend/collectibles/collectibles-service.js`**:
  `getEquippedEntranceForVerifiedTikTok(tiktokUsername)` جديدة — نظير
  `getEquippedFrameForVerifiedTikTok` الموجودة بالضبط (نفس شرط التحقق:
  حساب مسجَّل + `tiktok_verified=1` + مطابقة يوزرنيم غير حساسة لحالة
  الأحرف).
- **`backend/platforms/tiktok/tiktok-connector.js`**: `onComment`
  الآن يرسل حقل `entrance` إضافي (بجانب `frame` الموجود) — نفس منطق
  `extractEquippedFrame` تماماً (`extractEquippedEntrance`، فشل
  الاستعلام لا يوقف معالجة التعليق).
- **`adapters/agp-tiktok-adapter.js`**: `playerData` عند كل انضمام
  يحمل الآن `entrance` أيضاً (بجانب `avatarUrl`/`frame` الموجودين) —
  يصل تلقائياً لكائن اللاعب بـ`AGP.player` (الحقول الإضافية تُنسخ
  عموماً بدون تفسير، لا حاجة تعديل `agp-player-manager.js`).
- **`js/agp-entrance.js`** (جديد بالكامل): يستمع لأحداث
  `player:joined`/`player:removed`/`player:listReset` من
  `agp-player-manager.js`. لاعب ينضم وله دخولية مفعَّلة → بانر دخول
  كبير (نفس تصميم/ألوان/توقيت `entrance-animations-preview.html`
  المُعتمَد سابقاً حرفياً، الأربعة نماذج) لبضع ثوانٍ → يختفي ويستقر
  كبطاقة صغيرة دائمة (توهج مستمر) طول بقاء اللاعب، وتُزال تلقائياً
  لو خرج. `AGP.playerEntrance.syncSettled(players)` تغطي أيضاً لاعباً
  انضم *قبل* فتح شاشة اللوبي (يفوته البانر لكن يظهر له مباشرة البطاقة
  المستقرة).
- **`js/agp-game-shell.js`**: `renderLobbyScreen()` (شاشة "اللوبي —
  بانتظار اللاعبين" فقط) يضيف حاويتَي `#agp-entrance-stage`/
  `#agp-entrance-settled-list` الفارغتين + استدعاء `syncSettled` بعد
  كل بناء للشاشة. `position:relative` مضافة لـ`.agp-lobby-box` لتثبيت
  موضع البانر بداخلها.
- **`games/elimination-roulette/index.html`**: `<script>` جديد لـ
  `js/agp-entrance.js`، بعد `agp-player-card.js` وقبل `agp-game-shell.js`.

### نطاق متعمَّد (Scope) — راجع تعليقات الكود لتفاصيل كل نقطة
- الدخولية تعمل **فقط** بشاشة اللوبي الرئيسية (نفس نطاق "الإطار يظهر
  باللوبي حصراً" المُتَّبع أصلاً) — لا شاشة الإعدادات (المعاينة
  المصغَّرة)، ولا "إضافة لاعبين جدد" (اللوبي المصغَّر)، ولا أثناء الجولة.
- لاعب انضم أثناء عرض شاشة غير اللوبي الرئيسية (مثلاً شاشة الإعدادات
  مفتوحة وقت انضمامه) يفوته بانر الدخول الكبير نفسه (فات لحظته)، لكن
  يحصل على البطاقة المستقرة فور فتح شاشة اللوبي (`syncSettled`) — لا
  يضيع تأثير الدخولية كلياً.

### لم يتغيّر
لا تعديل على أي جدول/عمود قاعدة بيانات، ولا على مسارات HTTP، ولا على
منطق النقاط/المستويات، ولا على أي لعبة أو ملف غير المذكورين أعلاه.

### ملاحظة صادقة
شكل/حجم الإطار الجديد (62% قطر الصورة داخل المربّع) رقم افتراضي معقول
بناءً على وصف الصورة الوحيدة اللي فحصناها (`frame-founder.png`) — قد
يحتاج ضبطاً بسيطاً (تكبير/تصغير) بعد المعاينة الحية على بث فعلي، خصوصاً
لو باقي الإطارات الـ13 مصمَّمة بنسب "حلقة/هامش" مختلفة شوي عن هذا
الملف. عدِّل `AVATAR_SIZE_PCT` بأعلى `js/agp-player-card.js` (مكان واحد
يُطبَّق على كل الإطارات) لو احتجنا تعديلاً بعد المعاينة.

### التحقق
`node --check` ناجح على الملفات الست الجافاسكريبتية المعدَّلة/الجديدة.
تأكدت بالبحث النصي أن `agp-pcard-framed`/الأسماء القديمة المرتبطة
بالقالب العريض غير مستخدَمة بأي مكان آخر بالمشروع (تعديل معزول بالكامل
داخل `agp-player-card.js`).

---
## [0.44.3] — إصلاح جذري: الإطارات (ونقاط الحسابات الحقيقية) ما كانت تتحقق أبداً — سبب حقيقي مؤكَّد بفحص كود المكتبة

### السياق
بعد توثيق حساب تيك توك بالكامل، الإطار المفعَّل استمر ما يظهر أبداً
باللوبي (لقطة شاشة + بيانات `AymanGamesPlatform.player.getAllPlayers()`
حقيقية من بث فعلي أثبتت المشكلة: اللاعب "AYMN¹" ظهر بـ
`"id": "tiktok:7029124818248565761"` و`"frame": null`، رغم أن الحساب
موثَّق وعليه إطار مفعَّل فعلياً).

الرقم الطويل بحقل `id` هو المفتاح — هذا ليس يوزرنيم تيك توك (المفروض
نص مثل "aymn.gaming")، هذا **رقم داخلي خام** لتيك توك. لتأكيد السبب
تحقّقاً لا تخميناً، ثبّتُّ نسخة `tiktok-live-connector@2.4.3` (نفس
النسخة المعتمَدة بالمشروع) في بيئة فحص منفصلة وفتحت تعريفات الأنواع
الفعلية لمكتبة `tiktok-live-proto` التابعة لها (المصدر الحقيقي لشكل
رسائل تيك توك الخام) — ورسالة الـ `User` الخام (بروتوكول Webcast v3
الحالي) **لا تحتوي حقل `uniqueId` إطلاقاً**. تيك توك أزالته من
البروتوكول الخام؛ الحقل البديل الفعلي بنفس المعنى (اليوزرنيم القابل
للعرض) هو `displayId`.

### أُصلح — `backend/platforms/tiktok/tiktok-connector.js`
`extractUser(data)` كانت تعتمد حصراً على `user.uniqueId`
(`var uniqueId = user.uniqueId || user.id || 'unknown';`) — بما أن هذا
الحقل غير موجود إطلاقاً بالرسالة الخام الفعلية، كانت **تتساقط دائماً
تقريباً على `user.id` الرقمي الخام** بدل اليوزرنيم الحقيقي، لكل تعليق/
هدية/متابعة تقريباً بلا استثناء. الإصلاح:

```js
var identifier = user.displayId || user.uniqueId || user.id || 'unknown';
```

`displayId` أولاً (البديل الفعلي المؤكَّد من كود المكتبة)، ثم
`uniqueId` كاحتياط ثانٍ (لو رجعته نسخة مستقبلية من المكتبة)، ثم `id`
الرقمي كملاذ أخير فقط بدل ما كان أساسياً بالخطأ.

هذا الإصلاح بمكان واحد يصلح تلقائياً **كل** المسارات التالية بدون أي
تعديل إضافي عليها (كلها تعتمد على نفس نتيجة `extractUser`):
- **الإطارات باللوبي**: `extractEquippedFrame(user.uniqueId)` (بالسطر
  الخاص بحدث CHAT) يستخدم `uniqueId` المُرجَع من `extractUser` نفسها —
  يستفيد فوراً من التصحيح.
- **مطابقة نقاط الجولات** (`auth-router.js` → `findVerifiedUserByTikTok`):
  تعتمد على نفس `id`/`uniqueId` القادم من هذا المسار، فكانت تفشل غالباً
  لنفس السبب بالضبط — رغم أن إصلاح [0.44.0] (تقشير بادئة "tiktok:")
  كان صحيحاً بذاته، هذا العطل الأعمق كان يمنعه من النجاح فعلياً على بث
  حقيقي.
- **صورة/اسم تيك توك بالبروفايل** ([0.44.2]) و**التحقق من البايو**
  ([0.44.1]): هذي تعتمد على اليوزرنيم اللي يدخله المستخدم يدوياً عند
  الربط (`linkTikTokUsername`)، غير متأثرة بهذا العطل مباشرة — لكنها
  تستفيد بشكل غير مباشر لأن أي مطابقة لاحقة (نقاط/إطارات) صارت تستخدم
  نفس صيغة اليوزرنيم الآن.

### أُضيف — سجل تشخيص مؤقت (أول 5 أحداث فقط)
لأن هذا التفسير (رغم أنه مبني على دليل حقيقي من كود المكتبة، لا تخمين)
لم يُختبَر بعد ضد بث حقيقي من هذه البيئة (لا وصول شبكي لتيك توك هنا
كالعادة)، أضفت سطر تسجيل يطبع الحقول الخام كاملة (`displayId`,
`uniqueId`, `id`, `secUid`, `nickname`) لأول 5 أحداث فقط بعد كل إعادة
تشغيل للسيرفر، بنفس أسلوب تشخيص [0.44.1]. راجع سجلات Render بعد بث
تجريبي قصير للتأكد أن `displayId` يحمل فعلاً اليوزرنيم الصحيح — لو
أكَّدت ذلك ممكن نحذف هذا السجل لاحقاً (غير ضروري دائماً).

### لم يتغيّر
لا تعديل على أي ملف غير `tiktok-connector.js` — كل الملفات الأخرى
(`collectibles-service.js`, `points-service.js`, `auth-router.js`,
`agp-player-manager.js`, ...) تستهلك نتيجة `extractUser` كما هي دون أي
افتراض خاطئ من جهتها؛ المشكلة كانت بمصدر البيانات نفسه فقط.

### ملاحظة صادقة
هذا التشخيص الآن مبني على دليل حقيقي (فحص فعلي لكود المكتبة المثبَّتة)
وليس تخميناً كما بالمرات السابقة — لكن القيمة الفعلية لـ `displayId` في
بيانات تيك توك الحقيقية (هل هي فعلاً اليوزرنيم بصيغته الصحيحة تماماً؟)
ما زالت تحتاج تأكيداً من بث حقيقي بعد الرفع، عبر سجل التشخيص أعلاه.

### التحقق
`node --check` ناجح على `tiktok-connector.js` المعدَّل. تأكدت بالبحث
النصي أن هذا هو الملف الوحيد بالمشروع كله الذي يستخدم `.uniqueId`، فلا
مكان آخر يحتاج نفس الإصلاح.

---
## [0.44.2] — صورة/اسم تيك توك تصير الهوية الظاهرة بالبروفايل + تفعيل إحصائيات اللاعب الحقيقية

### السياق
طلب صريح: "بعد الربط بتيك توك، صورة واسم الحساب بالمنصة تصير نفس صورة
واسم حساب تيك توك المرتبط" — هذا لم يُبنَ بـ[0.44.0] (كنت بنيت بس أيقونة
صغيرة داخل بطاقة "حساب تيك توك" فرعية، مو الهوية الرئيسية بأعلى
البروفايل). وطلب ثانٍ: تفعيل بطاقات "قريباً" المرتبطة بمستوى/نقاط
اللاعب فعلياً بدل ما تبقى نص ثابت.

**نطاق هذا الإصدار مقصود وضيّق عمداً** (بناءً على تعليمات صريحة "لا
تعدّل أي شي آخر لم يُطلَب منك") — فعّلت فقط الأجزاء اللي عندها بيانات
حقيقية جاهزة بدون أي قاعدة عمل مُخترَعة مني. باقي بطاقات "قريباً"
بالبروفايل (تفصيلها بالأسفل) ما لها بيانات مصدر حقيقية أصلاً ولا تعريف
متفق عليه — تركتها كما هي بانتظار قرارك.

### أُصلح/أُضيف — الهوية الرئيسية بالبروفايل تتبع تيك توك
`profile.html`: بمجرد ما `tiktok_verified && tiktok_username` (نفس
شرط "مرتبط وموثَّق" الحالي)، الصورة الدائرية الرئيسية بأعلى البروفايل
(كانت دائماً الأحرف الأولى من اسم حساب المنصة) والاسم الظاهر بجانبها
يتحوّلون لصورة/اسم عرض تيك توك الحقيقيين — **فقط لو الاستخراج نجح
فعلياً** (`tiktok_avatar_url`/`tiktok_display_name`، راجع [0.44.0]/[0.44.1]
لتحفّظات الاستخراج). لو فشل الاستخراج أو ما ربط تيك توك أصلاً، يبقى
الشكل الأساسي (أحرف أولى + اسم حساب المنصة) تماماً كالسابق — صفر كسر.

### أُضيف — إحصائيات اللاعب الحقيقية (كانت "قريباً" ثابتة)
- **قاعدة البيانات** (`backend/db/database.js`): عمودان جديدان على
  `user_points` — `games_played`، `games_won` (ترقية آمنة عبر
  `ensureColumn`).
- **الباك إند** (`backend/points/points-service.js`): `awardForRoundCompletion`
  يحدّث العدّادين الآن مع كل جولة مكتملة فعلياً (بعد مطابقة الحساب
  الموثَّق — نفس المسار اللي أصلحناه بـ[0.44.0])، بمعزل عن سقف النقاط
  اليومي (المشاركة/الفوز حقيقيان بصرف النظر عن اقتطاع النقاط). `getUserPoints`
  يرجع الآن `gamesPlayed`/`gamesWon`/`winRatePct` أيضاً.
- **الواجهة** (`profile.html`): بطاقة "إحصائيات اللاعب" تعرض الأرقام
  الحقيقية الآن (جولات مكتملة / فوز / نسبة فوز)، وبطاقة "كيف تكسب نقاط
  الخبرة؟" تعرض القواعد الفعلية المبرمجة بالكود (+4 نقاط/جولة، +20
  إضافية للفوز، +4/ساعة لعب، سقف 100/يوم) بدل نص "قريباً" ثابت.

### لم يتغيّر (عمداً — قرارات لسا معلّقة عندك)
هذي بطاقات "قريباً" ثانية بنفس صفحة البروفايل **ما لمستها إطلاقاً**،
كل وحدة محتاجة قرار أو مصدر بيانات مو موجود حالياً:

- **مستوى الستريمر (SP)**: ما فيه أي نظام نقاط/مستويات للستريمرز
  إطلاقاً بالكود الحالي (بعكس نقاط اللاعب) — يحتاج قرار منك: وش
  بالضبط يُحتسَب كـSP؟ (عدد البثوث؟ ساعات البث؟ قيمة الهدايا المستلَمة؟
  متابعين جدد؟ مزيج منهم؟) بمجرد ما تحدد القاعدة أقدر أبنيها فوراً
  بنفس نمط نقاط اللاعب بالضبط.
- **مشاهدات إجمالية/متوسط المشاهدين**: `tiktok-connector.js` الحالي ما
  يستخرج عدد المشاهدين إطلاقاً من بيانات البث — يحتاج تعديل استخراج
  جديد بالباك إند أولاً.
- **الألعاب التي تم بثّها (تفصيل لكل لعبة)**: لا يوجد تتبّع حالياً
  لأي لعبة تحديداً استُخدِمت بكل بث.
- **تحليلات الجمهور (توزيع حسب الدولة)**: بيانات جغرافية غير متوفرة
  إطلاقاً من مكتبة الاتصال الحالية.
- **أبرز اللحظات**: يحتاج تصميم نظام كامل لتحديد/حفظ "لحظة بارزة" —
  لا تعريف موجود لهذا المفهوم بالكود حالياً.
- **البث القادم (جدولة + تذكير)**: ميزة جدولة كاملة غير موجودة —
  الزر معطَّل (`disabled`) بالكود عمداً حتى تُبنى.
- **إنجازات الستريمر**: يحتاج نظام تعريف إنجازات (شروط/شارات) غير
  موجود بعد.

### ملاحظة صادقة
لو صورة/اسم تيك توك ما يظهرون رغم التوثيق، غالباً بسبب نفس مشكلة
استخراج البايو الموثَّقة بـ[0.44.1] (تيك توك يحجب الطلب الآلي) — تحل
تلقائياً بمجرد ما تنجح محاولة تحقق جديدة بعد ذاك الإصلاح.

### التحقق
`node --check` ناجح على الملفات الثلاثة المعدَّلة بجافاسكريبت + سكربت
`profile.html` المضمَّن (فحص منفصل).

## [0.44.1] — تشخيص حقيقي لفشل "الكود غير موجود بالبايو" + تقوية الطلب

### السياق
بلاغ اختبار حقيقي: كتب الكود ببايو حسابه (`zni2aa`) وضغط "تحقق" أكثر من
مرة، ونفس الخطأ `code_not_found_in_bio` يتكرر. جرّبت أجلب نفس صفحة
البروفايل بنفسي (بأداة الفحص عندي) للتأكد، ورجعت محجوبة بـ`robots.txt`
تيك توك — إشارة قوية إن تيك توك يقاوم فعلياً أي وصول آلي/من سيرفر
لصفحات البروفايل، وعلى الأغلب نفس الشي يصير مع طلب الباك إند (يرجّع له
صفحة "تحقّق إنك إنسان" بدل البروفايل الحقيقي، فالكود طبعاً ما يكون
فيها). ما قدرت أثبت هذا 100% (ما عندي وصول شبكي حقيقي لتيك توك من
بيئة التطوير، نفس القيد الموثَّق سابقاً بكل مكان بالمشروع) — فبدل ما
أخمّن حلاً ثاني بلا دليل، سويت شيئين: قوّيت الطلب، وأضفت تشخيصاً حقيقياً
يورينا بالضبط وش يرجع تيك توك بالمرة الجاية.

### أُصلح/أُضيف — `backend/auth/auth-service.js`
- **هيدرز أشبه بمتصفح حقيقي** بدل `User-Agent: Mozilla/5.0` وحدها
  (كانت أشبه بتوقيع سكربت واضح) — الآن UA كامل لمتصفح Chrome حقيقي +
  `Accept`/`Accept-Language`. يقلّل احتمال الحجب، **لا يضمنه كلياً**.
- **تشخيص حقيقي عند فشل "الكود غير موجود"**: بدل ما نرجع بس رسالة خطأ
  عامة، الآن نسجّل بسجلات الخادم (Render Logs) طول الصفحة المستلَمة
  فعلياً، أول 300 حرف منها، وعلامة `looks_like_bot_check` (بحث عن كلمات
  زي "verify you're human"/"captcha"). هذا يحوّل "تخمين" لـ"دليل فعلي"
  بالمحاولة الجاية.

### كيف تشخّص بالضبط
1. جرّب "تحقق" مرة وحدة كمان من نفس صفحة البروفايل.
2. افتح سجلات خدمة الباك إند على Render، ودوّر على سطر يبدأ بـ
   `Auth: TikTok verification code not found`.
3. ابعث لي محتوى السطر كامل (خصوصاً `html_length` و`looks_like_bot_check`
   والـ`snippet`) — بهذا أقدر أقول لك بالضبط إذا كانت المشكلة حجب فعلي
   من تيك توك (وقتها نحتاج نغيّر طريقة التحقق كلياً لشي مختلف)، أو شي
   ثاني تماماً.

### ملاحظة صادقة
لو تأكد إن تيك توك فعلاً يحجب هذا النوع من الطلبات (السيناريو الأرجح)،
الحل الجذري مو تحسين الهيدرز أكثر (سباق ما ينتهي) — الحل الأصح تصميمياً:
التحقق عبر تعليق حقيقي بالبث المباشر (زي "اكتب الكود بالشات") بدل قراءة
البايو، لأن هذا المسار **مُثبَت شغّال فعلياً بالإنتاج** (نفس آلية قراءة
تعليقات تيك توك المستخدَمة للإطارات/الألعاب). أقدر أبنيه كخيار بديل لو
التشخيص أكّد إن قراءة البايو محجوبة فعلياً.

### التحقق
`node --check` ناجح.

---
## [0.44.0] — إصلاح جذري: النقاط ما كانت تُحتسب أبداً تقريباً + عرض صورة/اسم تيك توك بالبروفايل

### السياق
بعد بلاغ باختبار حقيقي: "النقاط والمستوى ما تنحسب"، و"المنصة ماتعترف
بربط حسابات التيك توك". تتبّعت السلسلة كاملة من الملف للملف (مو تخمين):
لقيت باگاً حقيقياً ومؤكَّداً وراء مشكلة النقاط، وباگاً كامناً ثانياً
بمكان غير مرتبط بالضبط بما اشتكى منه المستخدم لكنه خطر مستقبلي حقيقي،
وأضفت ميزة عرض صورة/اسم تيك توك الحقيقيين بالبروفايل.

### أُصلح — `games/elimination-roulette/agp-elimination-roulette.js` و`dashboard-core/js/dashboard-core.js`
**السبب الجذري الحقيقي لعدم احتساب النقاط**: دالة إنهاء المباراة بكلا
الملفين كانت ترسل `tiktokUsername: player.name || player.id` للباك
إند. المشكلة:
- `player.name` = اسم العرض بتيك توك (Nickname)، قابل للتغيير، **مو
  اليوزرنيم الحقيقي**.
- `player.id` = بصيغته الداخلية الكاملة `tiktok:اليوزرنيم` (فيها بادئة
  زايدة).

بينما الباك إند (`findVerifiedUserByTikTok`) يقارن حرفياً ضد
`tiktok_username` المحفوظ بالحساب (بدون أي بادئة). فعملياً المطابقة
تفشل بصمت لكل مشارك تقريباً (إلا لو اسم العرض تصادف كان مطابقاً
لليوزرنيم بالضبط) → صفر نقاط دائماً تقريباً، بصرف النظر عن التوثيق أو
عدد الجولات المُكمَّلة. **هذا الباگ كان موجوداً بالملفين معاً** (نمط
واحد نُسخ من `dashboard-core.js` الأصلي لملف اللعبة).

الإصلاح: نشيل بادئة `tiktok:` من `player.id` قبل الإرسال، ونرسل
اليوزرنيم الخام الحقيقي فقط. تحقّقت برمجياً من منطق الإزالة بـ4 حالات
(معرّف بادئة تيك توك عادي، معرّف فيه نقطة/شرطة سفلية، معرّف بدون بادئة
كحماية، ونص فارغ) — كلها نجحت.

### أُصلح — `backend/auth/auth-service.js` (باگ كامن، اكتُشِف أثناء الفحص)
دالة `validateSession` (تغذّي `/api/auth/me`) كانت **ما تجيب عمود
`tiktok_verified` أصلاً** باستعلامها. النتيجة: أي كود يعتمد على
`AGPAuth.me().tiktok_verified` مباشرة كان بيشوفه `undefined` دائماً
(يعني "غير موثَّق") حتى لو الحساب موثَّق فعلياً بقاعدة البيانات.
**ملاحظة صادقة**: تأكدت إن `profile.html` نفسها ما تأثّرت بهذا (تستخدم
مسار `/api/profile` المنفصل، وهو كان صحيحاً أصلاً من البداية) ولا لقيت
أي كود حالي ثاني يعتمد فعلياً على هذا الحقل من `/api/auth/me` — يعني
مو هذا سبب بلاغك تحديداً على الأغلب، لكنه خطر حقيقي كان قاعد ينتظر أول
كود مستقبلي يعتمد عليه. أضفت العمود للاستعلام + تحويله لـ boolean، بنفس
نمط بقية الحقول بالدالة.

### أُضيف — صورة واسم عرض تيك توك الحقيقيين بالبروفايل
طلب: "بروفايل اللاعبين والاستريمرز، بمجرد ما يربط حسابه بالتيك توك،
تظهر صورته بتيك توك واسم حسابه". التنفيذ:

- **قاعدة البيانات** (`backend/db/database.js`): عمودان جديدان على
  جدول `users` — `tiktok_avatar_url` و`tiktok_display_name` (ترقية
  آمنة عبر `ensureColumn` الموجودة أصلاً، صفر خطر على بيانات موجودة).
- **الباك إند** (`backend/auth/auth-service.js`): لحظة نجاح التحقق
  فعلياً (`verifyTikTokOwnership`)، نفس صفحة بروفايل تيك توك العامة
  المجلوبة أصلاً للبحث عن الكود تُستخدَم أيضاً لاستخراج الصورة (وسم
  `og:image`) واسم العرض (وسم `og:title`) — **بدون أي طلب شبكي إضافي**.
  تُخزَّن فوراً. عند إلغاء الربط (`unlinkTikTok`) تُصفَّر الحقول أيضاً،
  حتى ما تعلق صورة/اسم حساب قديم لو ربط لاحقاً حساباً مختلفاً.
  مُرفَقة الآن ضمن `getPublicProfile` (المصدر اللي تقرأ منه `profile.html`).
- **الواجهة** (`profile.html`): بطاقة "حساب تيك توك" الموجودة أصلاً —
  الرمز 🎵 كان دائماً بديلاً ثابتاً، الآن يتحول لصورة حقيقية + اسم عرض
  فور توفّرهم، مع بقاء 🎵 كبديل تلقائي لو فشل تحميل الصورة (`onerror`)
  أو لو الاستخراج رجع null.

### ملاحظة صادقة
- استخراج الصورة/الاسم من HTML الصفحة العامة (وسوم meta) **تقريبي وغير
  مؤكَّد بالكامل** — نفس تحفّظ باقي استخراجات تيك توك بالمشروع (لا وصول
  شبكي حقيقي لتيك توك من بيئة التطوير هذه لاختباره فعلياً). لو تيك توك
  غيّر بنية صفحته لاحقاً قد يرجع null — هذا لا يكسر التحقق نفسه أبداً،
  فقط الصورة/الاسم يختفوا ويرجع الرمز 🎵 الافتراضي.
- **الحسابات الموثَّقة من قبل هذا الإصدار** ما راح يكون عندها صورة/اسم
  محفوظين تلقائياً (الاستخراج يصير فقط لحظة تحقق جديدة). الحل: إلغاء
  الربط وإعادته مرة وحدة يفعّلها بأثر رجعي — أو أقدر أضيف لاحقاً خياراً
  إدارياً "إعادة جلب" لو تبيه.
- مشكلة **الإطار اللي ما يظهر باللوبي رغم التوثيق الكامل** لسا مفتوحة —
  تتبّعت السلسلة كاملة (تيك توك → الباك إند → المتصفح → اللوبي) ولقيتها
  مبنية صح نظرياً، بعكس باگ النقاط اللي كان واضحاً بالكود. محتاج نتيجة
  خطوة التشخيص المباشر (`?agpDebug=1` + `AGP.player.getAllPlayers()`
  أثناء بث حقيقي) عشان ألقي السبب الفعلي بدل ما أخمّن حلاً ما أضمن
  إنه يعالج المشكلة الحقيقية.

### التحقق
- `node --check` ناجح على الملفين الأربعة المعدَّلة بجافاسكريبت + سكربت
  `profile.html` المضمَّن (استخراج تلقائي بـPython وفحص منفصل).
- محاكاة برمجية لمنطق إزالة بادئة `tiktok:` — 4 حالات، كلها ناجحة.

---
## [0.43.1] — توسيع قائمة هدايا الإنعاش بالدعم (7 ← 20) + أسماء عربية بالعرض

### السياق
بعد نقاش الكتالوج الحقيقي لهدايا تيك توك ([0.43.0])، تبيّن إن ربطه
فعلياً يحتاج تعديلاً بالباك إند + تغيير ترتيب شاشات الإعدادات (الكتالوج
الحقيقي لا يُعرَف إلا بعد الاتصال بالبث، بينما اختيار الهدية يصير قبل
الاتصال حالياً) — تأجَّل لمرحلة لاحقة. الحل الفوري المعتمَد: توسيع
القائمة اليدوية الثابتة لتغطية عدد أكبر من الهدايا الحقيقية المعروفة.

### أُصلح — `games/elimination-roulette/agp-elimination-roulette.js`
- `COMMON_GIFTS` من 7 لـ20 هدية.
- كل هدية الآن باسم عربي بالعرض (`label`) — القيمة الفعلية المطابَقة
  (`value`) بقيت بالإنجليزية كما هي (هذا ما يصل فعلياً بحقل `giftName`
  من `stream:giftReceived`، لا يجوز تعريبه أو المطابقة تفشل بصمت).

### التحقق
- `node --check` ناجح.
- تحقّق برمجي من عدد الهدايا (20 بالضبط) وسلامة بنية المصفوفة.

### ملاحظة صادقة
نفس تحفّظ كل أسماء/تخمينات هذا الملف: قائمة هدايا تيك توك ليست ثابتة
عالمياً (تختلف حسب المنطقة/نسخة التطبيق) — الأسماء أفضل معرفة عامة
متاحة، غير مؤكَّدة 100% ضد حسابك تحديداً. لو أي هدية ما اشتغلت
بالاختبار الحقيقي، افتح `?agpDebug=1` وقارن القيمة الفعلية الواصلة
بـ`payload.giftName` مع الـ`value` المطابق هنا، وأصلح الاسم لو اختلف.

---

## [0.43.0] — بطاقة اللاعب المشتركة + الإطارات باللوبي، وأربعة إصلاحات حقيقية بـ"روليت الإقصاء"

### السياق
اختبار حقيقي ثانٍ لأول لعبة على المنصة ("روليت الإقصاء") بعد [0.42.2] كشف
أربعة أخطاء سلوكية فعلية (بمنطق اللعبة والجذر المشترك)، بالإضافة لطلب
ميزة جديدة: بطاقة لاعب موحّدة (صورة بروفايل تيك توك + اسم الحساب) تظهر
بكل مكان يُعرض فيه لاعب، مع إطار زخرفي يظهر باللوبي فقط لمن يملك حساباً
مسجَّلاً وموثَّقاً بالمنصة وفعّل إطاراً بملفه الشخصي.

### أُصلح — أخطاء حقيقية (ملف اللعبة فقط، `games/elimination-roulette/agp-elimination-roulette.js`)
- **أي شخص بالشات كان يقدر يختار الإقصاء/الإرجاع، مو بس صاحب الدور**:
  `wireCommentListener` كان يتحقق فقط من صحة الرقم المكتوب، بدون أي تحقق
  من هوية كاتب التعليق. صار الآن يتحقق إن `payload.id`/`payload.name`
  يطابق `_pendingTurn.chooser` قبل قبول أي رقم — أي تعليق من شخص ثاني
  يُتجاهَل بصمت.
- **الإنعاش عن طريق الدعم كان لا نهائي**: `eliminatePlayer` كان يبني سجل
  إقصاء جديد بعداد `revivedCount: 0` من الصفر **في كل مرة** يُقصى فيها
  نفس اللاعب من جديد — فيقدر يتنعش بلا حد أقصى فعلي (العداد يرجع صفر مع
  كل دورة إقصاء جديدة له). أُضيف عداد مستقل (`_giftReviveCounts`) مرتبط
  بمعرّف اللاعب نفسه **طول المباراة كاملة** (يُصفَّر فقط مع مباراة جديدة)،
  ويُقارَن بـ`giftRevivalMaxCount` الحقيقي بدل عداد السجل اللحظي.
- **اللاعب المنعَش بالدعم كان يظهر فوراً بنافذة الإقصاء المفتوحة بنفس
  الجولة**: `revivePlayerByEntry` كان يضيفه صراحة لقائمة `_pendingTurn.candidates`
  الحالية لو نافذة إقصاء مفتوحة وقتها. أُزيل هذا السلوك — اللاعب يرجع
  لـ`_alive` فقط، ويشارك بالعجلة تلقائياً بداية الجولة الجاية (مو بنفس
  الجولة اللي رجع فيها).

### أُصلح — خلل جذر حقيقي (`js/agp-game-shell.js`)
- **إضافة لاعب بعد بدء المباراة كانت تُرجع الجميع (حتى المُقصَين) للعجلة**:
  السبب الفعلي مستمع عام `stream:statusChanged` كان يستدعي
  `renderLobbyScreen()` (فيها زر "انهاء وبدء الجولة" اللي يُعيد
  `onStartRound` من الصفر) عند أي حالة "connected" واردة، **حتى لو
  المباراة شغّالة أصلاً**. هذي الحالة تصل فعلياً بدون أي تدخل من
  الاستريمر: إعادة اتصال تلقائية بتيك توك بعد انقطاع مؤقت
  (`tiktok-connector.js` عنده Retry تلقائي فعلي)، أو حتى إعادة اتصال
  قناة WebSocket بيننا وبين الباك إند نفسها (نفس آلية موجودة بـ
  `agp-tiktok-adapter.js`). لو صادف هذا وقت كانت شاشة الإعدادات مفتوحة
  (بالضبط لما الاستريمر يفتحها لإضافة لاعب)، تُستبدَل فجأة بشاشة اللوبي
  الأولى، وضغط "انهاء وبدء الجولة" يصفّر المباراة بالكامل ظناً إنه
  المسار الصحيح. أُضيف علم `_roundStarted` — بعد بدء الجولة، أي تغيّر
  لاحق بحالة الاتصال يُتجاهَل تماماً بهذا المستمع (لا تأثير على أي شاشة
  مفتوحة). المسار الصحيح والآمن أصلاً لإضافة لاعب (زر "➕ فتح التسجيل
  لإضافة لاعبين" بشاشة الإعدادات المفتوحة أثناء اللعب) لم يتأثر ويبقى
  يعمل كما صُمم من [0.41.0].

### أُضيف — بطاقة اللاعب المشتركة + الإطارات (جذر + باك إند، ميزة جديدة)
- **`js/agp-player-card.js`** (ملف جذر جديد كلياً): وحدة `AGP.playerCard`
  قابلة لإعادة الاستخدام من أي لعبة حالية أو مستقبلية — `renderHtml(player, opts)`
  تبني بطاقة (صورة تيك توك + اسم الحساب)، و`fitAllNames(rootEl)` تصغّر
  تلقائياً حجم خط أي اسم يفيض عن صندوقه الثابت (لا يتمدد الصندوق نفسه).
  قالب الإطارات (نسبة القماشة 3.2:1، فتحة الصورة 90% من الارتفاع بهامش
  5% من العرض، فتحة الاسم بهامش 4% بعدها لين 96% من العرض بارتفاع 65%)
  موثَّق بالتفصيل أعلى الملف — أي إطار جديد (كتالوج أو مخصّص) لازم
  يلتزم بنفس النسب بالضبط.
- **`backend/platforms/tiktok/tiktok-connector.js`**: كل تعليق وارد
  الآن يحمل أيضاً `avatarUrl` (رابط صورة بروفايل تيك توك) و`frame`
  (الإطار المفعَّل لصاحب التعليق، لو يملك حساباً موثَّقاً بالمنصة —
  عبر دالة جديدة `getEquippedFrameForVerifiedTikTok`).
- **`backend/collectibles/collectibles-service.js`**: دالة جديدة
  `getEquippedFrameForVerifiedTikTok(tiktokUsername)` — تبحث عن حساب
  مسجَّل **موثَّق فعلياً** (`tiktok_verified = 1`) بنفس يوزرنيم تيك توك،
  وترجع إطاره المفعَّل (لو موجود) أو `null`. لا اعتمادية دائرية مع
  `auth-service.js` (استعلام مستقل بنفس الشرط).
- **`backend/protocol/message-builder.js` / `message-schema.js` /
  `backend/websocket/ws-server.js`**: `avatarUrl`/`frame` يمرّان الآن
  ضمن رسالة `comment` كاملة، بنفس أسلوب [0.42.2] بالضبط (تعديل نقطة
  واحدة بكل ملف، بدون أي تغيير على الشكل العام للرسائل).
- **`adapters/tiktok/agp-tiktok-adapter.js`**: `avatarUrl`/`frame`
  يُرفقان الآن ضمن بيانات اللاعب المرسَلة لـ`AGP.keywordManager`/
  `AGP.queueManager` — تصل تلقائياً لكل مكان يُستخدَم فيه كائن اللاعب
  (`AGP.player`/`AGP.gameManager.getPlayers()`) بدون أي تعديل إضافي على
  `agp-player-manager.js`/`agp-keyword-manager.js` (يحفظان أي حقل إضافي
  كما هو أصلاً دون تفسير).
- **`js/agp-game-shell.js`**: قوائم اللاعبين الثلاث بشاشات اللوبي/الإعدادات
  (`renderLobbyPlayerList`، `renderSettingsPlayerList`،
  `renderMiniLobbyList`) تستخدم الآن `AGP.playerCard` بدل نص `<li>` عادي
  — `showFrame: true` دائماً (كلها شاشات لوبي). حقل جديد اختياري
  `_config.assetBasePath` (تمرره كل لعبة حسب عمق مجلدها) لحل مسار صور
  الإطارات بشكل صحيح.
- **`games/elimination-roulette/agp-elimination-roulette.js`**: قائمة
  الأحياء/المُقصَين (`renderAliveList`) وصفوف نافذة اختيار الإقصاء/الإرجاع
  (`renderTurnModal`) تستخدم نفس البطاقة، لكن **بدون إطار عمداً**
  (`showFrame: false`) — القرار الصريح إن الإطار يظهر باللوبي حصراً.
- **`games/elimination-roulette/index.html`**: سطر تحميل جديد لـ
  `js/agp-player-card.js` قبل `agp-game-shell.js`.

### التحقق
- `node --check` ناجح على كل الملفات العشرة المعدَّلة/الجديدة.
- اختبار تسلسل كامل حقيقي (نفس أسلوب [0.42.2]): بناء رسالة `comment`
  بـ`avatarUrl`/`frame`/`isFollower` → `JSON.stringify`/`parse` (تماماً
  مثل WebSocket حقيقي) → التحقق من وصول الثلاثة سليمة + شكل الغلاف صحيح
  عبر `message-schema.js`. نجح.
- اختبار منطقي مستقل لـ`getEquippedFrameForVerifiedTikTok` (قاعدة
  بيانات وهمية تحاكي شكل الاستعلامات الفعلي، لأن `better-sqlite3` تعذّر
  تجميعه بهذي البيئة بدون وصول شبكي لملفات Node الثنائية): 4 سيناريوهات
  (مستخدم موثَّق بإطار كتالوج، مستخدم غير موثَّق [يُرفض رغم تطابق
  اليوزرنيم]، يوزرنيم غير موجود، مدخل فارغ) — كلها نجحت.
- إصلاحات ملف اللعبة الثلاثة (`agp-elimination-roulette.js`) تحقّقت
  بمراجعة منطقية يدوية دقيقة + `node --check`، بدون تشغيل فعلي بمتصفح
  حقيقي (نفس القيد الموثَّق بكل الميزات المرتبطة بواجهة اللعبة منذ
  [0.41.0]) — يحتاجون تأكيد صاحب المشروع على بث حقيقي.

### لم يتغيّر / مؤجَّل عمداً
- **ميزة "إنعاش صديق"**: لسا بانتظار توضيح السلوك المطلوب بالضبط (شرط
  تفعيلها الحالي — وقوف العجلة على نفس الشخص مرتين متتاليتين — لم
  يُلمس، راجع نقاش سابق).
- **كتالوج هدايا تيك توك الحقيقي** (بدل القائمة اليدوية الثابتة
  `COMMON_GIFTS`): لا يزال يحتاج تصميماً منفصلاً (مسار باك إند جديد
  لجلب الكتالوج الفعلي من `tiktok-live-connector` + دعم بـ
  `agp-game-shell.js` لحقل يتحمّل قائمته ديناميكياً) — خارج نطاق هذا
  الإصدار.
- لا تعديل على `backend/auth/auth-service.js` أو `backend/db/database.js`
  — الميزة الجديدة تستخدم الجداول/الدوال الموجودة أصلاً فقط.

### ملاحظات صادقة
- **`avatarUrl` غير مؤكَّد بالكامل**: نفس تحفّظ `isFollower` الأصلي —
  `extractAvatarUrl` بـ`tiktok-connector.js` يجرّب عدة أشكال محتملة
  لحقل `profilePicture` (حسب نسخة `tiktok-live-connector` v2.4.3)
  ويرجع `null` لو ما لقى شيء، لكن لم يُختبَر ضد بث حقيقي من هذه البيئة
  (لا وصول شبكي لتيك توك هنا) — يحتاج تأكيداً فعلياً بعد النشر، بنفس
  أسلوب `_debugFollowStatus` المستخدم لـ`isFollower`.
- **نسخة `agp-release-FINAL` المحلية كانت قديمة لملفين**: `backend/protocol/message-builder.js`
  و`backend/websocket/ws-server.js` بهذي النسخة المحلية لم يكونا يحتويان
  إصلاح [0.42.2] (isFollower) أصلاً — استُخدمت بدلاً منهما النسختان
  المُصلَحتان فعلياً من التسليم السابق كنقطة انطلاق، تجنّباً لأي تراجع
  عرضي عن [0.42.2]. تأكّد منها بمقارنة مباشرة قبل أي تعديل.
- **`games/elimination-roulette/` لم يكن موجوداً إطلاقاً بنسخة
  `agp-release-FINAL` المحلية** رغم توثيقه بـ[0.41.0] — استُخدمت أحدث
  نسخة متاحة من هذا الملف من عمل الجلسة السابقة كنقطة انطلاق. لو فيه أي
  تعديل يدوي مباشر عليه بالمستودع الحقيقي بعد [0.42.2] لم يصلني، ممكن
  يكون ضاع هنا — يُستحسَن مقارنة سريعة (Diff) بعد الرفع للتأكد.
- **ملاحظة جانبية غير مرتبطة بهذا الإصدار**: `games/elimination-roulette/index.html`
  يحمّل `../../adapters/agp-tiktok-adapter.js` (بدون مجلد فرعي `tiktok/`)،
  بينما الملف الفعلي بالمستودع (حسب النسخة المحلية المتاحة لي) موجود
  على `adapters/tiktok/agp-tiktok-adapter.js` (**مع** `tiktok/`) — تضارب
  محتمل بمسار سكربت لم ألمسه (خارج نطاق هذا التسليم، ولا أعرف أيهما
  يطابق الوضع الحقيقي المنشور فعلياً على Render). يستحق تأكيداً سريعاً
  منك قبل الرفع: افتح صفحة اللعبة فعلياً وتأكد من Console إنه ما فيه
  خطأ 404 على هذا السكربت تحديداً.
- **صور الإطارات نفسها لسا مسؤولية الأدمن**: هذا الإصدار يبني فقط منطق
  العرض/الربط — أي إطار فعلي (كتالوج أو مخصّص) لازم صورته PNG تُرفَع
  يدوياً لجذر المستودع بنفس أسلوب `logo.png`، وتلتزم بالضبط بقالب النسب
  الموثَّق أعلى `js/agp-player-card.js`.

---

## [0.42.2] — السبب الجذري الحقيقي: `isFollower` كان يُفقَد قبل وصوله للمتصفح إطلاقاً

### السياق
اختبار حقيقي بعد [0.42.1]: صاحب المشروع تابع حساب البث فعلياً وأرسل
الكلمة المفتاحية بالشات، **ولم يدخل اللوبي رغم كونه متابعاً حقيقياً**.
هذا كشف إن تشخيص [0.42.0] كان صحيحاً جزئياً بس ناقص — كان فيه سبب أعمق
لم يُكتشف وقتها.

### السبب الجذري الحقيقي (بعد تتبّع المسار الكامل من الباك إند للمتصفح)
- `backend/platforms/tiktok/tiktok-connector.js` يحسب `isFollower` فعلاً
  وصح ضمن الكائن الممرَّر لـ`_callbacks.onComment(data)`.
- **لكن `backend/websocket/ws-server.js`** (حلقة الوصل بين الموصِّل
  والمتصفح عبر WebSocket) كان معالج `onComment` فيه يبني رسالة الخروج
  هكذا: `builder.buildCommentMessage(platform, data.id, data.name, data.text)`
  — **يتجاهل `data.isFollower` تماماً**، ما يمرره للدالة إطلاقاً.
- **`backend/protocol/message-builder.js`**: دالة `buildCommentMessage`
  نفسها أصلاً **ما عندها حتى معامل (Parameter) لـ`isFollower`** — توقيعها
  كان `(platform, id, name, text)` فقط.
- **النتيجة**: `isFollower` يُحسَب صح بالخطوة الأولى، لكن يُفقَد بالكامل
  بخطوتين متتاليتين قبل ما توصل الرسالة للمتصفح. فالواجهة الأمامية كانت
  تستقبل `isFollower: undefined` **دائماً**، لكل شخص، بصرف النظر عن حالة
  متابعته الحقيقية — يعني بوابة [0.42.0]/[0.42.1] كانت ترفض **الجميع**
  فعلياً لما `followersOnly` مفعّل، حتى المتابعين الحقيقيين. هذا يفسّر
  بدقة اختبار صاحب المشروع: تابع فعلاً، أُرسلت الكلمة، ورُفض رغم ذلك.

### أُصلح
- **`backend/protocol/message-builder.js`**: `buildCommentMessage` تقبل
  الآن معامل خامس `isFollower`، ويُضاف لحمولة الرسالة الصادرة.
- **`backend/websocket/ws-server.js`**: معالج `onComment` يمرر
  `data.isFollower` الآن عند بناء الرسالة.
- **`backend/protocol/message-schema.js`**: تحديث التوثيق (تعليق فقط،
  لا تحقق فعلي) ليعكس الحقل الجديد بحمولة `comment`.

### التحقق
- `node --check` ناجح على الملفات الثلاثة.
- **اختبار طرف-لطرف كامل جديد** (غير مقتصر على الواجهة الأمامية هذي
  المرة): محاكاة `tiktok-connector.js` يحسب `isFollower` → `ws-server.js`
  يبني الرسالة → تسلسل JSON فعلي (`JSON.stringify`/`parse`، تماماً مثل
  WebSocket حقيقي) → `agp-tiktok-adapter.js` يستقبلها ويطبّق البوابة.
  النتيجة: متابع حقيقي يدخل اللوبي، غير متابع يُرفض — كلاهما نجح فعلياً
  عبر المسار الكامل، مو بس جزء منه كالاختبارات السابقة.

### لم يتغيّر
- لا تعديل على `adapters/agp-tiktok-adapter.js` أو `js/agp-game-shell.js`
  (نسخة [0.42.1] تبقى صحيحة كما هي — المشكلة لم تكن فيهما).
- لا تعديل على منطق حساب `isFollower` نفسه بـ`tiktok-connector.js` (يبقى
  نفس الملاحظة الصادقة القديمة: تخمين `followStatus === '1' || '2'` غير
  مؤكد 100%، بانتظار تأكيد فعلي على بث حقيقي الآن بعد ما البيانات تصل
  فعلياً للمتصفح لأول مرة).

### ملاحظة صادقة
هذا الإصلاح ضروري لتشتغل ميزة "المتابعون فقط" على الإطلاق — بدونه، حتى
لو منطق [0.42.0]/[0.42.1] كان مثالياً، كانت النتيجة العملية واحدة دائماً:
رفض الجميع. بعد هذا الإصلاح، الاختبار الحقيقي التالي (متابعة فعلية +
كلمة مفتاحية) من المفترض ينجح — يحتاج تأكيد صاحب المشروع بعد النشر
لتحديد ما إذا بقي أي خلل بمنطق `extractIsFollower` نفسه (تخمين
`followStatus` لسا غير مؤكد كما هو موثَّق سابقاً).
---

## [0.42.1] — تصحيح: حذف ميزة "الاستثناء اليدوي" من إصلاح [0.42.0]

### السياق
بعد نشر إصلاح [0.42.0]، تبيّن إن فهمي لجزء واحد من المتطلب الأصلي كان
غلط. المطلوب الفعلي أبسط ومختلف عمّا نُفِّذ:
- متابع فعلي + كلمة مفتاحية بالشات → دخول تلقائي (هذا الجزء كان صحيحاً
  من [0.42.0] ولم يتغيّر).
- غير متابع + كلمة مفتاحية → **رفض دائم**، حتى يتابع الحساب فعلياً على
  تيك توك ويكتب الكلمة من جديد — عندها حالته `isFollower` تصير `true`
  تلقائياً من الباك إند، ويدخل عادي **بدون أي خطوة يدوية من الاستريمر**.

الإصدار [0.42.0] أضاف بدل ذلك واجهة "استثناء يدوي" (حقل يوزرنيم + زر
بشاشة اللوبي) يسمح للاستريمر يدخل شخصاً غير متابع يدوياً — هذا لم يكن
مطلوباً، ويُحذف بالكامل بهذا الإصدار.

### أُصلح — إزالة كاملة لميزة الاستثناء اليدوي
- **`adapters/agp-tiktok-adapter.js`**: حذف `_followerExceptions`،
  `normalizeExceptionKey`، `isFollowerException`،
  `AGP.services.TikTokService.addFollowerException`،
  `AGP.services.TikTokService.removeFollowerException` بالكامل. بوابة
  `handleIncomingComment` تبقى بنفس منطق [0.42.0] (رفض غير المتابع لو
  `followersOnly` مفعّل) لكن **بدون** شرط الاستثناء — رفض غير مشروط.
- **`js/agp-game-shell.js`**: `renderLobbyScreen()` رجعت **حرفياً**
  لنفس نسختها الأصلية قبل [0.42.0] (تأكّدنا بمقارنة مباشرة إنها مطابقة
  100% للنسخة الأصلية) — حذف حقل "➕ إضافة استثناء" ودالة
  `handleAddFollowerException` بالكامل.
- **`backend/platforms/tiktok/tiktok-connector.js`**: تحديث تعليق
  `_followersOnly` ليعكس عدم وجود أي استثناء يدوي بعد الحين.

### التحقق
- `node --check` ناجح على الملفات الثلاثة.
- إعادة الاختبار المنطقي المحلي (نفس أسلوب [0.42.0]) بسيناريوهات محدَّثة:
  (أ) غير متابع يُرفض، (ب) متابع حقيقي يدخل تلقائياً، (ج) **نفس الشخص
  يصير متابعاً فعلياً بعدها ويكتب الكلمة من جديد → يدخل تلقائياً بدون
  أي تدخل يدوي** (هذا السيناريو بالذات هو المطلوب الأصلي، وتم التأكد
  إنه يعمل صح)، (د) "الكل" يدخل الجميع بدون قيد. كل السيناريوهات نجحت.
- تأكدنا ببساطة إن `agp-game-shell.js` رجعت مطابقة حرفياً (Diff) لنسختها
  الأصلية قبل أي تعديل — لا أثر متبقٍّ لميزة الاستثناء.

### لم يتغيّر
- منطق حساب `isFollower` بالباك إند نفسه لم يتغيّر (يبقى نفس الملاحظة
  الصادقة: تخمين غير مؤكد 100%، بانتظار تأكيد فعلي على بث حقيقي).
- لا تعديل على أي ملف آخر غير الثلاثة أعلاه.

---

## [0.42.0] — إصلاح: خيار "المتابعون فقط" غير مُفعَّل فعلياً بأي لعبة

### السياق
أثناء اختبار لعبة "روليت الإقصاء"، لوحظ إن خيار "من يقدر يدخل؟" (الكل /
المتابعون فقط) بشاشة الإعدادات **ما يأثر على شي فعلياً** — أي شخص يدخل
اللوبي بمجرد كتابة الكلمة المفتاحية بالشات، بغض النظر عن الخيار المحدد.

### السبب الجذري (بعد فحص الكود الفعلي)
- الباك إند (`backend/platforms/tiktok/tiktok-connector.js`) يحسب حالة
  المتابعة (`isFollower`) فعلياً لكل تعليق وارد ويرسلها ضمن بياناته،
  لكن متغيّر `_followersOnly` بنفس الملف **متعمَّد عدم استخدامه للفلترة**
  — بتعليق يحيل لملف `agp-shell-config.js` **غير موجود إطلاقاً بالمستودع**
  (تأكدنا: 404 بكل الأماكن المحتملة). على الأرجح إشارة لعمل خُطط له ولم
  يُنفَّذ أبداً.
- الواجهة الأمامية (`adapters/agp-tiktok-adapter.js`) كانت تستقبل
  `isFollower` من الباك إند ضمن كل تعليق، لكن **تتجاهله تماماً** عند
  بناء بيانات اللاعب المرشّح للانضمام قبل تمريرها لـ`AGP.keywordManager`.
- `AGP.keywordManager.checkKeyword` (عام ومحايد بالتصميم، راجع تعليقه
  التوثيقي) يتحقق فقط من تطابق نص الكلمة، ولا يعرف شيئاً عن حالة المتابعة.
- **النتيجة**: الميزة غير مكتملة بالكامل عبر السلسلة، لا نقطة فعلية
  توقف غير المتابع في أي مكان.

### أُصلح
- **`adapters/agp-tiktok-adapter.js`** (ملف مشترك — الإصلاح عام لأي لعبة
  تستخدم `AGP.streamConnector`/`AGP.services.TikTokService`، وليس خاصاً
  بلعبة روليت الإقصاء وحدها):
  - `isFollower` يُمرَّر الآن ضمن بيانات كل لاعب مرشّح.
  - بوابة تحقق جديدة قبل استدعاء `AGP.keywordManager.checkKeyword`: لو
    `followersOnly` مفعّل بخيارات الاتصال و`isFollower` = false ولا يوجد
    استثناء يدوي لهذا الحساب → رفض صامت (حدث `keyword:rejected` بسبب
    `not_follower`)، بدون دخول اللوبي.
  - دالتان جديدتان على `AGP.services.TikTokService`:
    `addFollowerException(usernameOrId)` و`removeFollowerException(usernameOrId)`
    — قائمة استثناءات محلية بالمتصفح لجلسة الاتصال الحالية (تُطابَق
    بالمعرّف أو باليوزرنيم، غير حساسة لحالة الأحرف).
- **`js/agp-game-shell.js`** (ملف مشترك — نفس النطاق العام): شاشة اللوبي
  تعرض الآن حقل "➕ إضافة استثناء" (يوزرنيم + زر) **فقط لو خيار
  "المتابعون فقط" مفعّل بإعدادات اللعبة الحالية**، يستدعي
  `addFollowerException` مباشرة عند الإضافة.
- **`backend/platforms/tiktok/tiktok-connector.js`**: تصحيح تعليق توثيقي
  فقط (لا تغيير سلوك) — إزالة الإشارة الخاطئة لملف `agp-shell-config.js`
  غير الموجود، وتوضيح إن الفلترة الفعلية تتم بالواجهة الأمامية.

### التحقق
- فحص صحة الصياغة (`node --check`) لكل الملفات الثلاثة: ناجح.
- اختبار منطقي محلي (محاكاة بروتوكول WebSocket بالكامل، بدون اتصال شبكي
  حقيقي بتيك توك — غير متاح من بيئة العمل): 6 سيناريوهات مغطاة ونجحت
  كلها: (أ) رفض غير المتابع، (ب) قبول المتابع الحقيقي، (ج) قبول غير
  المتابع بعد إضافته كاستثناء (بالاسم)، (د) نفس الشي بالمعرّف، (هـ) قبول
  الجميع لما "الكل" مفعّل، (و) رجوع الرفض بعد إزالة الاستثناء.

### لم يتغيّر
- لا أي تعديل على `AGP.keywordManager` نفسه (يبقى عاماً ومحايداً كما
  صُمم) ولا على أي ملف `agp-*.js` أساسي آخر غير المذكورين أعلاه.
- لا تغيير على منطق حساب `isFollower` بالباك إند نفسه (يبقى تخميناً غير
  مؤكد 100%، بانتظار `_debugFollowStatus` على بث حقيقي — نفس الملاحظة
  الصادقة الموثقة سابقاً بالكود).

### ملاحظة صادقة
هذا الإصلاح عام لأي لعبة تستخدم نفس محوّل تيك توك — أي لعبة قادمة تستخدم
خيار "المتابعون فقط" بنفس النمط (`followersOnly` بخيارات الاتصال) تحصل
على نفس الحماية تلقائياً بدون أي عمل إضافي. لم يُختبَر على بث تيك توك
حقيقي من أي بيئة AI (لا اتصال شبكي فعلي بتيك توك متاح هنا) — الاختبار
الحقيقي الأول يحتاج تأكيد صاحب المشروع على بث فعلي.

---

## [0.41.0] — "روليت الإقصاء" (Elimination Roulette): أول لعبة أصلية (Native) داخل المنصة

### السياق
أول لعبة جديدة كلياً تُبنى بعد فك ربط الألعاب الثلاث القديمة [0.39.0].
بعكس روليت القبائل (مستضافة خارجياً، مربوطة عبر `js/agp-game-bridge.js`
وpostMessage)، هذه اللعبة **أصلية بالكامل** داخل نفس هذا المستودع —
صفحتها الخاصة تحمّل AGP Core كاملاً + `js/agp-game-shell.js` (ملف عام
كان موجوداً بالمستودع من قبل لكنه غير مستخدم/غير موثَّق حتى الآن — لم
يُعدَّل هنا، فقط اعتُمد عليه كما هو) + منطق اللعبة نفسها. تحلّ محل بطاقة
`coming-soon-1` (من البطاقات العشر الجاهزة منذ [0.39.0]).

### أُضيف — `Project-Testing`
- **`games/elimination-roulette/agp-elimination-roulette.js`** (جديد):
  منطق اللعبة بالكامل —
  - تسجيل كـ Game Object حقيقي عبر `AGP.gameManager.registerGame(...)`
    (`id: 'elimination-roulette'`)، ودورة حياة الجولة بالكامل عبر
    الأحداث العامة الموجودة أصلاً (`game:roundStarted`/`game:roundEnded`/
    `game:reset`) — بنفس نمط `dashboard-core/js/dashboard-core.js`
    (`NS.components.round`). صفر تعديل على أي ملف `agp-*.js` أساسي.
  - حقول إعدادات المباراة (عبر `AGP.gameShell.init`، بدون أي تعديل على
    `agp-game-shell.js` نفسه): الحد الأقصى للاعبين (عدّاد)، دخول الكل/
    المتابعون فقط (مربوط مباشرة بخيار `followersOnly` الجاهز أصلاً
    باتصال تيك توك الحقيقي)، تفعيل "انعاش صديق"، تفعيل "الإنعاش عن
    طريق الدعم" + اختيار نوع الهدية (قائمة هدايا شائعة ثابتة — لا يوجد
    API فعلي لكتالوج هدايا تيك توك بالمشروع حالياً) + عدد مرات الإنعاش
    المسموحة، موقّت الإقصاء (20/25/30/40 ثانية) + سلوك انتهاء الوقت
    (إقصاء صاحب الدور أو تخطي دوره فقط).
  - عجلة تدور (يدوياً من الاستريمر) وتتوقف عشوائياً عند لاعب حي، تفتح
    نافذة اختيار الإقصاء (لاعبون آخرون مرقّمون) — الاختيار يتم بكتابة
    الرقم بشات البث (عبر حدث `stream:commentReceived` الموجود أصلاً)،
    أو بضغطة مباشرة من الاستريمر على الاسم (طبقة راحة إضافية).
  - تكرار نفس الاسم مرتين متتاليتين (لو "انعاش صديق" مفعّلة) يفتح نافذة
    إرجاع لاعب مُقصى بدل نافذة الإقصاء لتلك الجولة.
  - الإنعاش عن طريق الدعم عبر حدث `stream:giftReceived` الموجود أصلاً —
    مطابقة اسم الهدية المحدَّدة، وسقف عدد مرات لكل لاعب.
  - الحد الأقصى للاعبين يُطبَّق بإغلاق التسجيل تلقائياً (`AGP.lobby.close()`
    الجاهزة أصلاً) عند الوصول للعدد — **لا سعة قصوى مفروضة بأي مكان آخر
    بالمنصة** (ديْن معماري موثَّق أصلاً بـ`ARCHITECTURE.md`)، فهذا أول
    تطبيق فعلي لها، محصور بمنطق هذه اللعبة فقط.
  - عند بقاء لاعب واحد: تقرير نقاط حقيقي عبر
    `window.AGPAuth.reportRoundCompletion(...)` (نفس مسار
    `/api/points/round-complete` الحقيقي المستخدَم أصلاً من
    `dashboard-core`، بدون أي نظام نقاط جديد).
- **`games/elimination-roulette/index.html`** (جديد): صفحة استضافة
  اللعبة، تحمّل AGP Core كاملاً بنفس ترتيب `index.html` القياسي، ثم
  `js/agp-game-shell.js`، ثم `agp-elimination-roulette.js`.
- **`index.html`**: بطاقة `coming-soon-1` السابقة صارت بطاقة نشطة
  (`data-agp-game-id="elimination-roulette"`, `data-agp-game-status="active"`)
  بنفس نمط بطاقات الألعاب النشطة القديمة (غلاف + عنوان + وصف + زر لعب
  يفتح `games/elimination-roulette/index.html` بتبويب جديد). بوابة
  الدخول العامة (تسجيل دخول + موافقة الأدمن على `.btn-play`) تعمل معها
  تلقائياً بدون أي تعديل إضافي (مستمع عام على `document`، لا يعتمد على
  معرّف لعبة محدَّد).
- **`elimination-roulette-cover.png`** (جديد، بجذر المستودع): غلاف
  مبدئي تلقائي التوليد بهوية بصرية بنفسجية متوافقة — استبدله بأي صورة
  غلاف حقيقية بنفس الاسم وقتما تجهز.

### ملاحظات صادقة
- **تصحيح بخصوص اسم المستودع**: بداية هذه الجلسة افترضنا (بناءً على
  معلومة أعطانيها صاحب المشروع) أن الواجهة الأمامية تعيش بمستودع منفصل
  اسمه `agp-platform`، وأن `Project-Testing` نسخة قديمة/مرجعية فقط. لاحقاً
  تبيّن أن **لا يوجد مستودع باسم `agp-platform`** أصلاً — كل الملفات
  (الواجهة الأمامية والباك إند والتوثيق) موجودة فعلياً بمستودع
  `Project-Testing` نفسه فقط (تأكّد ذلك من كون ملف `index.html` اللي
  رفعه صاحب المشروع بنفسه كنسخة "الموقع الفعلي المنشور" مطابقاً حرفياً
  لنسخة `Project-Testing`). لذلك كل ملفات هذا الإصدار (الأربعة أعلاه)
  رُفعت فعلياً لمستودع `Project-Testing` وحده، لا يوجد أي رفع لأي مستودع
  آخر. لا معرفة أكيدة لسبب الالتباس الأول (ربما اسم قديم من مرحلة سابقة
  للمشروع، أو خلط مع اسم خدمة Render)، لكن هذا لا يؤثر على صحة أي ملف من
  ملفات هذا الإصدار نفسها.
- **`js/agp-game-shell.js` غير مذكور بأي إصدار سابق بهذا السجل** رغم
  وجوده فعلياً بالمستودع (تاريخ Git يُظهر حذفه مرة وإعادة رفعه/تعديله 4
  مرات بدون أي توثيق) — اعتُمد عليه هنا **كما هو دون أي تعديل** بعد
  مراجعته كاملاً والتأكد من سلامة منطقه (شاشة إعدادات + اتصال تيك توك +
  لوبي فعلية وتعمل بشكل صحيح). لم يُختبر على اتصال تيك توك حقيقي فعلي
  (لا يوجد اتصال شبكي حقيقي من بيئة العمل تسمح بذلك).
- تفسير "تكرار الاسم مرتين متتاليتين" طُبِّق حرفياً كمقارنة كل دورة
  بالدورة السابقة مباشرة فقط (لا أي تكرار أطول) — لو المقصود مختلف، هذا
  معزول بدالة واحدة (`handleWheelLanded`) سهلة التعديل.
- لا اختبار فعلي على بث تيك توك حقيقي (نفس القيد الموثَّق أصلاً بكل
  الميزات المرتبطة بتيك توك بهذا المشروع) — تم التحقق من صحة الكود
  (Syntax) وتوازن الوسوم فقط، بنفس أسلوب [0.40.1].

### لم يتغيّر
- صفر تعديل على أي ملف `js/agp-*.js` أساسي، ولا على `js/agp-game-shell.js`
  نفسه، ولا على منطق أو تصميم أي عنصر آخر بالصفحة الرئيسية (البانر،
  عداد الزوار، شريط الداعمين، بقية البطاقات).
- صفر تعديل على الباك إند (`backend/`) — كل التكامل مع تيك توك والنقاط
  يمر عبر أحداث ومسارات موجودة أصلاً وتعمل فعلياً.

---
## [0.40.1] — فحص وتصحيح تناسق الجوال (نتيجة سؤال مباشر عن الاستجابة)

### السياق
سؤال مباشر: هل الشاشة الرئيسية متناسقة فعلاً مع شاشات الجوال مع الحفاظ
على تناسقها مع باقي الشاشات؟ الجواب الصادق وقتها كان "لا" — لم يكن قد
تم فحص هذا فعلياً بمتصفح حقيقي من قبل، فقط تحقق من صحة الكود (Syntax).
بعد طلب الفحص الفعلي، تم تنفيذه.

### طريقة الفحص (بصراحة تامة)
- إضافة تمديد `mcp__claude-in-chrome__*` (امتداد المتصفح) غير متاح في
  بيئة العمل هذه، فتم استخدام أداة اختبار محلية (Playwright) بدلاً منه:
  تشغيل الصفحات على خادم ملفات محلي، وفحص كل صفحة على 5 مقاسات شاشة
  فعلية (320px، 375px، 414px، 768px، 1280px) — من أصغر جوال شائع إلى
  شاشة حاسوب — والتأكد برمجياً من عدم وجود أي تجاوز أفقي (Horizontal
  Overflow) في أي صفحة، مع لقطة شاشة (Screenshot) فعلية لكل حالة
  للمراجعة البصرية المباشرة.
- **ملاحظة صادقة على حدود هذا الفحص**: تم الفحص على نسخة محلية ثابتة من
  الملفات (ليس الموقع المنشور فعلياً على Render)، ولا يوجد اتصال شبكي
  من بيئة العمل هذه لسيرفر الـ Backend الحقيقي، فأي محتوى ديناميكي يعتمد
  على استدعاء الـ API (كالبانر المتبدّل للداعمين أو الثيم المفعّل) لم
  يظهر أثناء الفحص، لكن هذا لا يؤثر على فحص التناسق البصري/الأبعاد
  (CSS/Layout) نفسه وهو ما كان مطلوباً هنا.

### وُجد
- **الصفحة الرئيسية (`index.html`) فقط**: تجاوز أفقي فعلي على شاشات
  الجوال الضيقة (320px و375px) — السبب: شريط التنقل العلوي
  `nav.mainnav` أصبح يحتوي 5 عناصر بعد إضافة زر "دعم المنصة" مؤخراً في
  هذه الجلسة، بدون خاصية `flex-wrap` تسمح له بالانتقال لسطر جديد عند
  ضيق المساحة، فيخرج جزء من الشريط عن حدود الشاشة.
- باقي الصفحات المفحوصة (`admin.html`، `profile.html`، `support.html`،
  `top-supporters.html`) لا يوجد فيها أي تجاوز أفقي على أي من المقاسات
  الخمسة المفحوصة.

### أُصلح
- **`index.html`** (CSS فقط، لا تغيير في المنطق أو البنية):
  - `nav.mainnav`: إضافة `flex-wrap: wrap` (بدل عدم التفاف)، وتحويل
    الفراغ (`gap`) إلى صيغة صف/عمود منفصلة (`12px 24px`) لمسافة أفضل
    عند الالتفاف لسطرين.
  - إضافة `@media (max-width: 480px)` جديد (لا يوجد له أثر إطلاقاً على
    الشاشات الأكبر): تقليل بسيط في المسافات وحجم الخط لعناصر شريط
    التنقل وزر "دعم المنصة" فقط عند شاشات الجوال الضيقة تحديداً.
- تم إعادة الفحص الكامل (نفس الـ 5 صفحات × 5 مقاسات = 25 فحص) بعد
  التعديل: **صفر حالات تجاوز أفقي** في كل الصفحات وكل المقاسات، وتمت
  مراجعة لقطات الشاشة بصرياً للتأكد من أن شريط التنقل يلتف بشكل مقروء
  ومرتّب على الشاشات الضيقة.

### لم يتغيّر
- لا أي تعديل على منطق JS، أو على أي ملف `js/agp-*.js`، أو على أي صفحة
  غير `index.html`.
- تم التحقق من سلامة الكود بعد التعديل بنفس أسلوب الجلسة (فحص كل كتلة
  `<script>` داخلية عبر `node --check`، والتأكد من توازن وسوم `<div>`).

---

## [0.40.0] — ثيم المناسبات (تغيير ألوان الموقع مؤقتاً من لوحة الأدمن)

### السياق
سؤال: هل ممكن تغيير ثيم/ألوان المنصة بسهولة حسب المناسبات الوطنية
(اليوم الوطني، يوم التأسيس...)؟ الجواب نعم — أُضيف نظام ثيم بسيط:
3 أكواد لون فقط، يُفعَّل/يُعطَّل من `admin.html` بضغطة، يُطبَّق فوراً
على الصفحة الرئيسية لكل زائر بدون أي نشر جديد أو تعديل كود. **تم
الاتفاق صراحة إن البانر (الصورة الترويجية أعلى الصفحة) يبقى خارج هذا
النظام** — صاحب المشروع يغيّره يدوياً بنفس طريقة رفع `hero-banner.png`
الحالية وقت كل مناسبة، فلا حاجة لمكان جديد له.

### أُضيف — Backend (`Project-Testing`)
- **`backend/db/database.js`**: جدول جديد `site_theme` — صف واحد ثابت
  (id = 1، نفس نمط جدول `announcement`)، active + 3 أكواد لون
  (accent/accent_2/accent_pink) + preset_key اختياري.
- **`backend/theme/site-theme-service.js`** (جديد): `getActiveTheme()`
  (ترجع null لو غير مفعَّل)، `setTheme(presetKey, accent, accent2,
  accentPink)` (تحقّق من صحة كل كود Hex قبل الحفظ)، `clearTheme()`.
- **`backend/http/auth-router.js`**: `GET /api/theme` (عام)،
  `POST /api/admin/theme` و`POST /api/admin/theme/clear` (أدمن فقط).

### أُضيف — Frontend
- **`auth/auth-client.js`**: `getSiteTheme`، `adminSetSiteTheme`،
  `adminClearSiteTheme`.
- **`admin.html`**: قسم جديد "🎨 ثيم المناسبات" — قائمة ثيمات جاهزة
  (🇸🇦 اليوم الوطني السعودي، 🏛️ يوم التأسيس، 🎨 مخصّص) تملأ 3 منتقيات
  لون (`<input type="color">`) تلقائياً عند الاختيار، وتبقى قابلة
  للتعديل اليدوي قبل الحفظ. زرّان: "تفعيل الثيم" و"إلغاء التفعيل
  (رجوع للوضع الطبيعي)"، وحالة حالية واضحة (مفعَّل/غير مفعَّل).
  **ملاحظة صادقة**: أكواد الثيمين الجاهزين (الوطني/التأسيس) تقريبية —
  لا يوجد لدينا مصدر رسمي مؤكد لكل كود بالضبط؛ عدّلها من نفس اللوحة لو
  عندك الكود الرسمي.
- **`index.html`**: سكربت جديد مبكر (يشتغل مباشرة بعد تحميل
  `auth-client.js`، قبل أي شيء ثاني) يجلب الثيم النشط عبر
  `AGPAuth.getSiteTheme()` ويطبّقه فوراً عبر
  `document.documentElement.style.setProperty` على `--accent`،
  `--accent-2`، `--accent-pink`، `--accent-gradient` (يُشتَق من
  اللونين الأول والثاني)، و`--border` (نفس لون accent بشفافية 0.28،
  متّسق مع القيمة الافتراضية). **لا تعديل على أي قاعدة CSS ثابتة** —
  استبدال قيم متغيرات فقط وقت التشغيل، فيرجع الموقع لألوانه الافتراضية
  تلقائياً بمجرد "إلغاء التفعيل" من الأدمن، بدون أي نشر جديد.

### لم يتغيّر
- البانر (`hero-banner.png`) خارج هذا النظام تماماً — يبقى يُدار يدوياً
  زي ما كان، بطلب صريح من صاحب المشروع.
- صفر تعديل على أي قاعدة CSS ثابتة أو ملف `agp-*.js`. النطاق الحالي
  `index.html` فقط (الصفحة الأكثر ظهوراً للزوار) — الصفحات الثانية
  (`profile.html`، `admin.html`، صفحات الدعم...) كل واحدة فيها لوحة
  ألوان مستقلة خاصة بها، لم تُشمَل بهذا الإصدار؛ يمكن التوسعة لها
  لاحقاً لو احتجت.

---

## [0.39.0] — فك ربط 3 ألعاب، قصاصات احتفالية، شاشة بداية أكبر، ونظام داعمي المنصة

### حُذف — روليت القبائل / مين الامبوستر / مافيا (فك ربط فقط)
بطلب صريح: أُزيلت بطاقات الثلاث ألعاب من `index.html` بالكامل (والسطر
اللي يحمّل `games/roulette/agp-roulette.js`)، تحضيراً لألعاب جديدة.
**فك ربط فقط، لا حذف**: ملفات الربط نفسها باقية بالمستودع (`games/roulette/`
وغيرها) بدون حذف، ومستودعات الألعاب محفوظة خارجياً عند صاحب المشروع —
يسهل ترجيع أي واحدة لاحقاً (بطاقتها من تاريخ Git + سطر السكربت لو
روليت). البطاقات العشر "قريباً" الجاهزة لم تتأثر.

### أُضيف — قصاصات احتفالية متساقطة مع فيديو نهاية حفلة الترحيب
طبقة `#welcome-confetti-layer` تغطي شاشة الحفلة كاملة، تبدأ بالضبط مع
بداية تشغيل فيديو النهاية (`playEndingVideo()`) وتتوقف وتُمسَح تلقائياً
لما تنتهي الحفلة (`finishCeremony()`). قصاصات إيموجي (🎉🎊✨🎈🥳🪅) عشوائية
الموضع/السرعة/الحجم، CSS/JS عادي بدون مكتبات خارجية، تحترم
`prefers-reduced-motion`.

### تغيّر — شاشة البداية (Splash)
اللوقو أكبر وضوحاً (`clamp(140px, 24vw, 220px)` بدل 96px ثابت)، وأنيميشن
دخول بارز (تكبير + دوران + توهج خلفي متوهّج) بدل الظهور البسيط السابق،
وأنيميشن خروج مماثل بالقوة (تكبير + تمويه + تلاشي) بدل التلاشي البسيط.
المدة الإجمالية باقية ثانيتين كما اتُّفق سابقاً.

### أُضيف — نظام داعمي المنصة (زر "دعم المنصة"، شريط متحرك، توب الداعمين)

**⚠️ ملاحظة صادقة مهمة قبل التفاصيل**: لا يوجد حالياً أي ربط تلقائي مع
منصة الدعم الفعلية (كريترز/دكان تب سابقاً، creators.sa) — بحثنا فلم
نجد توثيق API/Webhook عام لها، وصاحب المشروع بانتظار رد الدعم الفني
منهم. لذلك النظام الآن **إدخال يدوي بالكامل من الأدمن**: يشوف الدعم
فعلياً بلوحة تحكم كريترز، ثم يضيفه يدوياً من `admin.html`. متى توفّر
Webhook حقيقي من عندهم، يُضاف مسار HTTP عام جديد يستقبله وينادي
`addSupporter()` بدون أي تغيير على التخزين أو العرض — مستقلان تماماً
عن مصدر الإدخال.

**Backend (`Project-Testing`)**:
- **`backend/db/database.js`**: جدول جديد `supporters` (id, name,
  message, amount, created_at).
- **`backend/supporters/supporters-service.js`** (جديد): `addSupporter`،
  `listRecent(limit)` (الأحدث أولاً — للشريط المتحرك)، `listTop(limit)`
  (مجموع المبالغ لكل اسم، تنازلياً — لصفحة توب الداعمين)، `listAll`
  (لوحة إدارة الأدمن)، `deleteSupporter` (تصحيح خطأ إدخال).
- **`backend/http/auth-router.js`**: مسارات جديدة —
  `GET /api/supporters/recent` وGET `/api/supporters/top` (عامة، بدون
  تسجيل دخول)، و`GET/POST /api/admin/supporters` +
  `POST /api/admin/supporters/delete` (أدمن فقط).

**Frontend**:
- **`auth/auth-client.js`**: `getRecentSupporters`، `getTopSupporters`،
  `adminListSupporters`، `adminAddSupporter`، `adminDeleteSupporter`.
- **`admin.html`**: قسم جديد "داعمو المنصة" — نموذج إضافة (اسم + رسالة
  اختيارية + مبلغ) وجدول بكل الإدخالات مع زر حذف لكل صف.
- **`index.html`**:
  - زر "💜 دعم المنصة" جديد بالهيدر، بجانب "المجتمع" مباشرة، بتصميم
    بارز (تدرّج الألوان الرئيسي) يربط لصفحة `support.html` الجديدة.
  - الشريط اللي كان فيه نص "لتفعيل الاشتراك مجاناً..." الثابت صار شريط
    داعمين متحرك: آخر 3 داعمين، كل وحد يظهر 4 ثوانٍ بالتدوير المتكرر
    (تلاشي بسيط بين كل واحد والي بعده)، مع إعادة جلب دورية كل 30 ثانية
    تلتقط أي دعم جديد تلقائياً (يحل مكان الأقدم من آخر 3). لو ما فيه
    داعمون بعد، يبقى النص الافتراضي القديم كما هو بدون أي تغيير.
- **`support.html`** (صفحة جديدة): شرح قصير عن الدعم، زر دعم بارز
  (رابط مؤقت `href="#"` بانتظار رابط كريترز الحقيقي — معلَّق بتعليق
  واضح بالملف نفسه لصاحب المشروع يستبدله مباشرة من GitHub)، رابط
  لصفحة توب الداعمين، ورابط تواصل تيك توك، + قائمة "آخر الداعمين".
- **`top-supporters.html`** (صفحة جديدة): كل الداعمين مرتّبين حسب
  إجمالي مبلغ الدعم، بميداليات 🥇🥈🥉 لأول 3، وعدد مرات دعم كل واحد.
- **`profile.html`**: تمييز بصري خفيف (لمسة ذهبية على رأس البروفايل +
  الأفاتار) لأي حساب "ستريمر موافَق عليه فعلياً من الأدمن" عن بروفايل
  اللاعب العادي — Modifier إضافي (`.pp-header--streamer`) بنفس أسلوب
  `.game-card--v2`، بدون أي تعديل على البنية الأساسية.

### لم يتغيّر
- صفر تعديل على `js/agp-game-bridge.js` أو أي ملف `agp-*.js` آخر.
- صفر تعديل على البطاقات العشر "قريباً" الجاهزة.

---

## [0.38.3] — إصلاح: روليت القبائل كانت تفتح مباشرة بدون فحص الجلسة + تسريع شاشة البداية

### إصلاح — قفل "العب الآن" ما كان يشتغل على روليت القبائل تحديداً
**السبب الجذري**: روليت القبائل هي اللعبة الوحيدة المتصلة فعلياً بجسر
`AGP.gameBridge` (`js/agp-game-bridge.js` — منطق `AGP.*` أساسي لا
يُعدَّل)، وهو يضيف مستمع `click` خاص به على رابط "العب الآن" يفتح نافذة
اللعبة فوراً (`window.open`) بدون أي علم بحالة تسجيل الدخول. لأن سكربتات
`AGP.*` تُحمَّل وتُسجِّل مستمعها **قبل** سكربت فحص الجلسة (آخر سكربت
بالصفحة)، كان مستمع الجسر يفتح اللعبة فوراً قبل ما يوصل الدور لفحصنا —
فتفتح روليت مباشرة حتى لحساب غير موافَق عليه، بعكس "مين الامبوستر"
و"مافيا" (ما فيهم جسر مشابه، فمستمع الفحص كان الوحيد عليهم ويشتغل
صح).
**الحل**: بدون أي لمسة على `agp-game-bridge.js` أو `agp-roulette.js` —
غيّرنا طريقة تسجيل مستمعنا فقط (`index.html`، السكربت الأخير) من مستمع
مباشر على كل رابط `.btn-play`، إلى مستمع واحد مفوَّض على `document`
بمرحلة الالتقاط (`capture: true`). مرحلة الالتقاط تمر عبر `document`
**قبل** ما يوصل الحدث لعنصر الرابط نفسه أصلاً، فنقدر نوقفه بالكامل
(`preventDefault` + `stopPropagation`) قبل ما يوصل لمستمع الجسر
إطلاقاً — لأي لعبة متصلة بجسر مستقبلاً، لا روليت فقط.

### تغيّر — شاشة البداية (Splash)
- المدة قبل الاختفاء: 3 ثوانٍ → **ثانيتان**.
- سرعة أنيميشن الاختفاء نفسه: 0.6 ثانية → **0.3 ثانية** (اختفاء أسرع
  بوضوح، حسب الطلب).

### لم يتغيّر
- صفر تعديل على `js/agp-game-bridge.js`، `games/roulette/agp-roulette.js`،
  أو أي ملف `agp-*.js` آخر.

---

## [0.38.2] — تبسيط مرحلة نهاية حفلة الترحيب: فيديو واحد بدل عدّ تنازلي + صوت منفصل

### السياق
طلب صريح من صاحب المشروع بعد [0.38.1]: بدل مرحلة "عدّ تنازلي 5→0 + ملف
صوت خلفية منفصل + فيديو اختياري"، تصير المرحلة الأخيرة **فيديو واحد
فقط** (بصوته المدمج بداخله، بدون ملف صوت منفصل)، يشتغل تلقائياً فور
قص الشريطة، مدته المتوقعة 10 ثوانٍ، ولما ينتهي **تنتهي الحفلة بالكامل
تلقائياً** ويرجع المستخدم يستخدم المنصة بشكل طبيعي.

### تغيّر — `index.html`
- حُذف عنصر `<audio id="welcome-audio">` نهائياً (ما عاد هناك ملف صوت
  منفصل مطلوب — راجع القسم أدناه).
- حُذف الرقم النابض `#welcome-countdown-num` وأنيميشنه (`countdownPulse`)
  بالكامل من الـHTML والـCSS.
- `#welcome-video` صار بدون خاصية `muted` (الصوت المدمج بالفيديو نفسه
  هو المصدر الوحيد للصوت الآن).
- منطق الإنهاء الجديد: عند قص الشريطة، تبدأ مرحلة الفيديو مباشرة
  (`playEndingVideo()`) — الفيديو يشتغل تلقائياً، ومستمع لحدث
  `ended` يستدعي `finishCeremony()` فور انتهاء الفيديو فعلياً. **مؤقّت
  احتياطي 10 ثوانٍ** (`videoFallbackTimer`) يضمن انتهاء الحفلة حتى لو
  الفيديو تعذّر تشغيله أو تحميله (مثال: الملف غير مرفوع بعد) — حماية
  من "تعليق" الحفلة للأبد بانتظار حدث لن يصير. حارس `ceremonyEnded`
  يمنع استدعاء `finishCeremony()` مرتين (من الفيديو والمؤقّت معاً لو
  تصادفا).

### أُضيف/حُذف — الملفات المطلوب رفعها
- **حُذف تماماً**: `welcome-sound.mp3` ما عاد مطلوباً إطلاقاً.
- **باقٍ كما هو**: `welcome-video.mp4` فقط — ارفعه لجذر مستودع
  `agp-platform` بنفس أسلوب `logo.png`. لازم يحتوي الصوت بداخله (مو
  فيديو صامت)، ومدته المفضَّلة ~10 ثوانٍ (المؤقّت الاحتياطي 10 ثوانٍ
  بالضبط، فلو الفيديو أطول من كذا بيُقطَع عند الثانية العاشرة حتى لو
  ما خلص — لو تبي مدة أطول خبرني أعدّل المؤقّت ليطابقها بالضبط).

### لم يتغيّر
- صفر تعديل على السلايدات الأربعة، مرحلة قص الشريطة، أو الاستدعاء
  التلقائي عبر `AGPAuth.needsWelcome()` — نفس سلوك [0.38.1] بالضبط.

---

## [0.38.1] — حفلة ترحيب الستريمر الجديد بعد موافقة الأدمن

### السياق
تنفيذ نقطة "حفلة الترحيب" المؤجَّلة من [0.38.0] بعد نقاش تفصيلي أكّد
كل التفاصيل (مكان الظهور، سلوك عدم التكرار، مكان زر إعادة التفعيل
بالأدمن). لا تعديل على أي منطق `AGP.*` أو منطق قفل الألعاب أو بطاقتَي
"مين الامبوستر"/"مافيا" — الميزة بالكامل إضافية (Additive).

### أُضيف — Backend (`Project-Testing`)
- **`backend/db/database.js`**: عمود جديد `users.welcome_completed`
  (`INTEGER NOT NULL DEFAULT 0`) عبر `ensureColumn` (Migration آمنة،
  لا تمس أي بيانات موجودة — كل المستخدمين الحاليين يبدؤون بـ`0`، أي
  "لسا ما شافوا الحفلة"، وهذا مقصود فقط لمن سيُوافَق عليه كستريمر
  مستقبلاً؛ لا تأثير عملي على من هو ستريمر موافَق عليه حالياً إلا لو
  الأدمن فعّل له "إعادة الترحيب" يدوياً).
- **`backend/auth/auth-service.js`**:
  - `validateSession()` و`listAllUsersWithStats()` يرجّعان الآن
    `welcome_completed` (Boolean) ضمن بيانات المستخدم.
  - دالتان جديدتان: `completeWelcome(userId)` (تصفّر الحاجة للحفلة —
    تُستدعى فقط بعد إكمال الستريمر لها فعلياً بالكامل، وليس عند مجرد
    فتح الصفحة، عمداً حتى لو أغلق المتصفح أو حدّث الصفحة منتصف الحفلة
    تبدأ من جديد بالزيارة الجاية) و`resetWelcome(userId)` (يصفّرها
    لتظهر مرة وحدة إضافية — تُستدعى فقط من مسار الأدمن أدناه).
- **`backend/http/auth-router.js`**: مساران جديدان —
  `POST /api/auth/welcome/complete` (يتطلب Bearer، أي مستخدم لحسابه
  فقط) و`POST /api/admin/welcome/reset` (يتطلب Bearer + دور أدمن،
  بجسم `{ userId }`).

### أُضيف — Frontend (`agp-platform`)
- **`auth/auth-client.js`**: دوال جديدة بـ`window.AGPAuth` —
  `needsWelcome(user)` (شرط الظهور: `role === 'streamer'` +
  `can_run_games === true` + `!welcome_completed`)، `completeWelcome()`،
  `adminResetWelcome(userId)`.
- **`admin.html`**: زر "🎉 إعادة الترحيب" داخل جدول المستخدمين، بنفس
  خلية صلاحية الألعاب (بجانب مربّعات الصلاحيات مباشرة، حسب طلب صاحب
  المشروع) — يستدعي `AGPAuth.adminResetWelcome(userId)` عند الضغط،
  وعنوانه (`title`) يوضّح إن كان المستخدم شافها من قبل أو لسا.
- **`index.html`**: نافذة `#welcome-overlay` كاملة (كانت مضافة بالـHTML/
  CSS فقط بمرحلة سابقة، أُضيف الآن المنطق الكامل):
  - 4 سلايدات (نصوص مسوَّدة مؤقتة — عدّلها مباشرة داخل `#welcome-slides`
    بالـHTML وقت ما تجهّز النص النهائي)، تنقّل بينها بزر "التالي" الذي
    يتحوّل تلقائياً إلى "إنهاء" بالسلايد الأخير.
  - مرحلة قص الشريطة: ضغطة زر المقص (✂️) تضيف كلاس `is-cut` (أنيميشن
    CSS يفرّق نصفَي الشريطة)، وتشغّل الصوت والفيديو **مباشرة داخل نفس
    معالج الضغطة** (لضمان تجاوز سياسات المتصفح لمنع التشغيل التلقائي —
    Autoplay يحتاج إيماءة مستخدم مباشرة).
  - عدّ تنازلي من 5 إلى 0 بعد القص، ثم استدعاء `AGPAuth.completeWelcome()`
    وإخفاء/إزالة النافذة كلياً من الصفحة.
  - **ملفات الصوت والفيديو لسا غير مرفوعة**: `welcome-sound.mp3`
    (يُشغَّل دائماً) و`welcome-video.mp4` (اختياري، ~10 ثوانٍ) —
    ارفعهما لجذر المستودع بنفس أسلوب `logo.png`؛ الفيديو له `onerror`
    آمن (لو ما رفعته، الحفلة تكمل عادي بدون فيديو، الصوت فقط).
  - **الاستدعاء التلقائي**: مربوط داخل `refreshUser().then(...)`
    الموجود أصلاً بالسكربت الأخير — بعد كل تحديث ناجح لبيانات الجلسة من
    الخادم، لو `AGPAuth.needsWelcome(freshUser)` رجّعت `true`، تُستدعى
    `window.__showWelcomeCeremony()` تلقائياً. أي مستخدم غير ستريمر
    موافَق عليه، أو أكمل الحفلة قبل كذا، لا يشوف شيء إطلاقاً.

### لم يتغيّر
- صفر تعديل على منطق قفل الألعاب، ترتيب تحميل ملفات `agp-*.js`، أو
  بطاقتَي "مين الامبوستر"/"مافيا".
- المستخدمون الحاليون (قبل هذا الإصدار) لن تظهر لهم الحفلة تلقائياً
  إلا لو الأدمن فعّلها لهم يدوياً من `admin.html` — القيمة الافتراضية
  للعمود الجديد `1` منطقياً بمعنى "أكملها" لمن هو موافَق عليه بالفعل؟
  **تنويه صادق**: القيمة الافتراضية فعلياً `0` (لم يكملها) لكل الصفوف
  القديمة والجديدة على حد سواء — يعني كل ستريمر موافَق عليه حالياً
  (حتى لو من قبل هذا الإصدار) سيشوف الحفلة بأول زيارة له لـ`index.html`
  بعد رفع هذا التحديث، مرة واحدة فقط. لو ما تبي هذا السلوك لمستخدمين
  قدامى معيّنين، خبرني نضيف استثناء لهم قبل الرفع.

---

## [0.38.0] — إعادة تصميم الصفحة الرئيسية (index.html) — دفعة أولى

### السياق
بعد نقاش تفصيلي نقطة بنقطة مع صاحب المشروع (بالضبط بنفس أسلوب نقاش
نظام المقتنيات بـ[0.37.0])، اتُّفق على دفعة تعديلات بصرية/هيكلية على
`index.html` فقط — **بدون أي تعديل على منطق `AGP.*` أو ترتيب تحميل
ملفاته أو منطق قفل الألعاب الحالي** (تم التأكيد الصريح إنه يبقى كما
هو 100%). نقطة "حفلة ترحيب الستريمر الجديد بعد موافقة الأدمن" **لم
تُنفَّذ بعد** — لسا بانتظار تفاصيل إضافية من صاحب المشروع (مكان
الظهور بالضبط، سلوك القطع/الاستئناف، مكان زر "إعادة الترحيب" بالأدمن)
قبل التنفيذ.

### أُضيف/تغيّر — `index.html`
- **لوحة الألوان**: اعتماد الأكواد `#7C3AED` (بنفسجي رئيسي)، `#00C2FF`
  (سماوي)، `#FF4DFF` (وردي)، `#0E0E16` (خلفية داكنة)، `#FFFFFF` (أبيض)
  بدل الألوان القديمة التقريبية. التدرّج الرئيسي `--accent-gradient`
  صار بنفسجي↔وردي (بدل بنفسجي↔سماوي) لتقليل حضور السماوي البصري كما
  طُلب — السماوي (`--accent-2`) باقٍ متاحاً بس لمسات محدودة فقط.
- **اللوقو**: تكبير من 38px إلى 48px بالهيدر.
- **الهيرو**: حذف كتلة النص المكررة (العنوان الكبير + الوصف + صف
  الإيموجي) لأنها مكررة مع البانر المرفوع أصلاً. شارة "منصة مجانية"
  صارت أيقونة دائرية فقط بدون نص (🆓، مع `title`/`aria-label` لقارئ
  الشاشة). أُضيف سهم نابض بهدوء (CSS فقط، `prefers-reduced-motion`
  يوقفه) ينزل لزر جديد "🎵 حسابنا تيك توك" يربط لـ `@em70_q` مباشرة.
- **أزرار الفئات الثلاثة تحت الهيرو**: أُعيد ترتيبها وتسميتها — "🎵
  ألعاب تيك توك" (رابط فعّال يوصّل لـ `#games-section`، بدّل دور زر
  "ألعاب أون لاين" القديم)، و"💬 ألعاب دسكورد" و"🕹️ ألعاب منصة الكيك"
  (الاثنان `disabled` + شارة "قريباً" — كانا أصلاً بدون أي وظيفة
  برمجية، فقط تصميم).
- **⚠️ منطق قفل الألعاب لم يتغيّر إطلاقاً**: تأكيد صريح من صاحب
  المشروع أن رسالتَي القفل الحاليتين (زائر بلا حساب ← تسجيل دخول/إنشاء
  حساب | حساب مسجَّل غير موافَق ← تواصل عبر تيك توك) صحيحتان تماماً
  ويجب أن تبقيا كما هما لأي لعبة حالية أو مستقبلية — صفر تعديل على
  الجافاسكربت المرتبط بـ`showLockMessage`/`canPlayGames`/`hasSession`.
- **بطاقات الألعاب**: تصميم جديد (v2) بمقاس 250×250 (صورة غلاف
  200×130، زر شرح 75×30، خط Almarai من Google Fonts) طُبِّق **فقط على
  بطاقة روليت القبائل** + البطاقات الجديدة أدناه — بقرار صريح، بطاقتَي
  "مين الامبوستر" و"مافيا" **تركتا بتصميمهما القديم تماماً بدون أي
  لمسة** (سيُعاد تصميمهما لاحقاً بشكل منفصل). هذا يعني تفاوت مقاس
  مؤقت بين البطاقات بنفس الصف — معروف ومقصود، مو خطأ.
- **10 بطاقات "قريباً" جديدة**: أماكن محجوزة فارغة (`data-agp-game-status="coming-soon"`
  بدل `"active"` حتى لا تُحسب ألعاب فعلية بأي منطق مستقبلي)، بنفس
  تصميم v2، بدون رابط لعب فعلي (`disabled`). عند تجهيز كل لعبة لاحقاً:
  عدّل الصورة/العنوان/الرابط مباشرة داخل بطاقتها وفعّلها كـ`"active"`.
- **قسم "منصة متكاملة للاستريمرز"**: أُبقي بنفس المحتوى النصي، أُضيف
  أنيميشن ظهور بسيط (fade + slide-up) عند الوصول له بالسكرول
  (`IntersectionObserver`، مع fallback آمن للمتصفحات القديمة — العنصر
  يبقى ظاهراً طبيعياً بدونه). أزرار "قريباً" الثلاثة تحته حُدِّث نصها
  لمطابقة النموذج المعتمد ("🎁 ليربتات جاهزة للتحميل"، "🔔 إيقونات
  البثوث"، "🖼️ بانرات وقوالب البث") مع شارة "قريباً" واضحة على كل واحد.
- **قسم إحصائيات المنصة**: أُلغي إخفاؤه (`display:none` السابق) — يظهر
  الآن فاضياً (بدون أرقام حقيقية بعد، حسب الاتفاق) بنفس تصميم النموذج.
- **الفوتر**: أُعيد بناؤه بالكامل ليطابق النموذج المعتمد — عمودين
  (روابط مهمة + "من نحن")، معلومات تواصل كروابط مباشرة فعلية (تيك توك
  كان موجوداً، واتساب صار رابط `wa.me/966582411235` حقيقي بدل نص عادي)،
  وسطر حقوق النشر. روابط "سياسة الاستخدام"/"سياسة الخصوصية" (كانتا
  `href="#"` بدون وجهة) + 3 روابط جديدة (تنويه مهم / اقتراح الأفكار /
  الإعلانات والرعايات) تشير الآن لصفحات فعلية جديدة (راجع أدناه).
- **خلفية زخرفية ثابتة**: أيقونات عالم الألعاب (🎮🎲🕹️🏆...) بشفافية
  منخفضة جداً (0.05) وبدون أي حركة، طبقة واحدة خلف المحتوى، لا تؤثر
  على الأداء أو خفة الصفحة (نص Emoji فقط، صفر طلبات شبكة إضافية).
- **شاشة بداية (Splash)**: شعار على خلفية مغبشة (`backdrop-filter:
  blur`) يظهر أول تحميل، يختفي تلقائياً بعد 3 ثوانٍ بأنيميشن fade
  احترافي (~0.6 ثانية) ثم يُزال كلياً من الـ DOM — لا يعيق أي تفاعل مع
  الصفحة تحته بعد اختفائه.
- **SEO**: `<meta name="description">`، Open Graph + Twitter Card
  tags، `<link rel="canonical">`، ووسم JSON-LD بسيط (`WebSite` schema).
  النصوص مسوَّدة أولية — عدّلها مباشرة من GitHub وقتك تبي.

### أُضيف — 5 صفحات جديدة فارغة (جذر المستودع)
`usage-policy.html`، `privacy-policy.html`، `important-notice.html`،
`suggest-ideas.html`، `ads-sponsorships.html` — هيدر/فوتر أساسي بنفس
هوية الموقع البصرية فقط، بدون أي محتوى فعلي (`سيتم إضافة المحتوى هنا
قريباً`) — يُكتَب المحتوى الحقيقي لاحقاً مباشرة من GitHub.

### لم يتغيّر
- صفر تعديل على أي ملف `js/agp-*.js`، `games/roulette/agp-roulette.js`،
  أو ترتيب تحميل السكربتات — نفس الترتيب بالضبط.
- صفر تعديل على منطق قفل الألعاب (`showLockMessage`/`canPlayGames`)
  أو منطق زر الدخول/الملف الشخصي بالهيدر أو نافذة إعلان الأدمن.
- بطاقتا "مين الامبوستر" و"مافيا" — روابطهما ووصفهما وتصميمهما بالكامل
  كما كانت، بدون أي تعديل.

## [0.37.2] — بحث بجدول مستخدمي الأدمن + زر تسجيل خروج بصفحة البروفايل

### السياق
بعد تجربة لوحة الأدمن فعلياً، ظهرت حاجتان عمليتان واضحتان:
1. البحث بجدول المستخدمين كان يعتمد كلياً على التمرير اليدوي — غير عملي
   إطلاقاً مع مئات/آلاف المستخدمين مستقبلاً، خصوصاً إن أراد الأدمن منح
   إطار/دخولية لمستخدم معيّن (يحتاج معرفة "المعرّف الداخلي" الرقمي أولاً،
   وهو مختلف عن "الـID العام" الظاهر بالرابط).
2. صفحة `profile.html` (لما تُفتح كصاحب الحساب) ما فيها زر تسجيل خروج —
   لا طريقة يرجع فيها المستخدم لـ`login.html` عشان يدخل بحساب مختلف إلا
   بمسح الـ Token يدوياً من المتصفح.

### أُضيف — admin.html
- خانة بحث فوق جدول المستخدمين مباشرة (`#user-search`)، تفلتر فورياً
  (بدون أي طلب شبكي جديد — القائمة كاملة محمَّلة أصلاً بذاكرة محلية
  `allUsersCache`) على: المعرّف الداخلي (تطابق تام)، اسم المستخدم،
  الإيميل، الـID العام، ويوزرنيم تيك توك (الأربعة الأخيرة تطابق جزئي).
  البحث يبقى مطبَّقاً حتى لو ضُغط "تحديث" أو تغيّرت بيانات مستخدم (مثل
  حفظ ID عام جديد).
- إصلاح صغير جانبي: `colspan` بحالة "جارٍ التحميل…" كان لسا `9` من قبل
  توسعة الجدول لـ11 عموداً بـ[0.37.0] — صُحِّح لـ`11`.

### أُضيف — profile.html
- زر "تسجيل خروج" (`#pp-logout-btn`) بأعلى بطاقة البروفايل — يظهر فقط
  لصاحب الحساب نفسه وهو يشوف بروفايله (`isOwner`، نفس الفحص المستخدم
  أصلاً لإظهار زر "تفعيل" بالمقتنيات)، يستدعي `window.AGPAuth.logout()`
  ثم يحوّل لـ`login.html` — بالضبط نفس النمط المستخدم أصلاً بـ
  `admin.html`/`dashboard-core` (`btn-account-logout`)، بدون أي منطق
  جديد بـ`auth-client.js`.

### لم يتغيّر
- لا تعديل على أي مسار HTTP أو منطق سيرفر — كل ما سبق واجهة أمامية بحتة
  فوق بيانات/دوال موجودة أصلاً.

## [0.37.1] — إصلاح: لوحة الأدمن تفشل بتحميل قائمة المستخدمين (خطأ SQL بجداول المقتنيات)

### السياق
بعد نشر [0.37.0] وربط `announcement-service.js` المفقود، ظهر خطأ جديد عند
فتح `admin.html`: قسم الإعلان يعمل، لكن جدول المستخدمين يبقى عالقاً على
"جارٍ التحميل…" مع بانر أحمر "تعذّر تحميل قائمة المستخدمين" — أي أن
`GET /api/admin/users` كان يفشل بالسيرفر (500).

### السبب الجذري
3 استعلامات SQL (بـ`backend/collectibles/collectibles-service.js` و
`backend/points/points-service.js`) قارنت عمود `kind`/`frame_type` بقيمة
نصية بين علامتي اقتباس **مزدوجتين** (`kind = "level"`, `frame_type =
"catalog"`) بدل مفردتين. بمعيار SQL القياسي (ونسخة SQLite المستخدمة هنا)،
الاقتباس المزدوج يعني "اسم عمود"، لا نصاً حرفياً — وبما إنه لا يوجد عمود
اسمه `level` أو `catalog` بجدول `frame_catalog`/`user_frames`، فشل SQLite
بخطأ `no such column: "level"` عند أي استدعاء لـ:
- `collectiblesService.getCatalog()`
- `collectiblesService.autoGrantOnLevelUp()`
- `pointsService.getUserPoints()`

ودالة `listAllUsersWithStats()` (المستدعاة من `GET /api/admin/users`)
تستدعي `pointsService.getUserPoints()` لكل مستخدم بحلقة — فأي استثناء
بداخلها يُسقِط الطلب كاملاً بخطأ 500 (يُمسَك بمعالج الأخطاء العام
بـ`auth-router.js` ويرجَّع `{success:false, error:'internal_error'}`، وهذا
بالضبط ما تعرضه `admin.html` كـ"تعذّر تحميل قائمة المستخدمين").

**ملاحظة صادقة**: هذا خطأ برمجي وقع أثناء كتابة [0.37.0] نفسه (لم يُكتشَف
وقتها لأن فحص `node --check` يتحقق فقط من صحة صياغة JavaScript، لا من
صحة نصوص SQL المضمَّنة كسلاسل نصية داخله) — لا علاقة له بمشكلة
`announcement-service.js` المفقود التي أُصلِحت قبله.

### أُصلِح
- 3 مواضع بالضبط، تغيير الاقتباس المزدوج لمفرد حول القيم النصية `level`/
  `catalog`:
  - `collectiblesService.getCatalog()` — `ORDER BY (kind = 'level')`
  - `collectiblesService.autoGrantOnLevelUp()` — شرطا `WHERE`
  - `pointsService.getUserPoints()` — شرط `WHERE`
- **تحقق فعلي قبل التسليم**: نُفِّذ الكود الحقيقي (`database.js` +
  `auth-service.js` + `collectiblesService` + `pointsService`) ضد قاعدة
  SQLite تجريبية فعلية (وليس فقط فحص صياغة) — أُعيد إنتاج الخطأ حرفياً
  أولاً للتأكد من التشخيص، ثم تأكيد اختفائه تماماً بعد الإصلاح مع تمرين
  التدفق الكامل: منح إطار founder + تحقق حزمة الدخولية التلقائية + منح
  نقاط + فتح تلقائي لإطار مستوى + `listAllUsersWithStats()` لمستخدمين
  اثنين معاً، كل ذلك بنجاح تام.

### لم يتغيّر
- لا تعديل على أي هيكل جدول (نفس المخطط بالضبط)، ولا على أي مسار HTTP،
  ولا على أي ملف واجهة (`admin.html`/`profile.html`) — الإصلاح داخل
  منطق الاستعلامات فقط بملفين اثنين.

## [0.37.0] — نظام المقتنيات (إطارات + دخوليات) والنقاط/المستويات

### السياق
بعد نقاش تفصيلي طويل مع صاحب المشروع (نقطة بنقطة، قبل أي تنفيذ)، اتُّفق
على نظام "مقتنيات" يمنح اللاعبين/الستريمرز إطارات خاصة حول صورهم +
دخولية (أنيميشن + نص) تظهر عند انضمامهم، مبني على طبقتين منفصلتين
تماماً:
- **الطبقة 1 (هذا الإصدار)**: كل البيانات + منطق المنح/الفتح التلقائي +
  لوحة إدارة الأدمن + صفحة تفعيل المستخدم — **مبنية بالكامل هنا** داخل
  Project-Testing، لا تحتاج أي وصول لمستودعات الألعاب المنفصلة.
- **الطبقة 2 (خارج هذا الإصدار تماماً)**: الرسم البصري الفعلي للإطار/
  الدخولية **داخل** لوبي أي لعبة (الروليت وغيرها) — كل لعبة مستضافة في
  مستودع GitHub منفصل تماماً عن هذا المستودع، فهذا خارج نطاق هذا العمل
  كلياً. راجع قسم "التكامل مع الألعاب (الطبقة 2)" أدناه.

### أُضيف — قاعدة البيانات (`backend/db/database.js`)
- 5 جداول جديدة بالكامل (إضافة فقط، لا تعديل على أي جدول موجود):
  `frame_catalog` (كتالوج ثابت: 4 إطارات "خاصة" — مؤسس/ستريمر/داعم/عضو
  مميز — + 7 إطارات "مستوى")، `custom_frames` (إطارات حصرية حرة يرفعها
  الأدمن لاحقاً)، `user_frames` (ملكية/تفعيل)، `user_entrances` (دخولية
  نشطة لكل مستخدم)، `user_points` (نقاط إجمالية + سقف يومي).
- تهيئة أولية تلقائية (`INSERT OR IGNORE`) لصفوف الكتالوج الـ11 عند أول
  تشغيل — أسماء الملفات: `frame-founder.png`, `frame-streamer.png`,
  `frame-supporter.png`, `frame-distinguished.png`,
  `frame-level-1.png` … `frame-level-7.png` (يرفعها صاحب المشروع يدوياً
  لجذر المستودع بنفس أسلوب `logo.png`).

### أُضيف — منطق (ملفان جديدان، مجلدان جديدان)
- **`backend/collectibles/collectibles-service.js`**: منح/سحب/تفعيل
  إطارات، تعديل بيانات الكتالوج، إنشاء إطارات حصرية، تعيين/إزالة
  الدخولية، وفتح تلقائي لإطارات المستوى عند بلوغ عتبة النقاط
  (`autoGrantOnLevelUp`). قاعدة "الحزمة التلقائية": منح أي إطار من
  الأربعة "الخاصة" يمنح دخولية + توهج تلقائياً معه (افتراضياً من
  `frame_catalog`، قابل للتخصيص وقت المنح). صلاحية تجاوز الأدمن: منح أي
  إطار — حتى المقفول بمستوى — لأي مستخدم لا يتحقق أبداً من شرط النقاط
  الفعلي.
- **`backend/points/points-service.js`**: قواعد النقاط ثابتة بالكود
  (حُدِّدت صراحة من صاحب المشروع): +4 لكل جولة مكتملة، +20 إضافية لو
  فاز، +4 لكل ساعة لعب فعلي (مجموع مدة الجولة)، **سقف يومي 100 نقطة**
  (يُعاد ضبطه تلقائياً عند تغيّر التاريخ). عتبات المستويات السبعة نفسها
  **ليست** ثابتة بالكود — الأدمن يحددها يدوياً من لوحة التحكم لكل مستوى
  على حدة (تبدأ `NULL`/غير مفعّلة لحد ما يحددها).

### أُضيف — `backend/auth/auth-service.js`
- `findVerifiedUserByTikTok(tiktokUsername)`: مطابقة مشارك بجولة (معروف
  فقط بيوزرنيم تيك توك) بحساب مسجَّل موثَّق تيك توك فعلياً — أساس ربط
  النقاط بحساب حقيقي (لا نقاط لمن لا حساب له أو لم يوثّق تيك توك).
- `getPublicProfile` و`listAllUsersWithStats`: أُرفِق لهما حقول
  `points`/`frames`/`entrance` (الأولى) و`points`/`framesCount`/
  `equippedFrame` (الثانية) — نفس بوابة الخصوصية الموجودة أصلاً
  (صاحب الحساب أو الأدمن فقط لبروفايل كامل؛ جدول الأدمن كله محمي أصلاً).

### أُضيف — `backend/http/auth-router.js` (8 مسارات جديدة)
`GET/POST /api/admin/collectibles/catalog`,
`POST /api/admin/collectibles/custom-frame`,
`POST /api/admin/collectibles/grant`, `POST /api/admin/collectibles/revoke`,
`POST /api/admin/entrance` (أدمن فقط)، `POST /api/collectibles/equip`
(صاحب الجلسة لنفسه فقط)، `POST /api/points/round-complete` (يُستدعى من
`dashboard-core`). **منح تلقائي مرتبط**: تفعيل صلاحية `can_run_games`
لمستخدم عبر `POST /api/admin/permissions` الموجود أصلاً يمنحه الآن
تلقائياً إطار `streamer` (+ حزمته الكاملة) — تعطيل الصلاحية لاحقاً لا
يسحبه تلقائياً (قرار: الإطار إنجاز مكتسب، سحبه فعل يدوي صريح).

### أُضيف — `auth/auth-client.js`
10 دوال جديدة تطابق المسارات أعلاه 1:1: `adminGetCollectiblesCatalog`,
`adminUpdateCatalogEntry`, `adminCreateCustomFrame`, `adminGrantFrame`,
`adminRevokeFrame`, `adminSetEntrance`, `adminClearEntrance`,
`equipFrame`, `reportRoundCompletion`.

### أُضيف — `admin.html`
4 أقسام جديدة تحت جدول المستخدمين: **كتالوج الإطارات** (تعديل اسم عرض/
عتبة نقاط/دخولية افتراضية لكل صف من الـ11)، **إطارات حصرية** (إنشاء
إطار جديد باسم ملف حر)، **منح / سحب مقتنى** (لأي مستخدم بمعرّفه الرقمي،
مع تجاوز شرط المستوى)، **دخولية مستقلة** (بدون إطار). جدول المستخدمين
الأصلي أُضيف له عمودان: "النقاط / المستوى" و"الإطار الحالي".

### أُضيف — `profile.html`
تبويب "المقتنيات" (كان "قريباً") أصبح فعلياً: شبكة بطاقات لكل إطار
مملوك (صورة من جذر المستودع بنفس اسم ملفها + زر "تفعيل" لصاحب الحساب
فقط)، وبطاقة الدخولية النشطة. صندوق "مستوى اللاعب" (كان "قريباً") أصبح
شريط تقدّم حقيقي بالنقاط الفعلية والمسافة للمستوى التالي.

### أُضيف — `dashboard-core/js/dashboard-core.js`
`NS.components.round.start()`/`end()`: تسجيل توقيت بداية الجولة، وعند
الإنهاء إرسال قائمة اللاعبين الحاليين (`AGP.gameManager.getPlayers()`)
+ الفائز الأخير + مدة الجولة لـ `/api/points/round-complete` (فشل صامت
لا يعطّل تدفّق إنهاء الجولة العادي). **⚠️ افتراض غير مؤكَّد صراحة**:
الكود يفترض أن `player.name` (أو `player.id`) هو نفسه يوزرنيم تيك توك
الحقيقي للمشارك — لم يُختبَر ضد تدفّق انضمام حقيقي من هذه البيئة (لا
اتصال فعلي بتيك توك هنا، تماماً كسابقاتها من الملاحظات الصادقة بالمشروع).

### التكامل مع الألعاب (الطبقة 2) — لم يُبنَ هنا، بقرار صريح
لعبة الروليت (وأي لعبة أخرى) مستضافة بمستودع GitHub منفصل تماماً عن
هذا المستودع؛ صاحب المشروع اختار يفتح جلسة عمل منفصلة مرتبطة مباشرة
بريبو الروليت لبناء التكامل البصري الفعلي هناك (بدل هذه الجلسة). ما
يحتاجه ذلك العمل من هنا:
- `GET /api/announcement`-مثيل لكل مستخدم: لا يوجد endpoint عام لجلب
  إطار/دخولية مستخدم بيوزرنيم تيك توك مباشرة بعد (كل ما بُني هنا يفترض
  إما الأدمن مسجّل دخوله أو صاحب الحساب نفسه). أول خطوة فعلية بجلسة
  الروليت على الأغلب إضافة مسار عام جديد بسيط، مثل
  `GET /api/collectibles/by-tiktok?username=...`، يرجع فقط الإطار
  المفعَّل حالياً + الدخولية (بدون أي بيانات حساب حساسة) — هذا **لم
  يُبنَ بعد عمداً**، حتى يُقرَّر شكله بدقة وقت الحاجة الفعلية له.
- أسماء الملفات الثابتة للإطارات الأربعة عشر (4 خاصة + 7 مستوى + أي
  حصرية) موثَّقة أعلاه، ونماذج الدخولية الأربعة (`gold`/`neon`/`fire`/
  `ice`) بتصميمهم الكامل (أنيميشن CSS + تسلسل "دخول ثم استقرار كإطار
  دائم") جاهزة كملف HTML تجريبي مستقل سُلِّم لصاحب المشروع مسبقاً في
  هذه المحادثة (خارج المستودع، للمعاينة والتصميم فقط).

### لم يتغيّر
- لا تعديل على أي جدول/عمود/مسار API سابق. لا تعديل على منطق الألعاب
  أو `AGP.*` أو `index.html` أو ترتيب تحميل ملفاتها.
- هذا التحديث يلمس الباك إند (5 جداول جديدة + 8 مسارات جديدة) — يعني
  Redeploy لخدمة **Project-Testing** يسبب توقفاً قصيراً (ثوانٍ) بسبب
  القرص الدائم المُرفَق (راجع [0.34.1])، تماماً كأي تحديث باك إند سابق.

---

## [0.36.0] — إعلان الأدمن للزوار (نافذة منبثقة بالصفحة الرئيسية)

### السياق
طلب صاحب المشروع إمكانية نشر تنبيه/إعلان (نص + صورة اختيارية) من لوحة
الأدمن، يظهر لكل زائر يفتح الصفحة الرئيسية في كل مرة يدخل فيها، وقابل
للإزالة يدوياً في أي وقت.

### أُضيف
- **`backend/db/database.js`**: جدول جديد بالكامل `announcement` (صف
  واحد ثابت id=1) — إضافة إنشاء جدول فقط، لا تعديل على أي جدول موجود.
- **`backend/announcements/announcement-service.js`** (ملف جديد، مجلد
  جديد): منطق بحت — `getActiveAnnouncement`, `setAnnouncement`,
  `clearAnnouncement`. بنفس فلسفة `auth-service.js` (مسؤولية واحدة، لا
  معالجة HTTP هنا).
- **`backend/http/auth-router.js`**: مساران جديدان فقط —
  `GET /api/announcement` (عام، بدون تسجيل دخول، تستدعيه الصفحة
  الرئيسية) و`POST /api/admin/announcement` (أدمن فقط — نشر/تحديث أو
  إزالة عبر `active: false`). لا تعديل على أي مسار موجود.
- **`auth/auth-client.js`**: 3 دوال جديدة —
  `getAnnouncement`, `adminSetAnnouncement`, `adminClearAnnouncement`.
- **`admin.html`**: قسم جديد "الإعلان للزوار" فوق جدول المستخدمين —
  حقل نص، حقل اسم ملف صورة اختياري، زرّا "نشر/تحديث" و"إزالة الإعلان
  الحالي".
- **`index.html`**: نافذة منبثقة عامة (مخفية افتراضياً)، تُملأ وتُظهَر
  فقط لو رجع إعلان نشط فعلياً من الخادم عند تحميل الصفحة. زر إغلاق
  يخفيها للزيارة الحالية فقط (تظهر من جديد بالزيارة التالية طالما
  الأدمن ما أزالها). فشل الشبكة = لا شيء يظهر، بصمت تام.

### كيف تُضاف الصورة
لا نظام رفع صور فعلي (تفادياً لتعقيد غير ضروري) — بنفس أسلوب
`logo.png`/`hero-banner.png` الحالي بالضبط: ترفع الصورة بنفسك على
GitHub بجذر المستودع، وتكتب نفس اسم الملف بالضبط بحقل "اسم ملف الصورة"
بلوحة الأدمن.

### لم يتغيّر
- لا تعديل على أي جدول/عمود موجود، ولا على أي مسار API سابق، ولا على
  منطق الألعاب أو `AGP.*` أو ترتيب تحميل ملفاتها. الإعلان يظهر بالصفحة
  الرئيسية فقط (حسب الاختيار)، ولا يظهر بأي صفحة أخرى.
- هذا التحديث يلمس الباك إند (جدول جديد + مساران جديدان) — يعني
  Redeploy لخدمة **Project-Testing** يسبب توقفاً قصيراً جداً (ثوانٍ)
  بسبب القرص الدائم المُرفَق (راجع [0.34.1])، تماماً كما تم توضيحه.

---

## [0.35.1] — إصلاح: سطر تأكيد القرص الدائم بالـ Logs كان مخفياً بالإنتاج

### السياق
أثناء التأكد الفعلي من نجاح إعداد القرص الدائم على Render (بعد ترقية
الخدمة لـ Starter وربط قرص بـ Mount Path `/var/data`)، لم يظهر سطر
`Database: using persistent Render disk` إطلاقاً بالـ Logs — رغم إن
القرص كان مربوطاً صح فعلياً.

### السبب
`logger.log()` (المستخدَمة لطباعة ذلك السطر في `database.js`) مبنية
عمداً على إنها **تُكتَم بالكامل** لو `config.debug === false` — وهذا
بالضبط وضع أي بيئة إنتاج، لأن `config.debug` نفسه مبني على
`process.env.NODE_ENV !== 'production'`، وRender يضبط
`NODE_ENV=production` تلقائياً لكل خدمة Node. يعني السطر كان **يُطبَع
فعلياً بالكود، لكن يُكتَم قبل ما يوصل للـ Logs** — سلوك مقصود لرسائل
التصحيح العادية، لكنه غير مناسب لمعلومة تشغيلية حرجة مثل "وين تُخزَّن
قاعدة البيانات فعلياً".

### أُصلِح
- **`backend/utils/logger.js`**: إضافة دالة جديدة `logger.info()` —
  تطبع دائماً (زي `logger.error()` بالضبط) بصرف النظر عن وضع Debug،
  لكن عبر `console.log` بدل `console.error` (لأنها مو خطأ فعلياً).
  `logger.log()` و`logger.error()` الأصليتان لم تتغيّرا إطلاقاً.
- **`backend/db/database.js`**: تغيير سطر واحد فقط من `logger.log(...)`
  إلى `logger.info(...)` لنفس رسالة مسار قاعدة البيانات — الآن تظهر
  دائماً بالـ Logs بأي بيئة (تطوير محلي أو إنتاج على Render).

### لم يتغيّر
- لا تعديل على منطق قاعدة البيانات أو مسارها نفسه — فقط ظهور رسالة
  التأكيد بالـ Logs. أي استخدامات أخرى لـ `logger.log()` بباقي الملفات
  بقيت بنفس السلوك القديم (تُكتَم بالإنتاج) عمداً — هذا تغيير مستهدَف
  لسطر واحد حرج فقط، مو تغيير عام بسلوك التسجيل بكل المشروع.

---

## [0.35.0] — لوحة تحكم الستريمر (dashboard-core) بالكامل بالعربي

### السياق
لوحة تحكم الستريمر (`dashboard-core/index.html`) كانت بالإنجليزية
بالكامل منذ بنائها الأول (Phase 2) — نصوص واجهة ثابتة فقط، لا علاقة
بمنطق الألعاب. الأدمن طلب تعريبها بالكامل، مع توضيح صريح أن أقساماً
مثل "Registered Games"/"Game Selection"/"Game Settings" بدت له "غير
ضرورية" لعدم وضوح وظيفتها.

### أُضيف / أُصلِح (نصوص فقط — Text-only)
- **`dashboard-core/index.html`**: `lang="ar" dir="rtl"`، وترجمة كل
  نص واجهة ظاهر (العنوان، الشريط العلوي، التبويبات الخمسة، كل الألواح
  والحقول والأزرار والتلميحات في تبويبات اللعبة/اللاعبين/البث والغرفة/
  الجولة/سجل الأحداث، والنافذة المنبثقة العامة). لوحة إعدادات روليت
  القبائل (`#panel-roulette-settings`) كانت عربية أصلاً ولم تُلمَس.
- **`dashboard-core/js/dashboard-core.js`**: ترجمة كل نص يُبنى ديناميكياً
  من الكود (خطوات "المسار الموجَّه" (Workflow) وشرح كل خطوة، رسائل
  Toast لكل الأحداث، نصوص النافذة المنبثقة عند الحذف/عرض التفاصيل،
  حالات "لا يوجد فائز/لاعبون/ألعاب بعد"، حالة كلمة الانضمام، أزرار
  الاتصال/قطع الاتصال بمنصات البث).

### لم يتغيّر (Unchanged) — مهم
- **لا حذف لأي عنصر أو قسم**: "اختيار اللعبة"، "الألعاب المسجَّلة"،
  و"إعدادات اللعبة" (المسار الموجَّه من إنشاء غرفة → فتح تسجيل → إعدادات
  اختيارية → بدء/إنهاء جولة → إعادة ضبط) **هي دورة تشغيل اللعبة الفعلية
  نفسها**، موثَّقة كذلك في كود الملف نفسه — لا عناصر زخرفية زائدة. تمت
  ترجمتها فقط، ولم يُحذف أو يُعاد ترتيب أي منها؛ القرار النهائي بشأن أي
  جزء يُخفى فعلياً يحتاج تأكيداً صريحاً بعد التوضيح (راجع الرد المرافق).
- **لا تعديل على أي منطق**: لا اسم حدث، لا `data-attribute`، ولا صنف
  CSS (وبالتحديد `.btn-play` — تستخدمه أيضاً `index.html` لمنطق قفل
  الألعاب) تغيّر. قسم الفرق (`team-settings-section`) بقي مخفياً بنفس
  الطريقة (`display:none`) دون حذف، جاهزاً لأي لعبة جماعية مستقبلية.
  ترتيب تحميل ملفات `agp-*.js` لم يتغيّر إطلاقاً.
- معرّفات تقنية داخلية (مثل قيم `source` للاعبين: `lobby`/`queue`/…،
  ومفاتيح الأحداث في سجل الأحداث مثل `game:roundStarted`) بقيت كما هي
  عمداً — هذي معرّفات برمجية للتشخيص، وليست نصوص عرض للمستخدم النهائي.

---

## [0.34.2] — إصلاح: جلسة محلية قديمة تبقى عالقة بعد تصفير قاعدة البيانات

### السياق
بعد إعداد القرص الدائم ([0.34.1]) وتصفير قاعدة البيانات، فتح المستخدم
الصفحة الرئيسية من متصفح كان فيه جلسة قديمة محفوظة (Token + بيانات
مستخدم) من **قبل** التصفير. زر "الملف الشخصي" ظهر (بيانات محلية قديمة
لسا موجودة)، لكن الضغط عليه أدّى لرسالة "هذا الحساب غير موجود" — لأن
الحساب اللي تشير له تلك الجلسة القديمة فعلياً لم يعد موجوداً بالخادم.

### السبب
`refreshUser()` المُضافة في [0.34.0] كانت تُحدِّث `cachedUser` فقط لو
نجح الطلب — لو فشل (Token غير صالح/حساب محذوف)، **لا شيء كان يحصل**:
الجلسة القديمة تبقى محفوظة محلياً كما هي، وزر "الملف الشخصي" يستمر
بالظهور ويودّي لرابط ميت.

### أُصلِح
- **`index.html`**: لو `refreshUser()` رجعت `null` (الجلسة غير صالحة
  فعلياً على الخادم)، الآن تُمسَح الجلسة المحلية تلقائياً
  (`AGPAuth.clearSession()`) وترجع الواجهة لحالة "غير مسجَّل دخول"
  الحقيقية — تظهر أزرار "تسجيل دخول"/"إنشاء حساب" من جديد بدل زر ملف
  شخصي معطوب. لا حاجة لأي إجراء يدوي (لا مسح Cache، لا تسجيل خروج
  يدوي) — يصلح نفسه تلقائياً بمجرد فتح الصفحة مرة ثانية.

### لم يتغيّر
- أي منطق آخر — هذا استكمال مباشر لإصلاح [0.34.0]، نفس الملف فقط.

---

## [0.34.1] — قاعدة البيانات على قرص Render الدائم (يحل مشكلة اختفاء الحسابات)

### السياق
تشخيص كامل لمشكلة "البيانات تنحذف كل شوي حتى حساب الأدمن": قاعدة
البيانات (`backend/agp-data.sqlite`) كانت مجرد ملف عادي داخل مجلد
المشروع نفسه على خادم Render. Render (موثَّق رسمياً من عندهم) يمسح أي
ملفات محلية في الخدمة **في كل مرة تُعاد فيها** — نشر جديد، أو حتى
استيقاظ الخدمة من الخمول على الخطط غير المدفوعة (بعد ١٥ دقيقة بدون
طلبات). هذا سبب المشكلة بدقة، وليس خللاً بمنطق `auth-service.js` أو
`database.js` — الكود كان صحيحاً، لكن مكان تخزين الملف نفسه غير آمن
على هذا النوع من الاستضافة. تم اختيار حل **قرص دائم (Persistent Disk)
+ خطة Render مدفوعة (Starter)** بعد عرض الخيارات على صاحب المشروع.

### أُصلِح
- **`backend/db/database.js`**: مسار قاعدة البيانات صار ديناميكياً —
  لو مسار `/var/data` موجود فعلياً (أي قرص Render دائم مُرفَق ومُوصَّل
  بهذا المسار بالضبط)، الملف يُخزَّن فيه تلقائياً؛ غير ذلك (تطوير محلي،
  أو الخدمة بدون قرص بعد) يرجع لنفس السلوك القديم بالضبط (بجانب مجلد
  `backend/`). سطر Log جديد عند إقلاع الخادم يوضّح أي المسارين استُخدم
  فعلياً — يظهر بسجلات Render (Logs) للتأكد.

### خطوات الإعداد المطلوبة على Render (يدوية، مرة واحدة فقط)
1. من لوحة Render → خدمة `Project-Testing` → **Settings** → ترقية
   الخطة (Instance Type) من Free إلى **Starter**.
2. من نفس صفحة الإعدادات → قسم **Disks** → **Add Disk**:
   - الاسم: أي اسم (مثلاً `agp-data`).
   - **Mount Path**: بالضبط `/var/data` (يجب أن يطابق الكود تماماً).
   - الحجم: 1GB كافٍ جداً لبيانات حسابات نصية (يمكن التوسعة لاحقاً).
3. حفظ + Manual Deploy جديد بعد رفع هذا الملف.
4. للتأكد: افتح تبويب **Logs** بعد إعادة التشغيل وابحث عن سطر
   `Database: using persistent Render disk` — لو ظهر بدلاً من
   `local (non-persistent) path`، القرص متصل صح والبيانات الآن دائمة.

### لم يتغيّر
- شكل الجداول أو أي استعلام SQL — فقط *مكان* تخزين نفس الملف. لا هجرة
  بيانات مطلوبة لأنه أصلاً ما فيه بيانات قديمة تستاهل الحفظ (كانت
  تُمسَح على أي حال) — الحساب الأول اللي يسجّل بعد هذا التحديث (على
  القرص الدائم) هو أول حساب يبقى فعلياً للأبد.

---

## [0.34.0] — الشعار الحقيقي بدل النص، إصلاح رسالة قفل الألعاب، مكان للبانر، لوحة الأدمن بالعربي

### السياق
أربع ملاحظات منفصلة بعد تجربة فعلية على حساب ستريمر موافَق عليه من
الأدمن:

1. **رسالة "حسابك يحتاج موافقة الأدمن" تظهر لحساب مُوافَق عليه فعلاً**:
   السبب الجذري — الصفحة الرئيسية تفحص صلاحية `can_run_games` من نسخة
   المستخدم **المخزَّنة محلياً في المتصفح (localStorage)** وقت آخر
   تسجيل دخول، وليس من الخادم مباشرة. لو الأدمن وافق على الحساب **بعد**
   ما صاحبه سجّل دخوله أصلاً، النسخة المحلية تبقى بالصلاحية القديمة
   (غير موافَق) لحد ما يسجّل خروج ودخول من جديد — رغم إن الخادم نفسه
   محدَّث وصحيح. هذا خلل حقيقي، مو مجرد سوء فهم بالتصميم.
2. **الشعار نص "ألعاب أيمن" + حرف A بدل صورة `logo.png`** رغم إن الملف
   مرفوع فعلاً بجذر المستودع من قبل — كل صفحات الواجهة كانت تستخدم شعاراً
   نصياً بديلاً (Placeholder) بدل قراءة الملف الفعلي.
3. طلب مكان مخصَّص لبانر جاهز يُضاف لاحقاً بأعلى الصفحة الرئيسية.
4. لوحة الأدمن (`admin.html`) كانت بالإنجليزية بالكامل (نصوص واجهة
   ثابتة كتبتها بالإنجليزية وقت بنائها الأول، لا علاقة بلغة البيانات).

### أُصلِح (Bug Fix)
- **`auth/auth-client.js`**: دالة جديدة `refreshUser()` — تجلب بيانات
  المستخدم الحالي من `/api/auth/me` وتُحدِّث بها الجلسة المخزَّنة محلياً
  (`localStorage`) مباشرة، بدون أي تحويل/تسجيل خروج عند الفشل (خلافاً
  لـ`requireAuth`).
- **`index.html`**: بعد تأكيد وجود جلسة، يُستدعى `refreshUser()` بصمت
  بالخلفية فور تحميل الصفحة، فيحدَّث `cachedUser` قبل ما يوصل المستخدم
  لزر "العب الآن" — فتنعكس أي موافقة أدمن حديثة فوراً بدون أي إجراء من
  المستخدم (لا تسجيل خروج/دخول، لا Refresh يدوي مطلوب فعلياً رغم إنه
  كان يحل المشكلة مؤقتاً).

### أُضيف / تغيّر (الشعار والبانر)
- **`index.html`, `login.html`, `signup.html`, `profile.html`**: الشعار
  النصي (حرف "A" + "ألعاب أيمن") استُبدل بـ`<img src="logo.png">`
  فعلي في كل موضع (الترويسة العلوية، وتذييل الصفحة الرئيسية). الرابط
  يعيد للصفحة الرئيسية دائماً.
- **`dashboard-core/index.html`** و**`admin.html`**: نفس الاستبدال —
  الشعار النصي "AGP" بجانب `.topbar-logo` استُبدل بصورة `logo.png`
  أيضاً، لتناسق الهوية البصرية بكامل المنصة كما طُلب. أُضيفت قاعدة CSS
  جديدة (`.topbar-logo--img`) في `dashboard-core/css/style.css` —
  إضافية فقط، لا تعديل على `.topbar-logo` الأصلية.
- **`index.html`**: مكان جديد للبانر أعلى قسم الـ`hero` — عنصر
  `<img src="hero-banner.png">` مع `onerror` يُخفيه تلقائياً لو الملف
  غير مرفوع بعد (لا أيقونة "صورة مكسورة" لحد ما يُرفَع). **اسم الملف
  المطلوب: `hero-banner.png`، يُرفَع بجذر المستودع بجانب `logo.png`
  مباشرة — بمجرد رفعه يظهر تلقائياً بدون أي تعديل كود إضافي.** المقاس
  الموصى به: عرض حول 1600px، نسبة عرض:ارتفاع تقريباً 3:1 إلى 4:1 (مثلاً
  1600×450px)، خلفية شفافة أو داكنة تناسب ألوان المنصة، حجم ملف أقل من
  ٥٠٠ كيلوبايت إذا أمكن لسرعة تحميل أفضل.

### أُضيف / تغيّر (لوحة الأدمن بالعربي)
- **`admin.html`**: كل نصوص الواجهة الثابتة تُرجِمت للعربية بالكامل —
  "Admin Panel"→"لوحة الأدمن"، "Dashboard"→"لوحة التحكم"،
  "Logout"→"تسجيل خروج"، "Users & Streamers"→"المستخدمون والستريمرز"،
  "Refresh"→"تحديث"، عناوين الجدول التسعة كاملة، "Loading…"→"جارٍ
  التحميل…"، "No users yet."→"لا يوجد مستخدمون بعد."، "Save"→"حفظ"،
  "View"→"عرض". **لا تعديل على أي منطق أو استدعاء API** — ترجمة نصوص
  عرض فقط، أسماء الحقول والمفاتيح البرمجية (`can_run_games`, إلخ) لم
  تتغيّر لأنها داخلية وليست ظاهرة للمستخدم.

### لم يتغيّر
- منطق تسجيل الدخول/الخروج، الصلاحيات، تيك توك، الألعاب، وقاعدة
  البيانات — لا شيء منها تأثر بهذه المرحلة.
- شكل ومحتوى صفحة الأدمن (الجدول، الأعمدة، الأزرار) — نفس الوظائف
  بالضبط، فقط باللغة العربية.

### لم يُنفَّذ بعد (قيد النقاش مع صاحب المشروع)
- **فقدان بيانات قاعدة البيانات بشكل متكرر (حتى حساب الأدمن)**: تم
  تشخيص السبب الجذري بدقة — راجع الرسالة التالية بالمحادثة، يحتاج قراراً
  من صاحب المشروع (تكلفة/خطة استضافة) قبل التنفيذ.
- **قدرة الأدمن على منح مقتنيات (شارات/إطارات/دخوليات/ألوان أسماء)
  لأي مستخدم**: يحتاج تحديد نطاق أول (هل تُبنى الآن كإطار عام فارغ
  بانتظار نظام المقتنيات، أو تُؤجَّل لحد ما تُحدَّد المقتنيات نفسها؟) —
  راجع الرسالة التالية.

---

## [0.33.1] — إصلاح: زر "الملف الشخصي" ما كان يظهر بالصفحة الرئيسية بعد الدخول

### السياق
بعد تسجيل الدخول، أزرار "تسجيل دخول/إنشاء حساب" كانت تختفي صح من
الصفحة الرئيسية (`index.html`) — لكن زر "الملف الشخصي" المفروض يظهر
مكانها **ما كان يظهر إطلاقاً**، فما فيه طريقة ترجع بها لبروفايلك من
الصفحة الرئيسية مباشرة.

### السبب
خطأ برمجي بسيط بـ`index.html`: `#profile-button { display: none; }`
قاعدة CSS بمعرّف (ID) بأعلى الملف تُخفي الزر افتراضياً. الكود كان
يحاول إظهاره بعد تسجيل الدخول عبر `profileButton.style.display = '';`
— لكن **تفريغ الخاصية المضمّنة (inline style) لا يُلغي قاعدة الـCSS**،
بل يرجّع التطبيق لنفس قاعدة `#profile-button` (لأن معرّف الـID له أولوية
أعلى من التصميم الافتراضي للزر)، فيبقى الزر مخفياً فعلياً رغم أن الكود
"حاول" إظهاره.

### أُصلِح
- **`index.html`**: تغيير `profileButton.style.display = '';` إلى
  `profileButton.style.display = 'inline-block';` (قيمة صريحة تغلب
  فعلياً على قاعدة الإخفاء). سطر واحد فقط، لا تغيير آخر على الصفحة أو
  منطقها.

### لم يتغيّر
- كل شيء آخر بالصفحة الرئيسية (الألعاب، قفل "العب الآن"، النافبار،
  التصميم) — لا علاقة له بهذا الإصلاح إطلاقاً.

---

## [0.33.0] — تصميم جديد لصفحتي الدخول/إنشاء الحساب + إلغاء ربط تيك توك يدوياً + تصحيح شرط "ستريمر"

### السياق
استكمالاً لـ [0.32.0] (تصميم البروفايل، المعتمد كما هو دون أي تعديل)،
طُلب تطبيق نفس الهوية البصرية على `login.html` و`signup.html` بناءً
على تصميم مرجعي مُرسَل، بالإضافة لثلاث ملاحظات دقيقة:

1. **"ستريمر" يعني موافقة الأدمن فعلياً، لا مجرد اختيار وقت التسجيل**:
   `profile.html` ([0.32.0]) كانت تُظهر تبويب "معلومات الستريمر" بناءً
   على `is_streamer` (طلب المستخدم وقت التسجيل) بدل `can_run_games`
   (موافقة الأدمن الفعلية من `admin.html`) — تصحيح مباشر، بدون أي
   صلاحية أو عمود جديد (نفس النظام الموجود أصلاً من [0.29.0]/[0.31.0]).
2. **تيك توك يطلب تحقق كل مرة**: كانت `profile.html` تعرض نموذج
   الربط/التحقق دائماً بغض النظر عن حالة `tiktok_verified` الفعلية —
   لم يكن هناك أي كسر فعلي بالـBackend (`tiktok_verified` كان يبقى
   `1` للأبد أصلاً)، المشكلة كانت فقط أن الواجهة لا تفرّق بين "متصل"
   و"غير متصل". تمت إضافة **إلغاء ربط يدوي صريح** (لم يكن موجوداً
   إطلاقاً) ليتحكم المستخدم فعلياً متى ينتهي الربط.
3. تطبيق تصميم `login.html`/`signup.html` الجديد بأقل تغيير ممكن على
   المنطق، مع رفض تنفيذ عنصرين من التصميم المرجعي لا يقابلهما أي وظيفة
   حقيقية حالياً (تفصيل أدناه) بدل التظاهر بأنهما يعملان.

### أُضيف / تغيّر (Backend)
- **`backend/auth/auth-service.js`**: دالة جديدة `unlinkTikTok(userId)`
  — تصفّر `tiktok_username`/`tiktok_verified`/`tiktok_verification_code`
  لحساب واحد. لا تُستدعى تلقائياً من أي مكان آخر في الملف — فقط عبر
  طلب صريح من المستخدم. `verifyTikTokOwnership` و`generateVerificationCode`
  لم يتغيّرا إطلاقاً (لا إعادة تحقق دورية كانت موجودة أصلاً ولا أُضيفت).
- **`backend/http/auth-router.js`**: مسار جديد
  `POST /api/auth/tiktok/unlink` (يتطلب تسجيل دخول) → `unlinkTikTok`.

### أُضيف / تغيّر (Frontend)
- **`auth/auth-client.js`**: دالة جديدة `unlinkTikTok()` تطابق المسار أعلاه.
- **`profile.html`**:
  - تصحيح: تبويب "معلومات الستريمر" وشارة "ستريمر" يعتمدان الآن على
    `can_run_games` (موافقة الأدمن) بدل `is_streamer` (طلب وقت
    التسجيل). حساب طلب "استريمر" لكن الأدمن لم يوافق بعد يظهر له شارة
    "بانتظار موافقة الأدمن كستريمر" بدل شارة "ستريمر" المضلِّلة سابقاً.
  - قسم تيك توك أعيد بناؤه لثلاث حالات واضحة: **متصل** (بطاقة تعرض
    اليوزرنيم + شارة "متصل ✅" + زر "إلغاء الربط" فقط)، **غير متصل**
    (حالة واضحة + زر "ربط حساب TikTok" يفتح نفس نموذج الربط/التحقق
    الأصلي)، و**قراءة فقط** (أدمن يشاهد حساباً آخر). زر "إلغاء الربط"
    يستدعي `unlinkTikTok()` الجديدة ثم يحدّث الصفحة.
  - **ملاحظة صادقة**: لا صورة أو اسم عرض (Display Name) حقيقيين لحساب
    تيك توك — لا يوجد أي منهما في قاعدة البيانات أصلاً (العمود الوحيد
    هو `tiktok_username`)، فتُعرَض أيقونة عامة بدل صورة حقيقية بدل
    اختراع مصدر بيانات جديد أو استدعاء شبكي إضافي لجلبها.
- **`login.html` و`signup.html`**: إعادة بناء كاملة بنفس الهوية البصرية
  المستخدمة في `profile.html` (تصميم ذاتي الاكتفاء، لا اعتماد على
  `dashboard-core/css/style.css` أو `auth/auth.css`). التدفق البرمجي
  (`AGPAuth.signup/login/loginWithGoogle`، تسجيل الدخول التلقائي بعد
  إنشاء الحساب، تحويل ما بعد الدخول حسب `canAccessDashboard`) لم يتغيّر
  حرفياً بأي سطر منطق.
  - `signup.html`: نوع الحساب صار بطاقتين "لاعب"/"استريمر" بدل Checkbox
    (الافتراضي الآن "لاعب" بدل "استريمر" تماشياً مع التصميم)، لكن
    القيمة المُرسَلة لـ`AGPAuth.signup()` تبقى نفس `wantsToBeStreamer`
    بالضبط — طلب أولي فقط، **لا يمنح** صلاحية ستريمر فعلية (نفس قاعدة
    [0.31.0])؛ ملاحظة توضيحية تظهر تلقائياً عند اختيار "استريمر" تؤكد
    ذلك للمستخدم مباشرة. أُضيف حقل "تأكيد كلمة المرور" — تحقق واجهة
    فقط (لا يُرسَل للخادم، الخادم يستقبل كلمة مرور واحدة كما كان دائماً).
  - `login.html`: **لم يُنفَّذ حرفياً** جزءان من التصميم المرجعي لعدم
    وجود وظيفة حقيقية خلفهما حالياً (تفصيل كامل أدناه بقسم "لم يُنفَّذ").

### لم يتغيّر
- كل منطق `AGPAuth.*` (توقيعات الدوال، `request()`، تخزين الجلسة) —
  فقط دالتان جديدتان أُضيفتا (`unlinkTikTok`) دون لمس أي دالة موجودة.
- تسجيل الدخول بجوجل (Google Identity Services) — نفس الإعداد بالضبط،
  فقط غلاف بصري جديد حول نفس الزر.
- منطق ربط/توليد كود/التحقق من تيك توك نفسه (الاستدعاءات الثلاثة
  الأصلية) — لم تُعدَّل بحرف واحد، فقط صار يُتحكَّم بعرضها حسب الحالة.
- زر "الملف الشخصي" في `index.html` (أُضيف أصلاً في [0.31.0]) — راجعناه
  ووجدناه يحقق طلب "زر واضح للوصول للبروفايل من الرئيسية" بالكامل
  (ديناميكي، يوصل لبروفايل المستخدم الصحيح)، فلم يحتج أي تعديل إضافي.
- `admin.html`, لوحة الستريمر, نظام الألعاب, قاعدة البيانات (لا عمود
  جديد — فقط دالتان جديدتان تستخدمان أعمدة تيك توك الموجودة أصلاً من
  [0.29.0]) — لا شيء منها تأثر.

### لم يُنفَّذ (وليش)
- **"البريد الإلكتروني أو اسم المستخدم" بحقل الدخول**: التصميم المرجعي
  يعرض حقلاً موحَّداً، لكن `auth-service.login()` يقبل البريد
  الإلكتروني فقط (`WHERE email = ?`) — لا دعم دخول باسم المستخدم في
  الخادم. حقل الدخول بقي "البريد الإلكتروني" فقط تفادياً لعرض خيار لا
  يعمل فعلياً؛ توسعة الخادم لدعم ذلك لم تُطلَب صراحة فتُركت.
- **"نسيت كلمة المرور؟"**: لا نظام استرجاع كلمة مرور (لا خدمة بريد
  مُعدَّة بالخادم). الرابط موجود بصرياً كما بالتصميم، لكن يعرض ملاحظة
  "قريباً" صريحة بدل التظاهر بإرسال بريد استرجاع فعلي.
- **خانة "تذكرني"**: عنصر واجهة بصري بحت غير موصول بأي منطق — الجلسة
  أصلاً تُحفَظ محلياً ٣٠ يوماً بغض النظر عن هذه الخانة (`SESSION_
  DURATION_MS` في `auth-service.js`، لم يتغيّر)، فلا يوجد فرق فعلي
  لتوصيله به.
- صورة/اسم عرض حقيقيين لحساب تيك توك بالبروفايل — غير مخزَّنين بقاعدة
  البيانات أصلاً (راجع تفصيل أعلى)، تُعرَض أيقونة عامة بدلاً منها.
- مراجعة التنسيق المتجاوب لبقية صفحات المنصة (dashboard-core،
  admin.html) — لا تزال خارج نطاق هذه المرحلة.

---

## [0.32.0] — إعادة تصميم صفحة البروفايل + خصوصية كاملة (كل حساب يشوف بروفايله فقط)

### السياق
بعد [0.30.0] كان أي شخص (بدون تسجيل دخول حتى) يقدر يفتح بروفايل أي
مستخدم آخر عبر `profile.html?id=<custom_id>` ويشوف كل بياناته العامة.
تحدَّد مع صاحب المشروع أن هذا غير مقبول: **لا أحد يفتح بروفايل غيره**؛
كل حساب يشوف بروفايله الكامل هو فقط، والأدمن يقدر يشوف أي بروفايل
(للإدارة). المعلومة العامة الوحيدة لأي زائر آخر تبقى فقط اسم المستخدم
والـID العام (تجهيزاً لأي تصنيف/ترتيب لاعبين مستقبلي)، بالإضافة
لمستوى اللاعب (يُعرض حالياً كـ"قريباً" لعدم وجود نظام مستويات فعلي
بعد). بالتوازي، أُعيد تصميم شكل الصفحة بالكامل (ألوان/خطوط/تبويبات)
بناءً على تصميم مرجعي أرسله صاحب المشروع، مع الإبقاء الكامل على كل
وظيفة موجودة (تسجيل الدخول، ربط/تحقق تيك توك، الـID العام، إحصائيات
البث الحقيقية).

### أُضيف / تغيّر (Backend)
- **`backend/http/auth-router.js`**:
  - `handle()`: صار يحاول قراءة هوية المُرسِل (`requireUser`) على
    **كل** طلب، حتى المسارات العامة (`requireAuth: false`) — لا تغيير
    في سلوك الرفض (401/403) للمسارات المحمية، فقط المسارات العامة
    صارت "تعرف" هوية المرسِل لو أرفق Token صالحاً، دون أي إلزام بذلك.
  - `handlePublicProfile()`: أعيدت كتابتها بالكامل لتطبيق الخصوصية —
    لو الطالب هو صاحب الحساب نفسه أو أدمن، يرجع البروفايل الكامل؛ غير
    ذلك يرجع فقط `{ username, custom_id, restricted: true }`.
- **`backend/auth/auth-service.js`**:
  - `getPublicProfile(customId)`: أضيف `can_run_games` (مشتق من
    `permissions`) وتوسّع كائن `stats` من 3 حقول إلى 6 (أضيف
    `total_gifts`, `total_follows`, `total_live_ms`) — كلها حقول
    حقيقية كانت موجودة أصلاً في `getUserStats()` ولم تكن تُرسَل.

### أُضيف / تغيّر (Frontend)
- **`profile.html`**: إعادة بناء كاملة كصفحة مستقلة تماماً (تنسيق
  ذاتي داخل الملف نفسه، بدون أي اعتماد على `dashboard-core/css/
  style.css` أو `auth/auth.css` — لا تأثير على أي صفحة أخرى):
  - ثلاث حالات عرض حسب استجابة الخادم: تحميل، غير موجود، **مقيَّد**
    (اسم المستخدم + الـID فقط، لأي شخص غير صاحب الحساب/الأدمن)،
    و**كامل** (صاحب الحساب أو الأدمن).
  - رأس بروفايل جديد: صورة رمزية (حرف الاسم)، اسم المستخدم، الـID
    العام (مع زر نسخ)، شارات (أدمن/ستريمر/حالة موافقة الألعاب)، تاريخ
    الانضمام.
  - صندوقا "مستوى اللاعب (XP)" و"مستوى الستريمر (SP)" منفصلان تماماً
    (لا خلط بينهما) — كلاهما "قريباً" حالياً، لا أرقام وهمية.
  - ثلاثة تبويبات: **معلومات اللاعب** (معلومات الحساب + ربط/تحقق
    تيك توك — نفس منطق `dashboard-auth.js` بالضبط، فقط لصاحب الحساب؛
    عرض للقراءة فقط لو أدمن يشاهد حساباً آخر)، **معلومات الستريمر**
    (تظهر فقط لو `is_streamer` — الإحصائيات الست الحقيقية من البث +
    بقية الأقسام "قريباً": تحليلات الجمهور، أبرز اللحظات، البث القادم،
    الإنجازات، تفصيل الألعاب المبثوثة)، و**المقتنيات** ("قريباً" بالكامل
    — لا نظام إطارات/شارات فعلي).
  - تصميم متجاوب بالكامل (عمود واحد على الجوال، تبويبات قابلة للتمرير
    أفقياً، لا Scrollbar أفقي).
  - الألوان/الخطوط (Cairo) والبطاقات (`border-radius:16px`، حدود وردية
    شفافة، توهج خفيف) تتبع التصميم المرجعي المرسَل، بأقل توهج/حركة
    ممكنة حفاظاً على الأداء.

### لم يتغيّر
- تسجيل الدخول/الخروج/إنشاء الحساب (`login.html`, `signup.html`,
  `auth/auth-client.js`) — لا تعديل على أي دالة فيها.
- منطق ربط/تحقق تيك توك نفسه (`linkTikTok`, `requestTikTokVerification
  Code`, `verifyTikTok`) — نفس الاستدعاءات بالضبط، فقط شكل جديد للنموذج.
- `admin.html` وزر "View" فيه (يفتح `profile.html?id=...`) — يستمر
  بالعمل تماماً كما هو، لأن جلسة الأدمن تحصل على البروفايل الكامل تلقائياً.
- لوحة الستريمر (`dashboard-core`)، منطق الألعاب، وربط تيك توك بالبث
  المباشر — لا علاقة لهذا الإصدار بها إطلاقاً.
- لا تعديل على `backend/db/database.js` أو أي جدول — لا حقول جديدة،
  فقط حقول موجودة أصلاً صارت تُرسَل للواجهة.

### لم يُنفَّذ (وليش)
- نظام XP/مستويات اللاعب، ونظام SP/مستويات الستريمر، والمقتنيات
  (إطارات/شارات/ألقاب/تأثيرات)، وتحليلات الجمهور، وأبرز اللحظات،
  وجدولة البث القادم، والإنجازات: **لا يوجد أي منها في قاعدة البيانات
  حالياً**. حسب توجيه صاحب المشروع، لم يُخترَع أي Backend جديد لها —
  تُعرض بوضوح كـ"قريباً" في الواجهة فقط.
- إعادة تصميم `login.html`/`signup.html` بنفس الهوية البصرية الجديدة:
  لم تُطلَب صراحة في هذه المرحلة (الطلب اقتصر على صفحة البروفايل)،
  ولم تُنفَّذ لتفادي أي تغيير غير مطلوب.
- التصميم المرجعي المرسَل افترض React Components/TypeScript/Firebase/
  مكتبة أيقونات Lucide — لا شيء من هذا موجود في هذا المشروع (Vanilla
  JS بدون أي إطار عمل، SQLite، بدون Firebase إطلاقاً)؛ التصميم طُبِّق
  بصرياً (ألوان/خطوط/مسافات/بطاقات) ضمن نفس البنية التقنية الحالية دون
  أي Refactor كبير.
- مراجعة/تحسين التنسيق المتجاوب لبقية صفحات المنصة (غير صفحة البروفايل)
  لم تبدأ بعد في هذه المرحلة — يحتاج نقاشاً منفصلاً لتحديد نطاقه.

---

## [0.31.0] — لوحة الستريمر حصراً للأدمن + قفل الألعاب بموافقته

### السياق
اكتُشف بعد نشر [0.29.0] خللان: (1) `signup()` كانت تمنح كل حساب غير
الأول دور `role: 'streamer'` بالقاعدة **بغض النظر عن مربع "أبي أشتغل
كستريمر"**، و(2) `dashboard-core/index.html` (لوحة التحكم الكاملة:
غرف، جولات، تشغيل الألعاب) كانت محمية فقط بتسجيل الدخول (`requireAuth`)
بدون أي فحص آخر — أي حساب جديد كان يفتح له اللوحة كاملة مباشرة. بعد
نقاش مع صاحب المشروع تحدَّد التصميم الصحيح: **اللوحة حصراً لحساب
الأدمن**، وكل الحسابات الأخرى (عادي أو ستريمر) تُحوَّل دائماً لبروفايلها
العام. الفرق الفعلي بين حساب عادي وستريمر يصير بمكان آخر: **فتح الألعاب**
نفسها من الصفحة الرئيسية (أزرار "العب الآن") — مقفولة على أي زائر بدون
حساب أو حساب لم يوافَق عليه من الأدمن. لا حاجة لأي تعديل بقاعدة البيانات
— الآلية تستخدم صلاحية `can_run_games` الموجودة أصلاً من [0.29.0].

### أُضيف (Backend)
- **`backend/auth/auth-service.js`**: `login()`, `loginWithGoogle()`,
  و`validateSession()` صارت ترجّع `is_streamer` و`permissions`
  (كائن مُحلَّل، لا نص JSON خام) ضمن بيانات المستخدم — لم تكن هذي
  الحقول تصل للواجهة الأمامية سابقاً من هذي الدوال الثلاث تحديداً (كانت
  موجودة فقط من `listAllUsersWithStats` للوحة الأدمن). `signup()` صارت
  ترجّع `permissions: {}` أيضاً للتناسق. لا تعديل على أي منطق آخر.

### أُضيف (Frontend)
- **`auth/auth-client.js`**: دالتان جديدتان مستقلتان —
  - `canAccessDashboard(user)`: `true` لحساب الأدمن فقط، أي شيء غيره
    `false`. هذا هو الشرط الوحيد للوصول للوحة (`dashboard-core`).
  - `canPlayGames(user)`: `true` للأدمن، أو أي حساب معه
    `permissions.can_run_games === true`؛ غير ذلك `false`. هذا هو
    الشرط الوحيد لفتح روابط "العب الآن".
- **`dashboard-core/js/dashboard-auth.js`**: بعد `requireAuth` الناجح،
  فحص إضافي بـ`canAccessDashboard` — أي حساب غير الأدمن يُحوَّل فوراً
  لبروفايله العام (`profile.html?id=...`) بدل عرض اللوحة.
- **`login.html` و`signup.html`**: `goToDashboard()` الثابتة استُبدلت
  بـ`goAfterAuth(user)` تفحص `canAccessDashboard` (أدمن فقط) وتحوّل
  تبعاً لذلك — تطبَّق بعد الدخول بكلمة مرور، بجوجل، وبعد إنشاء حساب.
- **`index.html`**:
  - زر "الملف الشخصي" العلوي صار يحدَّد وجهته ديناميكياً (لوحة للأدمن
    فقط، بروفايل عام لغيره) بدل رابط ثابت للوحة.
  - أزرار "العب الآن" الثلاثة صارت محمية: زائر بدون حساب يُمنَع
    ويُعرَض له رابط دخول/تسجيل؛ حساب مسجَّل لكن غير موافَق عليه
    (`canPlayGames` = false) يُمنَع وتُعرَض له رسالة يحتاج فيها موافقة
    الأدمن؛ حساب موافَق عليه (أو الأدمن) يفتح الرابط عادي بدون أي تغيير.
    رسالة القفل عنصر واحد (`#games-lock-message`) يظهر ديناميكياً بدل
    نافذة `alert()`. فحص محلي متفائل من الجلسة المخزَّنة (بدون طلب
    شبكة إضافي)، بنفس أسلوب زر البروفايل تماماً.
- **`admin.html`**: أيقونة 🎙️ صغيرة بجانب عمود Role لأي حساب حدّد مربع
  "أبي أشتغل كستريمر" وقت التسجيل (`is_streamer`) — تساعد الأدمن يعرف
  مين يطلب الموافقة قبل ما يفعّل له `can_run_games`.

### لم يتغيّر
- شكل قاعدة البيانات بالكامل (`role` يبقى `admin`/`streamer` فقط —
  التمييز الفعلي صار عبر `can_run_games`، لا عبر `role` ولا عبر لوحة
  مشتركة أي وحدة).
- `admin.html` → `can_run_games` checkbox نفسه وسلوكه — هو نفسه آلية
  الموافقة الوحيدة، فقط الآن يتحكم بفتح الألعاب مو بدخول اللوحة.
- منطق الألعاب الخارجية نفسها (روليت، امبوستر، مافيا) — نفس الروابط
  الخارجية بالضبط، فقط الوصول لها صار مشروطاً من صفحتنا نحن.
- **ملاحظة صادقة**: الحماية هنا كلها من جهة الواجهة (Client-side)، بنفس
  أسلوب حماية `admin.html` الحالي — تعتمد على بيانات الجلسة المخزَّنة
  محلياً وقد تتأخر لحظياً عن قرار أدمن جديد (يلزم تحديث الصفحة). لا
  تُغني عن حماية أي إجراء حسّاس مستقبلي من جهة الخادم مباشرة لو احتاج
  الأمر لاحقاً — الألعاب نفسها (المواقع الخارجية) ليس لها أي علم بهذا
  النظام أصلاً.

---

## [0.30.0] — الـID العام (Public ID) لكل مستخدم + صفحة بروفايل عامة

### السياق
عمود `custom_id` كان موجوداً من [0.29.0] لكن اختيارياً وفارغاً افتراضياً
(المستخدم يسويه بنفسه لو حب). هذا الإصدار يجعله **إلزامياً وتلقائياً**:
كل حساب جديد يوصله رقم عام من 8 أرقام عشوائية فور التسجيل، ظاهر له
ولغيره. لا تعديل على `backend/db/database.js` (نفس العمود
`custom_id TEXT UNIQUE` من قبل).

### أُضيف (Backend)
- **`backend/auth/auth-service.js`**:
  - `generatePublicId()` — رقم عشوائي 8 أرقام (10000000–99999999)، يعيد
    التوليد لو تصادم مع رقم موجود (تحقق مباشر بقاعدة البيانات).
  - `signup()` و`loginWithGoogle()` (مسار الحساب الجديد فقط): يولّدان
    الـID تلقائياً ويخزّنانه بـ `custom_id` عند الإنشاء.
  - `validateSession()` و`listAllUsersWithStats()`: "شفاء ذاتي" —
    لو حساب قديم (من قبل هذا الإصدار) بدون `custom_id`، يُولَّد له واحد
    فوراً بدل ما يبقى فارغاً.
  - `getPublicProfile(customId)` جديدة — حقول عامة آمنة فقط (لا بريد،
    لا صلاحيات، لا الآيدي الداخلي).
- **`backend/http/auth-router.js`**:
  - `POST /api/admin/custom-id` (أدمن فقط) — يعدّل الـID العام لأي
    مستخدم (خلاف `POST /api/auth/custom-id` الموجود سابقاً اللي يقتصر
    على حساب الجلسة نفسها).
  - `GET /api/profile?id=<customId>` — مسار عام بدون تسجيل دخول، يرجّع
    بروفايل مختصر لأي مستخدم عبر `getPublicProfile`.

### أُضيف (Frontend)
- **`profile.html`** (جديد، جذر المشروع) — صفحة بروفايل عامة لأي زائر
  (بدون تسجيل دخول)، عبر `?id=<الـID العام>`: اسم المستخدم، الدور، حالة
  الستريمر، تيك توك (لو موثَّق)، تاريخ الانضمام، وإحصائيات مختصرة.
- **`auth/auth-client.js`**: `adminSetCustomId(userId, customId)` و
  `getPublicProfile(customId)` جديدتان.
- **`admin.html`**: عمود "Custom ID" صار قابلاً للتعديل مباشرة (حقل نص
  + زر Save لكل مستخدم) بدل عرض نصي فقط، بالإضافة لرابط "View" يفتح
  `profile.html` لنفس المستخدم.
- **`dashboard-core/index.html` + `dashboard-core/js/dashboard-auth.js`**:
  حقل "Custom ID" بقسم Account يُعبَّأ تلقائياً بالـID المولَّد عند فتح
  اللوحة (لم يكن يُعبَّأ سابقاً)، ورابط جديد "View my public profile".
- **`index.html`**: لا تغيير إضافي على هذا الإصدار (الأزرار العلوية
  عُدِّلت في تعديل سابق منفصل خارج هذا السجل).

### لم يتغيّر
- شكل عمود `custom_id` بقاعدة البيانات (نفس `TEXT UNIQUE` من [0.29.0]).
- `POST /api/auth/custom-id` (تعديل المستخدم لحسابه هو) — سلوكه كما هو.
- أي شيء آخر خارج نطاق هذا الإصدار (`AGP.*`, `websocket/`, `platforms/`).

---

## [0.29.0] — ربط نظام الحسابات: Auth + Admin HTTP API + واجهة كاملة

### السياق
`backend/auth/auth-service.js` + `backend/auth/password.js` +
`backend/db/database.js` (SQLite عبر `better-sqlite3`) كانت موجودة
بالكامل من مرحلة سابقة (حسابات، جلسات، دخول جوجل، تحقق ملكية تيك توك،
أدوار admin/streamer، صلاحيات، إحصائيات بث) لكن **غير موصولة بأي شيء
فعلياً** — لا مسار HTTP يستدعيها، ولا صفحة واجهة تستخدمها. هذا الإصدار
يوصّلها نهاية-لنهاية. لا تعديل إطلاقاً على الملفات الثلاثة أعلاه ولا
على `js/agp-*.js` أو منطق/تصميم `index.html` الحالي.

### أُضيف (Backend)
- **`backend/http/response.js`** — `sendJson`/`applyCors`، مسؤولية واحدة.
- **`backend/http/body-parser.js`** — قراءة/تحليل جسم JSON بحد أقصى
  لحجم الطلب، بدون أي مكتبة خارجية.
- **`backend/http/auth-router.js`** — راوتر HTTP كامل فوق
  `auth-service.js` كما هو (لا تعديل عليه): `/api/auth/signup`,
  `/login`, `/google`, `/logout`, `/me`, `/tiktok/link`,
  `/tiktok/verification-code`, `/tiktok/verify`, `/custom-id`، و
  `/api/admin/users`, `/api/admin/permissions` (محميان بدور `admin`).
  Middleware داخلي للتحقق من `Authorization: Bearer <token>` عبر
  `validateSession` الموجودة أصلاً.
- **`backend/config.js`**: إضافة `corsAllowedOrigins` (من env
  `CORS_ORIGINS`) — الجلسات تُمرَّر عبر Bearer فقط (لا كوكيز)، فعكس أي
  أصل مسموح به آمن هنا.
- **`backend/server.js`**: سطر تفويض واحد — أي طلب `/api/*` يمرّ الآن
  لـ `http/auth-router.js`. فحص الصحة على `/` لم يتغيّر إطلاقاً.

### أُضيف (Frontend — كله خارج `AGP.*` تماماً)
- **`auth/auth-client.js`** (جديد) — طبقة عميل مشتركة (`window.AGPAuth`،
  منفصل تماماً عن `window.AymanGamesPlatform`)، تخزين الجلسة في
  localStorage، ودالتا حراسة صفحات: `requireAuth`/`requireAdmin`.
- **`auth/auth.css`** — تنسيق تكميلي فوق `dashboard-core/css/style.css`
  (نفس متغيرات الألوان، بدون إعادة تعريف أي صنف موجود).
- **`login.html`** / **`signup.html`** (جذر المشروع) — بريد/كلمة مرور +
  زر Google (Google Identity Services)، بنفس النمط البصري لـ
  `dashboard-core`.
- **`admin.html`** (جديد) — جدول كل المستخدمين + إحصائياتهم + تبديل
  صلاحيات (`can_run_games`)، محمي بـ `requireAdmin`.
- **بوابة دخول للوحة الستريمر**: `dashboard-core/js/dashboard-auth.js`
  (جديد) + تعديل طفيف في `dashboard-core/index.html` (رابط CSS جديد،
  سطرا سكربت في أول `<body>`، حاوية حساب في `.topbar` الموجودة، وقسم
  "Account" جديد داخل تبويب Stream & Room الموجود — بجانب حقل TikTok
  username الحالي، دون لمسه). `dashboard-core.js` نفسه لم يُلمَس.

### تحقّق (Verified)
- تشغيل `backend/` محلياً واختبار كل مسار عبر curl: signup (أول مستخدم
  = admin تلقائياً)، تسجيل مكرَّر مرفوض، login يرجّع Token صالح، `/me`
  بدون Token أو بـ Token غير صالح يرجّع 401، `/admin/*` يرفض مستخدم
  `streamer` بـ 403، CORS Preflight يرجّع الترويسات الصحيحة.
- اختبار المتصفح الكامل (Playwright، Chromium): signup → تحويل تلقائي
  للوحة → logout → تحويل للدخول → دخول مباشر للوحة بدون جلسة يُبطَل
  ويحوَّل للدخول → login → ربط تيك توك + Custom ID من قسم Account يعملان
  فعلياً → `admin.html` لمستخدم `streamer` يحوَّل مباشرة للوحته (لا
  يمرّ بصفحة الدخول، بعد إصلاح صغير في `requireAdmin` أثناء هذا
  الاختبار نفسه — راجع أدناه). تأكَّد أن كل سكربتات `AGP.*` (الروليت،
  Game Bridge، إلخ) لا تزال تحمَّل وتعمل بنفس الترتيب والسلوك تماماً.

### تغيّر (Changed) — بدون أي تعديل على منطق AGP.* أو التصميم الحالي
- **`auth/auth-client.js`: `requireAdmin`** يقبل الآن معامل ثانٍ
  اختياري `nonAdminRedirectTo` — مستخدم مسجَّل دخول فعلياً بدور غير
  `admin` يُحوَّل مباشرة للوحته (افتراضياً) بدل صفحة الدخول (التي كانت
  ستكتشف صلاحية جلسته وتُعيد تحويله للوحته على أي حال) — اكتُشف هذا
  التسلسل غير الضروري أثناء اختبار Playwright أعلاه وأُصلِح فوراً.

### ⚠️ ملاحظات صادقة (لم تُختبَر فعلياً من هذه البيئة)
- **تسجيل الدخول بجوجل**: الكود يتبع توثيق Google Identity Services
  الرسمي بدقة (نفس النمط في `login.html`/`signup.html` وفي
  `auth-service.loginWithGoogle` الموجود أصلاً)، لكن لم يُختبَر ضد تدفق
  OAuth حقيقي كامل من بيئة التطوير هذه (لا نطاق إنتاج فعلي مصرَّح به
  هنا). يحتاج اختباراً حقيقياً على نطاق الإنتاج قبل الاعتماد الكامل
  عليه.
- **تحقق ملكية تيك توك عبر البايو**: نفس القيد الموثَّق مسبقاً في
  `auth-service.js` نفسه (لا وصول شبكي حقيقي لـ tiktok.com من هذه
  البيئة) — لم يتغيّر، فقط أصبح الآن قابلاً للاستدعاء فعلياً عبر
  `POST /api/auth/tiktok/verify`.
- **`CORS_ORIGINS` غير مضبوط بعد على نطاق الإنتاج الفعلي للواجهة
  الأمامية** (غير معروف من هذه البيئة) — الإعداد الافتراضي `['*']` آمن
  تقنياً (لا كوكيز، Bearer فقط) لكن يُفضَّل تضييقه لاحقاً في متغيرات
  بيئة Render.

---

## [0.28.0] — تدقيق نهائي قبل التجميد: إصلاح تسابق حالة بعد الفصل المتعمَّد

### أُصلح (Fixed)
- **تسابق حالة (Race Condition) في كلا الموصِّلين** (`tiktok-connector.js`
  الحقيقي و`mock-connector.js`): استدعاء `disconnect()` أثناء انتظار
  اتصال بطيء (شبكة حقيقية أو تأخير المحاكاة) لم يكن يمنع ذلك الاتصال
  المتأخر من "إحياء" الموصِّل لاحقاً — كان `connect().then()`/`.catch()`
  (الحقيقي) والمؤقّت المتأخر (المحاكاة) يُبلِّغان `onStatus('connected')`
  رغم فصل المستخدم يدوياً فعلاً، ويُعيدان `isConnected()` إلى `true` رغم
  إرجاعها `false` صحيحاً قبل ذلك. أُضيف نفس حارس `_intentionalDisconnect`/
  `_disconnected` المستخدَم أصلاً في مسارات أخرى من نفس الملفين، دون أي
  تغيير في الشكل الخارجي للموصِّل أو البروتوكول.
- **`ws-server.js`**: إضافة معالج `socket.on('end', ...)` — العميل الذي
  ينهي جهته (FIN) دون إطار WebSocket Close صريح (انقطاع شبكة، تبويب
  أُغلِق، تطبيق تحطَّم) كان يترك المقبس نصف مفتوح للأبد، فلا يُطلَق حدث
  'close' أبداً ولا يُنظَّف الاتصال أو موصِّله. أُثبتت المشكلة باختبار
  تحكُّم مباشر (0 من 5 عملاء أُغلِقوا فعلياً قبل الإصلاح، 200 من 200
  اتصال نُظِّف بعده تحت حمل حقيقي).

### تحقّق (Verified)
- محاكاة تحكُّم لكلا التسابقين (اتصال بطيء يُحل بعد الفصل المتعمَّد) —
  الحالة الداخلية والإشعارات الآن متجاهَلة تماماً بعد الفصل، مع تأكيد أن
  إعادة اتصال حقيقية لاحقة تعمل بشكل طبيعي تماماً.
- إعادة تشغيل اختبار الحمل الكامل (200 اتصال متزامن) بعد كل الإصلاحات:
  200 متصل = 200 مُزال، صفر أخطاء، لا تراجع في أي سلوك سابق.

---

## [0.27.0] — إصلاحات إنتاج بعد التبديل النهائي: تسريب اتصال مزدوج + إعادة اتصال المتصفح↔الخادم

### أُصلح (Fixed)
- **تسريب "اتصال مزدوج"** في `backend/websocket/ws-server.js`: وصول
  رسالة `connect` ثانية لنفس الاتصال كان يستبدل `activeConnector` دون
  فصل السابق أولاً — يترك موصِّل تيك توك القديم يعمل بالخلفية (بما فيه
  مؤقّتات إعادة اتصاله) ويكتب على نفس المقبس. الآن يُفصَل أي موصِّل
  نشط سابق بالكامل قبل إنشاء أي موصِّل جديد.
- **لا إعادة اتصال لقناة المتصفح↔الخادم الخلفي نفسها**: أُضيفت في
  `adapters/tiktok/agp-tiktok-adapter.js` — تأخير تصاعدي (Exponential
  Backoff + Jitter)، بلا حد أقصى لعدد المحاولات (خادمنا قابل للاسترداد
  دائماً، بخلاف تيك توك)، تتوقف فوراً عند قطع الاتصال يدوياً (بإعادة
  استخدام العلم `_connecting` الموجود أصلاً، دون أي علم أو دالة عامة
  جديدة). منفصلة تماماً عن إعادة اتصال الخادم بتيك توك.

### وثائق (Docs)
- تحديث `ARCHITECTURE.md` و`BACKEND_ARCHITECTURE.md` ليعكسا التبديل
  الفعلي للإنتاج، وتوثيق `rate-limiter.js` صراحة كـ"محجوز لعمل مستقبلي"
  بدل حذفه.

---

## [0.26.0] — التبديل النهائي للإنتاج: تفعيل موصِّل تيك توك الحقيقي

### تغيّر (Changed)
- **`backend/platforms/connector-router.js`**: التعديل الوحيد لهذه
  المرحلة. `'tiktok'` يُوجَّه الآن لموصِّل تيك توك الحقيقي بدل موصِّل
  المحاكاة. لا تعديل على أي ملف آخر — لا `ws-server.js`، لا البروتوكول،
  لا الواجهة الأمامية، لا AGP Core.
- موصِّل المحاكاة (`platforms/mock/mock-connector.js`) لم يُحذَف؛ يبقى
  متاحاً للاختبار المحلي بدون اتصال فعلي.

### تحقّق (Verified)
- سلسلة كاملة حقيقية: عميل WebSocket خام ← `ws-server.js` ← `connector-router.js`
  ← موصِّل تيك توك الحقيقي ← محاولة اتصال فعلية بخوادم تيك توك (فشلت
  بأمان لعدم وجود بث حقيقي، بشكل متوقَّع ومُعالَج بلا انهيار).

---

## [0.25.0] — موصِّل تيك توك الحقيقي: تنفيذ كامل + إعادة اتصال تلقائية

### أُضيف (Added)
- **`backend/platforms/tiktok/tiktok-connector.js`** — تنفيذ فعلي كامل
  (كان هيكلاً فارغاً)، فوق مكتبة خارجية واحدة معتمَدة صراحة
  (`tiktok-live-connector`، الاعتماد الوحيد في `backend/package.json`).
  يطبّق نفس شكل الموصِّل تماماً الذي يطبّقه موصِّل المحاكاة.
- تطبيع كامل لأحداث `chat`/`gift`/`follow` الواردة من المكتبة إلى نفس
  الشكل الموحَّد الموثَّق في البروتوكول (بما فيها تجميع تسلسل الهدايا
  القابلة للتكرار إلى حدث نهائي واحد فقط).
- إعادة اتصال تلقائية بتيك توك عند انقطاع غير متوقَّع بعد اتصال ناجح
  فعلاً (تأخير تصاعدي، حد أقصى 5 محاولات) — لا إعادة محاولة على فشل
  الاتصال الأول (اسم مستخدم خاطئ)، ولا على قطع اتصال متعمَّد.

### تحقّق (Verified)
- محاكاة كاملة لمسار إعادة الاتصال عبر مكتبة وهمية مضبوطة (تعذّر
  تفعيل انقطاع حقيقي منتصف الجلسة ضمن بيئة الاختبار)، ومحاولة اتصال
  حقيقية فعلية ضد بنية تيك توك التحتية (فشلت بأمان، مُعالَجة بلا انهيار).

---

## [0.24.0] — Phase 3–5: الأساس الخلفي (Backend) + جسر WebSocket + محاكاة كاملة

### أُضيف (Added)
- **`backend/`** (عملية Node.js مستقلة تماماً، بلا أي معرفة بـ `AGP.*`):
  خادم HTTP + WebSocket فعلي (تنفيذ يدوي كامل — مصافحة وتأطير رسائل —
  بدون أي مكتبة خارجية)، بروتوكول رسائل موثَّق (`connect`/`disconnect`/
  `status`/`comment`/`gift`/`follow`/`error`)، وموجِّه موصِّلات
  (`connector-router.js`) كنقطة التبديل الوحيدة بين المحاكاة والحقيقي.
- **`backend/platforms/mock/mock-connector.js`** — يولّد تعليقات/هدايا/
  متابعات محاكاة من طرف الخادم (بدل المتصفح كما كان سابقاً)، بلا أي
  معرفة بحالة AGP (لا يقرأ الكلمة المفعَّلة، تماماً كما سيتوجّب على أي
  موصِّل حقيقي مستقبلاً).
- **`adapters/tiktok/agp-tiktok-adapter.js`** — جسر WebSocket في
  المتصفح، يطبّق عقد `AGP.services.TikTokService` نفسه (تعديل في
  المكان، لا استبدال)، بلا أي معرفة بكون البيانات محاكاة أم حقيقية.
- **`adapters/mock/agp-mock-live-adapter.js`** (المرحلة السابقة لهذه):
  محاكاة محلية بالكامل في المتصفح، لا اعتماد على `backend/` — أول
  تطبيق فعلي لعقد `AGP.services.TikTokService`، ثم استُبدل لاحقاً
  بالجسر أعلاه لكنه محفوظ للاختبار بلا خادم خلفي.

### وثائق (Docs)
- **`docs/BACKEND_ARCHITECTURE.md`** (جديد) — تصميم كامل لـ`backend/`
  قبل أي تنفيذ: البروتوكول، دورة الحياة، إعادة الاتصال، الأمان، تحديد
  المعدّل.

### لم يتغيّر (Unchanged)
- AGP Core، Dashboard، بروتوكول WebSocket، عقد المحوِّل — صفر تعديل
  طوال كل هذه المرحلة، بما يتوافق مع تصميم "نقطة تبديل واحدة".

---

## [0.23.0] — مراجعة معمارية نهائية: إغلاق كل الفجوات الموثَّقة

### تغيّر (Changed) — بدون أي Manager/Service جديد
- **إغلاق الاستدعاء المزدوج لـ `onRoundEnd()`** (فجوة موثَّقة منذ
  [0.14.0]): أُضيف حارس تكرار مشترك `AGP._roundEndGuard` تستخدمه
  `agp-game-engine.js` (داخل `stop()`) و`agp-game-api.js` (داخل مستمع
  `session:roundFinished`) معاً — لا تُستدعى `onRoundEnd()` إلا مرة
  واحدة فعلياً لكل جولة، بصرف النظر عن ترتيب تنفيذ المسارين، ودون تعطيل
  أيٍّ منهما لو استُدعي بمفرده (لا يزال `AGP.gameEngine.stop()` يعمل
  منفرداً من الـ Console كما كان تماماً).
- **`agp-round-manager.js` أصبح يمر عبر `AGP.roomsManager`** (بدل
  `AGP.session` مباشرة) عند دخول `registration_open`/`idle` — يُغلِق
  فجوة [0.22.0] الموثَّقة (كانت `_activeRoomId` تبقى `null` رغم وجود
  جلسة فعلية). Fallback مباشر لـ `AGP.session` لا يزال موجوداً لو
  `agp-rooms-manager.js` غير محمَّل (استخدام مستقل).
- **`AGP.services.RoomsService.createRoom`/`getRoomState`** (في
  `agp-services.js`) لم تعودا Stub فارغتين؛ تُفوِّضان الآن فعلياً لـ
  `AGP.roomsManager` (استدعاء مؤجَّل داخل جسم الدالة، لأن الملف يُحمَّل
  قبل `agp-rooms-manager.js`). يُغلِق ازدواجية مفهوم "Room" بين
  الملفين. `joinRoom`/`leaveRoom` يبقيان بلا تنفيذ عمداً (اتصال شبكي
  حقيقي غير موجود بعد، لا مكافئ حالياً لهما في أي طبقة).
- **تصحيحات توثيقية** (لا تغيير سلوك): `agp-game-manager.js` كان
  يدّعي أن `agp-roulette.js` "لم يُعدَّل" (أصبح غير صحيح منذ [0.14.0])
  وأن `game:reset` يُستمَع له من وحدتين فقط (أصبح ثلاثاً منذ [0.14.0])
  — صُحِّحا. `docs/CLAUDE.md` كان ترتيب التحميل الموثَّق فيه ناقصاً
  `agp-rooms-manager.js` — أُضيف. `docs/ROADMAP.md` كان لا يزال يصف
  حالات Round Manager القديمة الست (`ReadyToSpin/Spinning/
  WinnerSelected`) رغم تعميمها في [0.19.0]، ويصف `RoomsService` كمهمة
  "قادمة" رغم اكتمالها في هذه المرحلة — كلاهما صُحِّح ليعكس الواقع.

### ✅ محصّلة المراجعة
- لا يوجد أي `AGP.xxx` مُسنَد في أكثر من ملف واحد (فُحِص آلياً عبر كل
  ملفات `js/`) — كل Namespace مملوك حصراً من ملف واحد.
- لا يوجد أي اسم حدث خاص بلعبة بعينها متبقٍّ في أي ملف منصة عام (فُحِص
  آلياً؛ الإشارات المتبقية لـ `wheelSpun`/`winnerSelected` كلها داخل
  تعليقات توثيقية تشرح السياق التاريخي فقط، لا كود فعلي).
- كل الفجوات المعمارية الموثَّقة صراحة في إصدارات سابقة ([0.10.0],
  [0.14.0], [0.22.0]) أُغلِقت فعلياً في هذا الإصدار.
- **الفجوة الوحيدة المتبقية (خارج AGP Platform نفسها عمداً)**: خريطة
  عرض حالات الجولة بالعربية في `dashboard/js/dashboard-pages.js`
  (`ROUND_STATE_LABELS`) لا تزال تحمل مفاتيح الحالات القديمة (موثَّقة
  في [0.19.0])؛ لم تُلمَس هنا لأن هذه المراجعة اقتصرت على AGP Platform
  فقط دون أي واجهة، بناءً على تعليمات صريحة.

## [0.22.0] — Rooms Manager: غرفة نشطة واحدة (بنية قابلة للترقية)

### أُضيف (Added)
- **`js/agp-rooms-manager.js` (جديد)**: `AGP.roomsManager` — تنفيذ فعلي
  لمفهوم "الغرفة" فوق `AGP.session` (بدون إعادة تنفيذ منطقه):
  `createRoom(gameId)`, `closeRoom()`, `getCurrentRoom()`,
  `hasActiveRoom()`, `getRoomState()`. **قيد صريح**: غرفة نشطة واحدة
  فقط — `createRoom()` يُرفَض إن وُجدت غرفة نشطة بالفعل. البنية
  الداخلية خريطة `roomId -> room` منذ البداية (لا متغيّر واحد)، خصيصاً
  لتسمح بترقية Multi-room مستقبلاً بإضافة معامل `roomId` للدوال العامة
  فقط، دون إعادة كتابة أي Manager آخر. أحداث `room:created`/
  `room:closed`.

### ⚠️ ملاحظة صادقة (لم تُصلَح، خارج نطاق هذه المرحلة)
- `agp-round-manager.js` قد يستدعي `AGP.session.createSession()`
  مباشرة عند دخول `registration_open` دون المرور عبر
  `AGP.roomsManager.createRoom()`، فتبقى `_activeRoomId` هنا `null`
  رغم وجود جلسة فعلية في هذه الحالة. توحيد المسارين يتطلب تعديل Round
  Manager، وتُرك لمرحلة منفصلة.

## [0.21.0] — Storage Manager + Score Manager (بدون منطق لعبة)

### أُضيف (Added)
- **`js/agp-storage-manager.js` (جديد)**: `AGP.storageManager` — غلاف
  عام فوق `localStorage` بمساحة اسم `agp:` وتسلسل JSON تلقائي
  (`get`, `set`, `remove`, `has`)، مع Fallback داخل الذاكرة إن لم يتوفر
  `localStorage`. لا اتصال شبكي.
- **`js/agp-score-manager.js` (جديد)**: `AGP.scoreManager` — سجل نقاط
  عام لكل لاعب (`addPoints`, `subtractPoints`, `setScore`, `getScore`,
  `getLeaderboard`, `reset`)، بأحداث `score:changed`/`score:reset`. لا
  يفرض أي قاعدة احتساب (تلك مسؤولية كل لعبة)، ولا يلمس `AGP.player`.
- كلاهما بدون واجهة أو اتصال فعلي أو كود خاص بأي لعبة. أُضيفا لترتيب
  التحميل في `index.html`/`dashboard/index.html` (بعد `agp-events.js`
  وبعد `agp-queue-manager.js` على التوالي).

## [0.20.0] — Timer Manager (بدون منطق لعبة)

### أُضيف (Added)
- **`js/agp-timer-manager.js` (جديد)**: `AGP.timerManager` — عدّادات
  تنازلية مسمّاة عامة (`start`, `pause`, `resume`, `stop`, `reset`,
  `getRemainingSeconds`, `isRunning`, `getActiveTimers`) عبر
  `setInterval` داخلي، بأحداث `timer:started/tick/paused/resumed/
  stopped/ended`. لا منطق لعبة، لا اتصال فعلي، لا واجهة. يُحمَّل مباشرة
  بعد `agp-events.js` (لا يعتمد إلا على core/events).

## [0.19.0] — Round Manager: تعميم كامل (إزالة أحداث الروليت)

### تغيّر (Changed)
- **`js/agp-round-manager.js`**: أُزيلت حالتا `spinning`/`winner_selected`
  (المرتبطتان بأحداث `game:wheelSpun`/`game:winnerSelected` الخاصة
  فعلياً بآلية عجلة الروليت) ودُمِجتا في حالة عامة واحدة `in_progress`.
  الحالة `ready_to_spin` أُعيدت تسميتها إلى `ready` (تسمية عامة). آلة
  الحالات الآن 5 حالات (`idle`, `registration_open`, `ready`,
  `in_progress`, `round_ended`) مبنية حصراً على مفردات `lobby:*`/
  `game:roundStarted`/`game:roundEnded`/`game:reset` العامة — لا يوجد
  أي اسم حدث خاص بلعبة بعينها داخل هذا الملف بعد الآن. مزامنة Session
  عند كل حالة لم تتغيّر في جوهرها (فقط أسماء الحالات المقابلة).
  `round:stateChanged` نفسه لم يتغيّر شكلاً.
- **لعبة الروليت غير متأثرة وظيفياً**: `games/roulette/agp-roulette.js`
  لا يزال يُبلِّغ `game:wheelSpun`/`game:winnerSelected` كما هي (لم
  يُعدَّل)، لكن Round Manager لم يعد يستمع لهما تحديداً؛ حالة الجولة
  تبقى `in_progress` طوال مدة الدوران واختيار الفائز، وتنتقل مباشرة
  لـ `round_ended` عند `game:roundEnded` كما كانت (النتيجة النهائية
  للانتقالات المتتابعة نفسها، فقط بتفاصيل أقل في المنتصف).

### ⚠️ ملاحظة صادقة (لم تُصلَح، خارج نطاق هذه المرحلة تحديداً)
- خريطة العرض العربية لحالات الجولة في `dashboard/js/dashboard-pages.js`
  (`ROUND_STATE_LABELS`) لا تزال تحتوي المفاتيح القديمة
  (`ready_to_spin`/`spinning`/`winner_selected`) ولا تعرف `ready`/
  `in_progress` الجديدين. Dashboard لن ينهار (fallback آمن موجود أصلاً:
  يعرض القيمة الخام بدل الترجمة العربية)، لكنه سيعرض `ready`/
  `in_progress` بالإنجليزية حتى تُحدَّث هذه الخريطة في مرحلة Dashboard
  قادمة — لم تُلمَس أي واجهة في هذه المرحلة بناءً على تعليمات صريحة.

## [0.18.0] — Queue Manager (بدون ربط فعلي)

### أُضيف (Added)
- **`js/agp-queue-manager.js` (جديد)**: `AGP.queueManager` — طابور
  لاعبين مرشَّحين (`enqueue`, `dequeue`, `peek`, `removeFromQueue`,
  `isQueued`, `getQueue`, `getQueueLength`, `clear`). يمنع التكرار
  داخل الطابور وضد اللاعبين المنضمين فعلاً (`AGP.player.hasPlayer`).
  `admitNext`/`admitAll` يقبلان فعلياً عبر
  `AGP.playerSource.submitPlayer` (بالمصدر المخزَّن مع كل عنصر). أحداث
  `queue:*`. لا اتصال فعلي، لا واجهة، لا كود خاص بأي لعبة. يُحمَّل بعد
  `agp-keyword-manager.js`.

## [0.17.0] — Keyword Manager (بدون ربط فعلي)

### أُضيف (Added)
- **`js/agp-keyword-manager.js` (جديد)**: `AGP.keywordManager` — إدارة
  كلمة انضمام واحدة (`setKeyword`, `getKeyword`, `activate`,
  `deactivate`, `isActive`, `checkKeyword`). عند القبول، تُمرِّر اللاعب
  حصراً عبر `AGP.playerSource.submitPlayer('keyword', ...)`، ويسجّل
  نفسه كمصدر `keyword` عند التحميل. أحداث `keyword:*`. لا اتصال فعلي،
  لا واجهة، لا كود خاص بأي لعبة. يُحمَّل بعد `agp-player-source.js`.

## [0.16.0] — Player Source Layer (بدون ربط فعلي)

### أُضيف (Added)
- **`js/agp-player-source.js` (جديد)**: طبقة عامة (`AGP.playerSource`)
  تُوحِّد إضافة اللاعبين من أي مصدر مستقبلي (`registerSource`,
  `submitPlayer`, `unregisterSource`, `getRegisteredSources`). لا تُدير
  قائمة لاعبين خاصة بها؛ تُطبِّع الطلب وتُمرِّره لـ
  `AGP.player.addPlayer` الوحيد فعلياً (Player Manager يبقى المالك
  الوحيد)، مع وسم كل لاعب بمصدره (`source`). أحداث `playerSource:*`
  منفصلة تماماً عن `player:*`. لا اتصال فعلي بأي مصدر (لا Stream
  Connector مفعَّل، لا TikTok). أساس جاهز ليستخدمه `agp-stream-
  connector.js` لاحقاً عند الربط الفعلي. يُحمَّل بعد
  `agp-player-manager.js` مباشرة في `index.html` و`dashboard/index.html`.

## [0.15.0] — Stream Connector Foundation (بدون اتصال فعلي)

### أُضيف (Added)
- **`js/agp-stream-connector.js` (جديد)**: مدير عام فوق خدمات البث
  Stubs الموجودة (`TikTokService`/`YouTubeService`/`TwitchService` من
  `agp-services.js`)، يفرض عقداً موحّداً (`connectToLiveStream`/
  `disconnectFromLiveStream`) ويتتبّع حالة كل منصة (`disconnected`/
  `connecting`/`connected`/`error`) عبر `AGP.events`
  (`stream:statusChanged`). الواجهة: `registerPlatform`, `connect`,
  `disconnect`, `getStatus`, `getSupportedPlatforms`,
  `getConnectedPlatforms`. لا اتصال فعلي بأي منصة، ولا أي كود خاص
  بتيك توك أو Widgets أو Dashboard هنا. يُحمَّل بعد `agp-services.js`
  مباشرة في `index.html` و`dashboard/index.html`.

## [0.14.0] — مزامنة Game Engine التلقائية + استخراج Game Bridge عام

### أُضيف (Added)
- **مزامنة تلقائية داخل `agp-game-engine.js`** (لا ملف جديد): استماع
  مباشر لحدثي `game:roundEnded` و`game:reset` الموجودين أصلاً، يُغلِق
  الفجوة الموثَّقة صراحة منذ [0.10.0] ("لا ربط تلقائي بعد بين 'اللعبة
  أبلغت بانتهاء الجولة' واستدعاء `stop()` تلقائياً"):
  - `game:roundEnded` -> `AGP.gameEngine.stop()` تلقائياً (فقط لو اللعبة
    المُبلِّغة هي المحمَّلة فعلياً وقيد التشغيل، بمقارنة `payload.id`).
  - `game:reset` -> `AGP.gameEngine.destroy()` تلقائياً (نفس الشرط، بدون
    اشتراط أن تكون قيد التشغيل).
  - عام تماماً (لا ذكر لأي لعبة بعينها)؛ يعمل لأي لعبة مستقبلية تُبلِّغ
    بنفس اسمي الحدثين، بما فيها ألعاب تيك توك القادمة.
- **`js/agp-game-bridge.js` (جديد)**: طبقة جسر `postMessage` عامة،
  مُستخرَجة بالكامل من المنطق الذي كان مكتوباً حصرياً داخل
  `games/roulette/agp-roulette.js` (فتح النافذة عبر `window.open`،
  تمرير أحداث دورة حياة Game Engine، استقبال أحداث اللعبة المُبلَّغة).
  الواجهة: `AGP.gameBridge.connect({ id, playLinkEl, incomingSource,
  outgoingSource, forwardedEvents, reportedEvents, onReady })` تُعيد
  كائن تحكّم `{ getGameWindow, sendToGameWindow, disconnect }`. لا يوجد
  أي معرّف لعبة أو نطاق ثابت داخل الملف نفسه — كل شيء يُمرَّر كخيارات،
  حتى يصبح أساساً جاهزاً لأي لعبة خارجية مستقبلية (بما فيها تيك توك)
  دون تكرار الكود أو تعديل هذا الملف نفسه.
- تحديث ترتيب تحميل السكربتات في `index.html` و`dashboard/index.html`:
  إضافة `js/agp-game-bridge.js` بعد `agp-game-manager.js` وقبل
  `games/roulette/agp-roulette.js` (تعديل ربط فقط).

### تغيّر (Changed)
- **`games/roulette/agp-roulette.js` أُعيدت كتابته** ليستخدم
  `AGP.gameBridge.connect(...)` بدل تكرار منطق `postMessage`/
  `window.open` يدوياً. **لا تغيير في السلوك الفعلي أو البروتوكول مع
  اللعبة**: نفس قيم `source` (`agp-platform` / `agp-roulette-integration`)،
  نفس أحداث دورة الحياة الممرَّرة، نفس أحداث اللعبة المُستقبَلة
  (`game:roundStarted/roundEnded/reset/wheelSpun/winnerSelected`)، ونفس
  سلوك "ready" (تحميل + تشغيل تلقائي). لا تعديل على تصميم أو منطق لعبة
  الروليت نفسها (المستودع الخارجي) إطلاقاً — فقط طريقة اتصال المنصة بها.

### ⚠️ ملاحظة معمارية صادقة (لم تُصلَح، موثَّقة فقط)
- بعد إضافة المزامنة التلقائية، قد يُستدعى `onRoundEnd()` الخاص باللعبة
  **مرتين** لنفس الجولة الفعلية عبر مسارين منفصلين: مرة عبر
  `AGP.gameEngine.stop()` (المسار الجديد)، ومرة عبر
  `agp-game-api.js` عند `session:roundFinished` (المسار القديم، الذي
  يُطلقه Round Manager تلقائياً في نفس اللحظة تقريباً). توحيد المسارين
  فعلياً يتطلب تعديل `agp-game-api.js`، وهو خارج نطاق هذه المرحلة تحديداً
  فتُرك كما هو. لا أثر ضار حالياً (الـ Hook الوحيد المتأثر في الروليت
  اليوم هو تسجيل رسالة Log فقط).

## [0.13.0] — Game Manager: واجهة موحّدة (Facade) فوق Game API + Game Engine

### أُضيف (Added)
- **`agp-game-manager.js`**: واجهة رقيقة فقط (Facade)، يعتمد عليها
  `AGP.gameManager`، بدون أي منطق جديد أو منقول:
  - `registerGame(game)` → تفويض مباشر لـ `AGP.gameAPI.register(game)`.
  - `unregisterGame(id)` → تفويض مباشر لـ `AGP.gameAPI.unregister(id)`.
  - `loadGame(id)` → تفويض مباشر لـ `AGP.gameEngine.loadGame(id)`.
  - `unloadGame()` → تفويض مباشر لـ `AGP.gameEngine.destroy()` (أقرب
    مكافئ موجود فعلياً لمفهوم "unload").
  - `getCurrentGame()` → تفويض مباشر لـ `AGP.gameEngine.getLoadedGame()`.
  - `getRegisteredGames()` → تفويض مباشر لـ `AGP.gameAPI.getAllGames()`.
  - لا يبث أي حدث بنفسه؛ كل أحداث `game:*` تُبَث أصلاً من
    `AGP.gameAPI`/`AGP.gameEngine` عند استدعاء دوالهما عبر هذا الملف.
- تحديث بسيط في ترتيب تحميل السكربتات داخل `index.html` لإضافة
  `js/agp-game-manager.js` بعد `agp-game-engine.js` وقبل
  `games/roulette/agp-roulette.js`.
- تحديث توثيقي في `CLAUDE.md` يوصي باستخدام `AGP.gameManager` كنقطة
  دخول موحّدة لأي كود **جديد** يتعامل مع الألعاب.

### ⚠️ ملاحظة معمارية صادقة (لم تُصلَح، موثَّقة فقط)
- **`games/roulette/agp-roulette.js` لم يُعدَّل** ولا يزال يستدعي
  `AGP.gameAPI`/`AGP.gameEngine` مباشرة (كما كان قبل هذه المرحلة)، وليس
  عبر `AGP.gameManager` الجديد — قرار متعمَّد بناءً على اختيار المستخدم
  صراحة (خيار "Facade فقط" بدل الدمج الكامل)، لتفادي أي تعديل غير
  ضروري على كود يعمل فعلياً مع لعبة حقيقية متصلة. النتيجة: يوجد الآن
  **مساران فعليان للوصول لنفس الوظائف** (المباشر القديم عبر
  `AGP.gameAPI`/`AGP.gameEngine`، والموحَّد الجديد عبر
  `AGP.gameManager`) حتى تُهاجَر الملفات القديمة لاحقاً إن رغب المستخدم.

### لم يتغيّر (Unchanged)
- **لا تعديل إطلاقاً على `agp-game-api.js` أو `agp-game-engine.js`** —
  كل المنطق والدوال والأحداث فيهما كما هي تماماً بدون أي نقل أو تكرار.
- لا تعديل على منطق أو تصميم لعبة الروليت أو أي عنصر واجهة قائم.
- لا Round Manager جديد، ولا Player Manager جديد، ولا تيك توك، ولا
  Cloudflare، ولا WebSocket.

## [0.12.0] — إعادة تسمية: agp-player.js → agp-player-manager.js

### تغيّر (Changed)
- **إعادة تسمية `js/agp-player.js` إلى `js/agp-player-manager.js`**
  (نفس المحتوى والمنطق بالكامل دون أي تغيير وظيفي — `addPlayer`,
  `removePlayer`, `reset`, `getAllPlayers`, `hasPlayer`,
  `getPlayersCount`, وأحداث `player:*` كلها كما هي تماماً)، لتوضيح أن
  هذا الملف هو **المسؤول الوحيد** عن إدارة اللاعبين على المنصة، بدل
  إنشاء وحدة "Player Manager" ثانية منفصلة كان يمكن أن تتعارض معه.
- تحديث عنوان الملف الداخلي والتوثيق أعلاه ليعكس دوره كمرجع وحيد.
- **تحديث كل المراجع لاسم الملف القديم عبر المشروع** (بحث شامل، لا مرجع
  متبقٍّ لـ `agp-player.js` في أي كود حي):
  - `index.html`: سطر التحميل + تعليق ترتيب التحميل.
  - `agp-lobby.js`: مرجعان في التوثيق الداخلي.
  - `agp-game-api.js`: مرجع واحد في التوثيق الداخلي (ترتيب التحميل المقترح).
  - `agp-events.js`: مرجع Namespace `player:*`.
  - `docs/CLAUDE.md`: ترتيب تحميل ملفات JS.
  - `docs/ROADMAP.md`: مرجعا حالة المرحلة 1 والمرحلة 2 (مع الإشارة
    للاسم القديم تاريخياً لمن يراجع السجل).
- **إدخالات CHANGELOG.md السابقة (مثل [0.4.0]) تُركت كما هي بدون تعديل**
  لأنها سجل تاريخي دقيق لما حدث فعلياً في حينه (كان اسم الملف
  `agp-player.js` وقتها فعلاً) — هذا الإدخال الحالي فقط يوثّق حدث
  إعادة التسمية نفسها.

### لم يتغيّر (Unchanged)
- لا تغيير على منطق أو تصميم أي لعبة (بما فيها الروليت).
- لا تعديل على `agp-round-manager.js` (لا يشير إليه إطلاقاً، فلم
  يحتَج أي تحديث).
- `AGP.player` يبقى الاسم البرمجي نفسه (لم يتغيّر أي كائن أو دالة، فقط
  اسم الملف على القرص).

## [0.11.0] — Round Manager: المسؤول الوحيد عن حالة الجولة

### أُضيف (Added)
- **`agp-round-manager.js`**: يعتمد عليه `AGP.roundManager`، ويحل
  القضية المعمارية المُبلَّغ عنها في 0.10.0 (أحداث `game:*` كانت تصل
  لـ `AGP.events` بلا أي مستمع فعلي):
  - ست حالات جولة رسمية: `Idle → RegistrationOpen → ReadyToSpin →
    Spinning → WinnerSelected → RoundEnded`، مع عودة لـ `ReadyToSpin`
    لجولة جديدة، أو لـ `Idle` من أي حالة عبر `game:reset`.
  - **الانتقال بين الحالات يحدث فقط عبر الاستماع لأحداث AGP Events
    موجودة أصلاً** (`lobby:opened/closed`, `game:roundStarted/
    wheelSpun/winnerSelected/roundEnded/reset`) — لا يوجد أي دالة
    `setState()` عامة يستدعيها كود آخر مباشرة.
  - **ربط مع `agp-session.js` بدون أي تعديل عليه**: عند كل انتقال حالة
    مناسب، يستدعي Round Manager دوال Session العامة الموجودة أصلاً
    (`createSession/openRegistration/closeRegistration/startRound/
    finishRound/endSession`) بدل إعادة تنفيذ أي من منطقها.
  - بث `round:stateChanged` عبر `AGP.events` عند كل انتقال ناجح
    (Namespace `round:*`، كان محجوزاً منذ إنشاء `agp-events.js`).
  - `getState()` / `getCurrentGameId()` للاستعلام فقط (قراءة، لا كتابة).
- **تعديل ضروري ودقيق في `agp-integration.js` (مستودع الروليت)**: تبديل
  ترتيب إرسال رسالتَي `game:winnerSelected` و `game:roundEnded` (الفائز
  أولاً ثم انتهاء الجولة) ليطابق تسلسل حالات Round Manager المطلوب
  (`Spinning → WinnerSelected → RoundEnded`)، رغم أن اللحظة الفعلية
  داخل اللعبة نفسها واحدة بالضبط. لا تعديل آخر على هذا الملف، ولا أي
  تعديل على `script.js`.
- تحديث بسيط في ترتيب تحميل السكربتات داخل `index.html` لإضافة
  `js/agp-round-manager.js` بعد `games/roulette/agp-roulette.js` وقبل
  `agp-bootstrap.js`.
- تحديث توثيقي بسيط في `agp-events.js` و`CLAUDE.md` لتمييز Namespace
  `round:*` كمُستخدَم فعلياً الآن.

### لم يتغيّر (Unchanged)
- **لا تعديل إطلاقاً على `agp-session.js`** — Round Manager يستدعي
  دواله العامة الموجودة فقط، دون أي تغيير في ذلك الملف.
- لا تعديل على منطق لعبة الروليت (`script.js`) أو تصميمها.
- لا تعديل على `agp-game-api.js` أو `agp-game-engine.js` أو `agp-core.js`.
- لا تيك توك، ولا Cloudflare، ولا WebSocket.

## [0.10.0] — اكتمال الربط الثنائي الاتجاه: أحداث اللعبة تصل إلى AGP Events

### أُضيف (Added)
- **اتجاه Game → Platform مكتمل الآن** (كان موثَّقاً كقيد معروف في
  0.9.0): أحداث فعلية داخل لعبة الروليت تصل الآن إلى `AGP.events` على
  منصة AGP، عبر `agp-integration.js` فقط (كما طُلب تحديداً):
  - `game:roundStarted` — عند نقر `#start-btn` أو `#play-again-btn`.
  - `game:roundEnded` / `game:winnerSelected` — عند ظهور نافذة الفائز
    (نفس اللحظة بالضبط في هذه اللعبة تحديداً؛ حدثان منفصلان على مستوى
    AGP Events لمرونة أكبر مع ألعاب مستقبلية).
  - `game:reset` — عند نقر `#reset-btn`.
  - `game:wheelSpun` — عند نقر `#spin-btn` (**نقطة مراقبة جديدة كلياً**،
    لم تكن موجودة في أي مرحلة سابقة).
- **تحديث `agp-integration.js` (مستودع الروليت)**: أُضيفت دالة
  `sendToOpener(type, payload)` مشتركة، واستُخدمت في كل نقاط المراقبة
  الأربع الموجودة أصلاً (إعادة استخدام، لا تكرار) بالإضافة لنقطة مراقبة
  خامسة جديدة (`#spin-btn`). لا تعديل على `script.js`/`style.css`/
  `index.html` أبعد من السطر الواحد المضاف في المرحلة السابقة.
- **تحديث `games/roulette/agp-roulette.js` (AGP Platform)**: التابع
  `handleMessageFromGameWindow` المستخدَم أصلاً لاستقبال رسالة `"ready"`
  وُسِّع (لا مستمع جديد منفصل) ليستقبل أيضاً الأنواع الخمسة أعلاه
  ويبثّها مباشرة عبر `AGP.events.emit(...)` بنفس الاسم، مع إضافة
  `payload.id = 'roulette-game'` للتمييز بين الألعاب مستقبلاً.
- تحديث توثيقي بسيط في `agp-events.js` لذكر هذه الأسماء الجديدة ضمن
  Namespace `game:*` الموجود أصلاً (لا Namespace جديد).

### لم يتغيّر (Unchanged)
- لا تعديل على `script.js` أو `style.css` أو تصميم أي من المشروعين.
- لا تعديل على `agp-game-api.js` أو `agp-game-engine.js` أو `agp-core.js`.
- لا ربط تلقائي بعد بين "اللعبة أبلغت بانتهاء الجولة" واستدعاء
  `AGP.gameEngine.stop()` تلقائياً على المنصة — يبقى قراراً مستقلاً
  متروكاً لمرحلة Round Manager قادمة (لتفادي فرض سلوك تلقائي غير مطلوب).
- لا Round Manager حقيقي، ولا تيك توك، ولا Cloudflare، ولا WebSocket.

## [0.9.0] — أول اتصال فعلي حقيقي: AGP Platform ↔ لعبة الروليت (postMessage Bridge)

### أُضيف (Added)
- **جسر اتصال حقيقي عبر `window.postMessage`** بين AGP Platform ولعبة
  الروليت (موقعان منفصلان تماماً/Cross-Origin)، باتجاه واحد فقط
  (Platform → Game) كما طُلب تحديداً:
  - `AGP.gameEngine.loadGame()` → رسالة `game:loaded` → `onLoad()`
    داخل `agp-integration.js` في مستودع الروليت.
  - `AGP.gameEngine.start()` → رسالة `game:started` → `onRoundStart()`.
  - `AGP.gameEngine.stop()` → رسالة `game:ended` → `onRoundEnd()`.
  - `AGP.gameEngine.destroy()` → رسالة `game:destroyed` → `onDestroy()`.
- **تحديث `games/roulette/agp-roulette.js` (AGP Platform)**:
  - زر "العب الآن" الآن يُفتَح عبر `window.open()` بدل رابط `target=_blank`
    عادي، للحصول على مرجع حقيقي (Reference) للنافذة المفتوحة (الشكل
    والنص والرابط لم يتغيّروا، فقط آلية الفتح).
  - انتظار رسالة `"ready"` من نافذة اللعبة (بدل التخمين وقت النقر
    مباشرة كما كان سابقاً) قبل استدعاء `loadGame()`/`start()` فعلياً —
    توقيت أدق وأصدق من الإصدار السابق.
  - استماع لأحداث `game:loaded/started/ended/destroyed` الصادرة أصلاً
    من `agp-game-engine.js` (بدون أي تعديل عليه)، وتمريرها كرسائل
    `postMessage` لنافذة اللعبة المفتوحة فقط (تحقق من `payload.id`).
- **تحديث `agp-integration.js` (مستودع الروليت)**:
  - استقبال رسائل المنصة (`handlePlatformMessage`) وتحويلها لاستدعاء
    الـ Hook المقابل، مع تحقق أمان صارم (`event.source === window.opener`
    بالضبط + تحقق من شكل الرسالة).
  - إرسال إشارة `"ready"` تلقائياً لـ `window.opener` (إن وُجد) فور
    جاهزية الصفحة لاستقبال الرسائل — يعمل فقط لو فُتحت الصفحة عبر
    `window.open()` من نافذة أخرى؛ لا يؤثر إطلاقاً على الاستخدام
    المستقل المباشر للعبة (بدون `window.opener` لا يحدث شيء).
- **تعديل ضروري وحيد على ملفات اللعبة**: إضافة سطر واحد في
  `index.html` الخاص بالروليت: `<script src="agp-integration.js"></script>`
  بعد `script.js` — بدونه الملف موجود لكن غير محمَّل أبداً. لا تعديل
  آخر على `index.html`، ولا أي تعديل على `script.js` أو `style.css`.

### ⚠️ حدود موثَّقة (Known Limitation، بلا تغيير عن المرحلة السابقة)
- الاتجاه المعاكس (Game → Platform) غير مربوط بعد: اللعبة لا تُرسِل
  تلقائياً أي إشعار للمنصة عند انتهاء الجولة فعلياً (فوز طبيعي). لذلك
  `AGP.gameEngine.stop()`/`destroy()` يبقيان استدعاءً يدوياً من طرف
  المنصة (Console أو تحكم إداري لاحق)، وهذا خارج نطاق هذه المرحلة تحديداً.

### لم يتغيّر (Unchanged)
- لا تعديل على منطق `script.js` إطلاقاً، ولا على `style.css`، ولا أي
  تصميم أو واجهة في أي من المشروعين.
- لا تعديل على `agp-game-api.js` أو `agp-game-engine.js` (الأحداث
  المستخدمة كانت موجودة وتُبَث أصلاً منذ مرحلة إنشائهما).
- لا Round Manager، ولا تيك توك، ولا Cloudflare، ولا WebSocket.

## [0.8.0] — أول ربط فعلي: لعبة الروليت كـ Plugin (Roulette Adapter)

### أُضيف (Added)
- **`games/roulette/agp-roulette.js`**: أول ملف "ربط" (Adapter/Plugin)
  حقيقي يربط لعبة موجودة فعلاً (روليت القبائل) بالمنصة عبر
  `AGP.gameAPI` و`AGP.gameEngine`، ليكون المرجع الذي تتبعه أي لعبة
  قادمة:
  - يُسجَّل بمعرّف `roulette-game` (مطابق لـ `data-agp-game-id` في
    `index.html`)، مع دوال دورة حياة: `onLoad`, `onRoundStart`,
    `onRoundEnd`, `onDestroy`.
  - يقرأ بيانات اللعبة (العنوان، الرابط، عنصر DOM) من
    `AGP.registry.getGame('roulette-game')` الذي اكتشفه `agp-registry.js`
    أصلاً من بطاقة `.game-card` — دون أي تعديل على تلك البطاقة.
  - يضيف مستمع `click` إضافي (بجانب السلوك الطبيعي غير المتغيّر) على
    زر "العب الآن" الموجود فعلياً، لتشغيل `AGP.gameEngine.start()`
    تقريبياً عند فتح اللعبة، دون تغيير `href`/`target`/نص/تصميم الزر.
  - التسجيل يحدث بعد حدث `platform:ready` (بعد اكتمال اكتشاف كل بطاقات
    الألعاب)، مع حالة دفاعية بسيطة لو كانت الصفحة جاهزة مسبقاً.
- تحديث بسيط في ترتيب تحميل السكربتات داخل `index.html` لإضافة
  `<script src="games/roulette/agp-roulette.js">` بعد
  `js/agp-game-engine.js` وقبل `js/agp-bootstrap.js` (تعديل ربط فقط،
  دون أي تغيير في التصميم أو منطق الألعاب الحالية).
- تحديث توثيقي في `CLAUDE.md` يوضّح تعريف مجلد `games/<game-id>/` كمكان
  ملفات ربط الألعاب القادمة، مع تنويه صريح حول محدودية الربط عندما
  تكون اللعبة مستضافة على نطاق خارجي منفصل (كما هو حال الروليت حالياً).

### ⚠️ قيد معروف وموثَّق (Known Limitation)
- لعبة الروليت مستضافة بالكامل على موقع خارجي منفصل
  (`https://aymn-games.github.io/roulette-game/`) ويُفتح في تبويب
  جديد؛ كودها الفعلي **غير موجود ضمن ملفات هذا المشروع**، لذا:
  - `onRoundStart()` يُستدعى فقط تقريبياً عند نقر زر "العب الآن" (لا
    يوجد اتصال حقيقي بمنطق اللعبة الداخلي).
  - `onRoundEnd()` **لا يُكتشف تلقائياً** (لا وسيلة اتصال بين تبويبين
    من نطاقين مختلفين)؛ يبقى تشغيله يدوياً فقط عبر
    `AGP.gameEngine.stop()` في هذه المرحلة.
  - أي ربط أعمق مستقبلاً (نتيجة الجولة الفعلية لحظياً) يتطلب إما دمج
    اللعبة عبر `iframe` مع بروتوكول `postMessage`، أو الوصول الفعلي
    لكود مستودع الروليت نفسه لإضافة نداءات صريحة لهذه المنصة.

### لم يتغيّر (Unchanged)
- لا تعديل على منطق أو تصميم أو رابط لعبة الروليت الفعلية، ولا على
  بطاقة `.game-card` الخاصة بها في `index.html`.
- لا Round Manager حقيقي، ولا أي ارتباط بتيك توك أو WebSocket أو
  Cloudflare في هذه المرحلة.

## [0.7.0] — محرك اللعبة المبدئي (Minimal Game Engine)

### أُضيف (Added)
- **`agp-game-engine.js`**: نسخة أولية (Minimal) من محرك اللعبة، يعتمد
  عليه `AGP.gameEngine`، ومسؤول فقط عن إدارة دورة حياة **لعبة واحدة
  محمَّلة حالياً**:
  - `loadGame(id)`: يجلب لعبة مسجَّلة مسبقاً عبر
    `AGP.gameAPI.getGame(id)`، يجعلها اللعبة النشطة الحالية أيضاً في
    `AGP.gameAPI` (عبر `setCurrentGame`)، ثم يستدعي `onLoad()`.
  - `start()` / `stop()`: تشغيل/إيقاف اللعبة المحمَّلة، مع استدعاء
    `onRoundStart()` / `onRoundEnd()` (تحكم يدوي صريح، منفصل عن التمرير
    التلقائي الموجود أصلاً في `agp-game-api.js` لأحداث Session).
  - `destroy()`: إيقاف اللعبة إن كانت تعمل، ثم استدعاء `onDestroy()`
    وتفريغ المحرك (لا يُلغي تسجيل اللعبة من `AGP.gameAPI`؛ ذلك يبقى
    منفصلاً عبر `unregister(id)`).
  - `getLoadedGame()` / `getLoadedGameId()` / `isRunning()`: دوال جلب
    حالة المحرك الحالية.
  - بث أحداث عبر `AGP.events` ضمن Namespace `game:*` (نفس Namespace
    المستخدم في `agp-game-api.js`): `game:loaded`, `game:started`,
    `game:ended`, `game:destroyed`.
- **تعديل بسيط ومقصود في `agp-game-api.js`**: نُقل استدعاء `onLoad()`
  من داخل `register()` إلى `agp-game-engine.js` (`loadGame()`)، لأن
  "التسجيل" في السجل و"التحميل الفعلي للتشغيل" مفهومان مختلفان، وهذا
  يمنع استدعاء `onLoad()` مرتين لنفس اللعبة. لا تغيير آخر على سلوك
  `register/unregister/getGame/getAllGames/setCurrentGame/getCurrentGame`.
- تحديث بسيط في ترتيب تحميل السكربتات داخل `index.html` لإضافة
  `js/agp-game-engine.js` بعد `agp-game-api.js` وقبل `agp-bootstrap.js`.
- تحديث توثيقي بسيط في `agp-events.js` و`CLAUDE.md` لتوضيح أن
  `agp-game-engine.js` يشارك Namespace `game:*` مع `agp-game-api.js`.

### لم يتغيّر (Unchanged)
- لا Round Manager حقيقي بعد؛ `start()`/`stop()` تحكم يدوي بسيط لهذه
  المرحلة فقط، وليس نظام جولات متعدد اللاعبين فعلي.
- لا منطق خاص بأي لعبة بعينها، ولا أي ارتباط بتيك توك أو WebSocket أو
  Cloudflare.
- لا تسجيل أو تحميل تلقائي لأي من الألعاب الثلاث الحالية (الروليت، مين
  الامبوستر، مافيا) في هذه المرحلة؛ المحرك جاهز فقط لربط أول لعبة لاحقاً.
- لا تعديل على منطق أو تصميم الألعاب الحالية أو أي عنصر واجهة قائم.

## [0.6.0] — الواجهة الموحّدة للألعاب (Game API Skeleton)

### أُضيف (Added)
- **`agp-game-api.js`**: هيكل (Skeleton) للعقد الموحّد الذي تلتزم به
  أي لعبة (حالية أو مستقبلية)، يعتمد عليه `AGP.gameAPI`:
  - `register(game)` / `unregister(id)`: تسجيل/إلغاء تسجيل كائن لعبة
    (Game Object) عن طريق `id` فريد.
  - `getGame(id)` / `getAllGames()`: جلب لعبة معيّنة أو كل الألعاب
    المسجَّلة.
  - `setCurrentGame(id)` / `getCurrentGame()`: تعيين/جلب اللعبة النشطة
    الحالية.
  - **تعويض تلقائي لدوال دورة الحياة الاختيارية** (`onLoad`,
    `onLobbyOpen`, `onLobbyClose`, `onRoundStart`, `onRoundEnd`,
    `onPlayerJoin`, `onPlayerLeave`, `onDestroy`) بدوال فارغة (No-op)
    عند التسجيل، حتى يبقى استدعاؤها آمناً دائماً دون تحقق مسبق.
  - **ربط بسيط بناقل الأحداث (تمرير فقط، بدون أي منطق لعب)**: تمرير
    أحداث `lobby:opened`, `lobby:closed`, `player:joined`,
    `player:removed`, `session:roundStarted`, `session:roundFinished`
    مباشرة إلى الدالة المقابلة في اللعبة النشطة الحالية فقط.
  - بث أحداث عبر `AGP.events` ضمن Namespace جديد (`game:*`):
    `game:registered`, `game:unregistered`, `game:currentChanged`.
- تحديث بسيط في ترتيب تحميل السكربتات داخل `index.html` لإضافة
  `js/agp-game-api.js` بعد `agp-lobby.js` وقبل `agp-bootstrap.js`
  (تعديل ربط فقط، دون أي تغيير في التصميم أو منطق الألعاب الحالية).
- تحديث توثيقي بسيط في `agp-events.js` لتمييز Namespace الـ `game:*`
  كمُستخدَم فعلياً الآن بدل كونه محجوزاً فقط لمرحلة قادمة.
- إضافة خطوة توثيقية في `CLAUDE.md` (قسم "طريقة إضافة لعبة جديدة")
  توضّح الفرق بين بطاقة العرض (`.game-card` + `agp-registry.js`) وكائن
  اللعبة البرمجي (`AGP.gameAPI.register(...)`).

### لم يتغيّر (Unchanged)
- لا يوجد Game Engine حقيقي بعد؛ لا منطق جولات أو قواعد لعب أو حساب
  نتائج، ولا Round Manager، ولا أي ارتباط بتيك توك أو Cloudflare.
- لا تسجيل تلقائي لأي من الألعاب الثلاث الحالية (الروليت، مين
  الامبوستر، مافيا) داخل `AGP.gameAPI` في هذه المرحلة؛ العقد جاهز فقط
  لاستخدامه لاحقاً.
- لا تعديل على منطق أو تصميم الألعاب الحالية أو أي عنصر واجهة قائم.

## [0.5.0] — مدير غرفة الانتظار (Lobby Manager Skeleton)

### أُضيف (Added)
- **`agp-lobby.js`**: هيكل (Skeleton) لمرحلة انتظار اللاعبين قبل بدء
  الجولة، يعتمد عليه `AGP.lobby`:
  - `open()` / `close()`: فتح/إغلاق التسجيل، بتفويض كامل لـ
    `AGP.session.openRegistration()` / `closeRegistration()` (Lobby
    Manager لا يحتفظ بحالة تسجيل مستقلة خاصة به).
  - `isOpen()`: يُقرأ مباشرة من `AGP.session.isRegistrationOpen()`.
  - `requestJoin(playerData)`: بوابة الانضمام الوحيدة — ترفض أي محاولة
    انضمام إذا كان التسجيل مغلقاً، وإلا **تُفوِّض الإضافة بالكامل** إلى
    `AGP.player.addPlayer(...)` دون إنشاء أي قائمة لاعبين موازية.
  - `getStatus()` / `printStatus()` / `debugJoin(id, name)`: دوال
    مساعدة بسيطة قابلة للاستدعاء مباشرة من الـ Console لاختبار غرفة
    الانتظار (فتح/إغلاق/انضمام/رفض) دون أي واجهة مؤقتة في الصفحة.
  - بث أحداث عبر `AGP.events` ضمن Namespace جديد (`lobby:*`):
    `lobby:opened`, `lobby:closed`, `lobby:playerAccepted`,
    `lobby:playerRejected`.
- تحديث بسيط في ترتيب تحميل السكربتات داخل `index.html` لإضافة
  `js/agp-lobby.js` بعد `agp-player.js` وقبل `agp-bootstrap.js` (تعديل
  ربط فقط، دون أي تغيير في التصميم أو منطق الألعاب الحالية).
- تحديث توثيقي بسيط في `agp-events.js` لتمييز Namespace الـ `lobby:*`
  كمُستخدَم فعلياً الآن بدل كونه محجوزاً فقط لمرحلة قادمة.

### لم يتغيّر (Unchanged)
- لا إضافة Round Manager أو Game Engine أو أي ارتباط بتيك توك أو
  WebSocket أو Cloudflare في هذه المرحلة.
- لا تعديل على منطق أو تصميم الألعاب الحالية أو أي عنصر واجهة قائم؛
  الاختبار يتم بالكامل عبر استدعاءات مباشرة من الـ Console.

## [0.4.0] — مدير اللاعبين (Player Manager Skeleton)

### أُضيف (Added)
- **`agp-player.js`**: هيكل (Skeleton) لإدارة قائمة اللاعبين داخل
  الجلسة الحالية، يعتمد عليه `AGP.player`:
  - `addPlayer(playerData)`: إضافة لاعب جديد، مع **تجاهل تلقائي لأي
    انضمام مكرر** بنفس الـ `id`.
  - `removePlayer(playerId)`: حذف لاعب من القائمة.
  - `findPlayer(playerId)` / `hasPlayer(playerId)`: البحث عن لاعب
    والتحقق من وجوده.
  - `getAllPlayers()` / `getPlayersCount()`: إرجاع نسخة آمنة من قائمة
    اللاعبين وعددهم.
  - `reset()`: تصفير قائمة اللاعبين بالكامل دون استبدال مرجع المصفوفة.
  - **ربط بـ Session Manager كمرجع فقط**: يعمل Player Manager فوق نفس
    المصفوفة المُدارة عبر `AGP.session.getPlayersRef()` مباشرة (لا
    يحتفظ بنسخته الخاصة عند وجود جلسة نشطة)، مع مصفوفة احتياطية داخلية
    فقط إن لم توجد جلسة بعد.
  - بث أحداث عبر `AGP.events` ضمن Namespace جديد (`player:*`):
    `player:joinRequested`, `player:joinRejected`, `player:joined`,
    `player:removed`, `player:listReset`.
- تحديث بسيط في ترتيب تحميل السكربتات داخل `index.html` لإضافة
  `js/agp-player.js` بعد `agp-session.js` وقبل `agp-bootstrap.js`
  (تعديل ربط فقط، دون أي تغيير في التصميم أو منطق الألعاب الحالية).
- تحديث توثيقي بسيط في `agp-events.js` لتمييز Namespace الـ `player:*`
  كمُستخدَم فعلياً الآن بدل كونه محجوزاً فقط لمرحلة قادمة.

### لم يتغيّر (Unchanged)
- لا إضافة Lobby أو Round Manager أو Game Engine أو أي ارتباط بتيك توك
  في هذه المرحلة — الهدف كان فقط تأسيس إدارة اللاعبين كوحدة مستقلة
  تتواصل عبر الأحداث بدل الاستدعاء المباشر.
- لا تعديل على منطق أو تصميم الألعاب الحالية أو أي عنصر واجهة قائم.

## [0.3.0] — ناقل الأحداث العام الموسَّع (Event Bus)

### أُضيف (Added)
- **`agp-events.js`**: الوحدة الرسمية لناقل الأحداث العام للمنصة:
  - إضافة دعم `AGP.events.once(eventName, handler)` فوق ناقل الأحداث
    الأساسي الموجود مسبقاً في `agp-core.js` (`on` / `off` / `emit`)،
    دون تعديل أو استبدال أي منطق موجود.
  - نسخة احتياطية كاملة (Fallback EventBus) تُستخدم فقط إذا لم يكن
    `AGP.events` موجوداً مسبقاً (حماية من ترتيب تحميل خاطئ).
  - توثيق داخلي لعقد الاستخدام العام واصطلاح تسمية الأحداث
    (`namespace:action`) وحجز Namespaces مستقبلية:
    `player:*`, `lobby:*`, `round:*`, `game:*`, `tiktok:*`,
    `cloudflare:*`, `network:*`، بالإضافة للـ Namespaces المستخدمة
    فعلياً حالياً: `session:*`, `registry:*`, `platform:*`.
- تحديث بسيط في ترتيب تحميل السكربتات داخل `index.html` لإضافة
  `js/agp-events.js` مباشرة بعد `agp-core.js` (تعديل ربط فقط، دون أي
  تغيير في التصميم أو منطق الألعاب الحالية).

### لم يتغيّر (Unchanged)
- لا إضافة أي Manager جديد (Player/Lobby/Round/Game Engine) في هذه
  المرحلة، ولا أي منطق خاص بلعبة أو تيك توك أو WebSocket — الهدف كان
  فقط تثبيت ناقل الأحداث كأساس عام تعتمد عليه كل الوحدات القادمة.
- سلوك `AGP.events.on/off/emit` الحالي كما هو تماماً؛ `agp-registry.js`
  و`agp-session.js` و`agp-bootstrap.js` يستمرون بالعمل دون أي تعديل.

## [0.2.0] — مدير جلسة البث (Session Manager Skeleton)

### أُضيف (Added)
- **`agp-session.js`**: هيكل (Skeleton) لإدارة جلسة البث، يعتمد عليه
  `AGP.session`:
  - تعريف حالات الجلسة (`AGP.session.STATES`): `IDLE`,
    `REGISTRATION_OPEN`, `REGISTRATION_CLOSED`, `ROUND_RUNNING`,
    `ROUND_FINISHED`, `SESSION_ENDED`، مع جدول انتقالات مسموح بها بينها.
  - إنشاء/إنهاء الجلسة (`createSession`, `endSession`).
  - فتح/إغلاق التسجيل (`openRegistration`, `closeRegistration`).
  - بدء/إنهاء جولة (`startRound`, `finishRound`) مع عدّاد جولة داخلي
    (`currentRound`).
  - كود انضمام مبدئي بسيط (`joinCode`) عبر توليد عشوائي (Placeholder).
  - مرجع مباشر لقائمة اللاعبين (`getPlayersRef`) دون أي منطق لإدارتهم.
  - بث أحداث عبر `AGP.events` عند التغييرات: `session:created`,
    `session:stateChanged`, `session:roundStarted`,
    `session:roundFinished`, `session:ended`.
- تحديث بسيط في ترتيب تحميل السكربتات داخل `index.html` لإضافة
  `js/agp-session.js` بعد `agp-registry.js` وقبل `agp-bootstrap.js`
  (تعديل ربط فقط، دون أي تغيير في التصميم أو منطق الألعاب الحالية).

### لم يتغيّر (Unchanged)
- لا إدارة فعلية للاعبين (Player Manager)، ولا Lobby، ولا Game Engine،
  ولا أي اتصال بتيك توك أو WebSocket في هذه المرحلة — كل ذلك مؤجّل
  لمراحل قادمة حسب `ROADMAP.md`.
- لا تعديل على منطق أو تصميم الألعاب الحالية (الروليت، مين الامبوستر،
  مافيا) أو أي عنصر واجهة قائم.
## [0.1.1] — توثيق المشروع (docs/)

### أُضيف (Added)
- إنشاء مجلد `docs/` كمرجع رسمي للمشروع، يضم:
  `PROJECT_VISION.md`, `PLATFORM_RULES.md`, `UI_GUIDELINES.md`,
  `ROADMAP.md`, `CHANGELOG.md`, `CLAUDE.md`.
- لا تغيير على أي كود أو تصميم في هذه المرحلة؛ الهدف تنظيمي/توثيقي بحت.

## [0.1.0] — البنية التحتية الأولى للمنصة

### أُضيف (Added)
- إنشاء الـ Namespace الرئيسي `window.AymanGamesPlatform` (اختصاراً `AGP`).
- **`agp-core.js`**:
  - إعدادات عامة للمنصة (`AGP.config`): اسم المنصة، النسخة، وضع
    Debug، ومفاتيح تفعيل الميزات (Feature Flags) لكل من:
    `realtimeSync`, `tiktokLive`, `youtubeLive`, `twitchLive`,
    `cloudflareWorkers`, `durableObjects`, `liveChatProviders`
    (جميعها معطّلة افتراضياً).
  - أداة تسجيل داخلية `AGP.log` تحترم وضع Debug.
  - ناقل أحداث داخلي (`AGP.events`) بنمط Publish/Subscribe
    (`on` / `off` / `emit`) للتواصل بين أجزاء المنصة مستقبلاً.
  - نقاط ربط مستقبلية معطّلة (`AGP.hooks`) لـ Cloudflare Workers،
    Durable Objects، WebSocket، RealtimeSync، ومزوّدي الدردشة المباشرة.
- **`agp-services.js`**:
  - هياكل خدمات فارغة (بدون تنفيذ فعلي، فقط رسائل تصحيح):
    `RoomsService`, `PlayersService`, `NetworkService`,
    `StorageService`, `TikTokService`, `YouTubeService`,
    `TwitchService`.
- **`agp-registry.js`**:
  - سجل داخلي للألعاب (`AGP.registry`) مع دوال `registerGame`,
    `getGame`, `getAllGames`.
  - اكتشاف تلقائي (`_discoverGamesFromDOM`) لبطاقات `.game-card`
    الموجودة في `index.html` (يقرأ العنوان، رابط اللعب، صورة الغلاف،
    والحالة) دون أي تعديل على الصفحة نفسها.
- **`agp-bootstrap.js`**:
  - نقطة تشغيل المنصة بعد اكتمال تحميل الصفحة (`DOMContentLoaded`
    أو تنفيذ فوري إن كانت الصفحة جاهزة بالفعل).
  - استدعاء الاكتشاف التلقائي للألعاب، ثم بث حدث `platform:ready`
    يحمل اسم المنصة، النسخة، وقائمة الألعاب المكتشفة.

### لم يتغيّر (Unchanged)
- لا تعديل على `index.html`، ولا على تصميم الواجهة، ولا على منطق
  الألعاب الحالية (الروليت، مين الامبوستر، مافيا)، ولا على النوافذ
  المنبثقة، عداد الزوار، أو بانر الاستريمر. كل الملفات الجديدة تعمل
  بشكل تراكمي (Additive) فقط فوق الصفحة الحالية.

---
