# إرشادات الواجهة (UI Guidelines) — AymanGamesPlatform

## 1) فلسفة تصميم الواجهة

الواجهة في هذه المنصة ليست "لوحة تحكم تقنية معقدة"، بل واجهة موجّهة
لجمهور عام يشاهد **بثاً مباشراً** ويحتاج أن يفهم ما يراه في ثوانٍ معدودة.
لذلك:

- **الوضوح أهم من الكثافة المعلوماتية**: أي عنصر جديد يُضاف يجب أن
  يجيب على سؤال "هل يحتاجه المشاهد/اللاعب الآن فوراً؟" قبل ظهوره.
- **التصميم الحالي (بطاقات الألعاب، البانر، النوافذ المنبثقة، عداد
  الزوار) هو المرجع البصري**، وأي إضافة مستقبلية يجب أن تتماشى معه
  شكلاً وسلوكاً (نفس الألوان، نفس منطق الحركة/الأنيميشن، نفس طريقة
  التموضع)، لا أن تفرض نمطاً بصرياً مختلفاً.
- **لا تغييرات بصرية بدون داعٍ**: أي تعديل على الواجهة الحالية يجب أن
  يكون مبرَّراً بحاجة وظيفية حقيقية، لا "تحسيناً جمالياً" عشوائياً.

## 2) البساطة وعدم الازدحام

- الصفحة الرئيسية تعرض الألعاب كبطاقات واضحة (صورة غلاف + عنوان + وصف
  مختصر + زر لعب)، وهذا النمط يجب أن يبقى هو الأساس حتى عند إضافة ألعاب
  جديدة أو ميزات جديدة.
- تجنّب إضافة عناصر واجهة متعددة تتنافس على انتباه المستخدم في نفس
  الوقت. أي ميزة جديدة (مثل مؤشرات حالة اللعبة أو إشعارات) يجب أن تكون
  **دقيقة الحجم وغير طاغية** على العناصر الأساسية (البانر، بطاقات الألعاب).
- الألوان والحركة (Animations) تُستخدم بحكمة لجذب الانتباه للعناصر
  التسويقية المهمة فقط (مثل بانر الاستريمر)، لا لكل عنصر في الصفحة.

## 3) ثبات لوحة التحكم

- أي عناصر تحكم أو معلومات ثابتة على الشاشة (مثل عداد الزوار الحالي
  أو بانر الاستريمر) يجب أن تبقى **في نفس الموضع وبنفس السلوك** بغض
  النظر عن حالة الصفحة أو التمرير، ما لم تتطلب اللعبة نفسها خلاف ذلك.
- لا تُستبدل لوحة التحكم أو تُعاد هيكلتها بالكامل عند إضافة ميزة جديدة؛
  الميزات الجديدة تُضاف كطبقة إضافية فوق ما هو قائم (مثلاً عبر عناصر
  جديدة مستقلة، لا عبر إعادة كتابة العناصر الحالية).
- أي تحكم إداري مستقبلي (خاص بالهوست مثلاً) يجب أن يكون منفصلاً بصرياً
  وواضحاً عن واجهة المشاهد العادي، دون تعقيد تجربة الأخير.

## 4) ظهور المعلومات المهمة فقط

- تُعرض للمستخدم فقط المعلومات التي تخدم قراره الفوري: اسم اللعبة،
  وصفها المختصر، وطريقة الدخول إليها (زر اللعب).
- المعلومات التقنية أو التشخيصية (مثل حالة الاتصال بالمنصة، سجلات
  الأحداث، تفاصيل السجل الداخلي للألعاب) تبقى **خلف الكواليس** (في
  الـ Console عبر `AGP.log`) ولا تُعرض للمستخدم النهائي أبداً ما لم
  تكن ميزة مقصودة صراحة (مثل لوحة تحكم إدارية منفصلة للهوست لاحقاً).
- أي رسائل تنبيه أو حالة (Status) للمستخدم يجب أن تكون **مختصرة، واضحة،
  ومؤقتة**، لا نصوصاً طويلة تُثقل الواجهة.

## 5) التوافق مع النمط الحالي

- أي عنصر HTML/CSS جديد يجب أن يتّبع نفس تسمية الأصناف (Class naming)
  المستخدمة حالياً قدر الإمكان (مثل `.game-card`, `.game-title`,
  `.btn-play`) لتسهيل التكامل مع `agp-registry.js` دون كود إضافي.
- الحفاظ على استجابة التصميم (Responsiveness) الحالية لكل الأجهزة
  (جوال/حاسوب) عند أي إضافة، دون كسر أي Breakpoint موجود.
