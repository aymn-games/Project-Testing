# قواعد ثابتة لتصميم واجهات الإعدادات (كل الألعاب)

هذه قاعدة هيكلية إلزامية لأي شاشة إعدادات أو صف خيارات في أي لعبة، حالية أو
مستقبلية، على منصة AGP.

1. **اتجاه الصفحة**: دائمًا RTL (`direction: rtl`).
2. **العنوان/اسم الخيار (Label)**: يُثبَّت بالكامل في أقصى اليمين
   (`text-align: right`).
3. **عنصر الإدخال/التحكم** (زر، مفتاح تبديل، عدّاد، شريط تمرير، مجموعة
   أزرار): يُثبَّت بالكامل في أقصى اليسار.
4. **توزيع الصف**: `display:flex; justify-content:space-between; align-items:center`
   بحيث يظهر العنوان يمينًا والعنصر التفاعلي يسارًا على نفس السطر.

## أين هذا مطبَّق فعليًا

الملف المشترك `js/agp-game-shell.js` هو من يبني كل صفوف الإعدادات
(`renderField()`) لجميع الألعاب. في الأنواع `pill-choice`/`pill-group`/
`counter`/`slider`/`toggle`/`modal-trigger` يخرج الكود دائمًا بترتيب
[عنصر التحكم، ثم `<span class="agp-shell-row-label">`]. القاعدة الثابتة
`.agp-shell-row-label{order:-1;}` (بالقرب من تعريف `.agp-shell-row` في
`js/agp-game-shell.js`) هي ما يقلب هذا الترتيب بصريًا: تجعل العنوان أول
عنصر في تدفق flex فيستقر على يمين الصف (لأن الاتجاه rtl)، بينما يبقى
عنصر التحكم في نهاية التدفق على اليسار.

**هذا التعديل نُفِّذ في الملف المشترك نفسه، وينطبق تلقائيًا على كل لعبة
تستخدم `renderField()` بدون أي تعديل إضافي في ملف اللعبة.**

## عند بناء أي لعبة جديدة أو شاشة جديدة

- إذا كانت الشاشة تستخدم نظام الحقول القياسي في `agp-game-shell.js`
  (`settingsFields`)، فالقاعدة مطبّقة تلقائيًا — لا حاجة لأي CSS إضافي
  لضبط الاتجاه.
- إذا بُنيت شاشة إعدادات مخصّصة (HTML/CSS خاص باللعبة، خارج نظام
  `renderField()`)، يجب الالتزام يدويًا بنفس القاعدة: صف flex واحد لكل
  خيار، `justify-content:space-between`، العنوان يمين، والعنصر
  التفاعلي يسار.
- ملاحظة: عنصر `<label class="agp-toggle-switch">` (زر التبديل نفسه) هو
  أيضًا وسم `<label>`؛ أي كود يبحث عن "أول label في الصف" لإضافة نص أو
  خاصية لعنوان الحقل يجب أن يستهدف `.agp-shell-row-label` تحديدًا أولًا
  (وليس أي `label`)، وإلا سيلتقط زر التبديل بالخطأ بدل نص العنوان.

## ملف `js/agp-game-shell.js` مشترك بين كل الألعاب

أي تعديل عليه يؤثر فورًا على كل الألعاب دفعة واحدة. هذا مقصود لتطبيق
معايير عامة كهذه، لكنه يستدعي حذرًا إضافيًا: أي تعديل مستقبلي على هذا
الملف يجب اختباره بصريًا على أكثر من لعبة واحدة قبل اعتماده، لأن أثره
غير محصور بلعبة واحدة.
